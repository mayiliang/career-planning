# Career Atlas 变更记录

本文件只记录用户可感知、已经落地并通过相应检查的变化。尚未完成的设想留在发布检查表，不写入已发布能力。

## 未发布 · 2026-08-20

### 新增

- 首次进入学习台时，直接展示一个真实知识点下的具体任务、固定输入、交付结果、完成判定和预计投入，不再只给“去学习”的泛化入口。
- 掌握挑战在答题前检查 AI 配置和连通性，并明确会发送给模型服务商的数据范围；AI 不可用时直接给出设置入口，不影响资料、笔记和站内练习。
- 求职支线支持手动创建岗位，以及中文/英文 CSV 的本地解析、逐行错误、服务端预览和全批校验后导入。
- 笔记增加本地即时草稿、1.2 秒自动保存、失败恢复和明确保存状态。
- 设置页增加可阅读个人数据 JSON 导出；备份恢复必须先完成完整性检查并显示当前值、恢复后值、差异和警告。

### 改进

- 统一对话框的 Escape 关闭、Tab 焦点循环、背景滚动锁定和关闭后焦点恢复；增加“跳到主要内容”入口并清理页面重复一级标题。
- 岗位详情不再依赖浏览器原生弹窗，操作结果和错误在页面内反馈。
- 普通请求增加超时和中文错误，避免网络异常时界面无限等待。
- 生产构建默认不公开源码映射；新增首屏、懒加载编译器、常规分块和总资源体积预算。

### 安全说明

- 浏览器 Worker 自检只用于本地反馈，不是安全沙箱，也不是服务端不可伪造的执行证明。
- 系统仍是单用户、本地优先产品，没有内置公网账号认证；不得未经认证网关直接公开部署。


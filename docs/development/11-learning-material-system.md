# 学习资料的组织与写作

本规范随 B01 首批重构落地，B02 于 2026-09-07 接入，B03、B04、B05 与 B06 于 2026-09-08 接入，B07、B08 与 B09 于 2026-09-09 接入，B10 与 B11 于 2026-09-10 接入，B12 于 2026-09-12 接入，B13、B14 与 B15 于 2026-09-14 接入，B16 于 2026-09-15 接入，B17、B18 与 B19 于 2026-09-19 接入，B20 与 B21 于 2026-09-20 接入，B22 于 2026-09-21 接入，B23 与 B24 于 2026-09-22 接入，B25 于 2026-09-23 接入，B26 于 2026-09-24 接入。已审校范围为 B01 的 JS-01、JS-02、JS-03、JS-07，B02 的 CS-01、CS-02、CS-03、JS-04，B03 的 JS-05、JS-06、TS-01、TS-02，B04 的 TS-03、WEB-01、REACT-01、VUE-01、VUE-02，B05 的 REACT-02、VUE-03、VUE-04、REACT-03，B06 的 REACT-04、VUE-05、REACT-05、VUE-06，B07 的 REACT-06、VUE-08、REACT-08、REACT-10，B08 的 VUE-07、REACT-07、VUE-10、REACT-09、VUE-11，B09 的 GIT-01、GIT-02、GIT-03、DEBUG-01，B10 的 ENG-01、ENG-02、ENG-03、ENG-05，B11 的 TEST-01、TEST-02、TEST-03、CAREER-01，B12 的 CAREER-02、CAREER-04、CAREER-05、WEB-02、WEB-03，B13 的 A11Y-01、BROWSER-01、BROWSER-02、WEB-04，B14 的 WEB-05、NET-01、SEC-01、SEC-02，B15 的 SEC-04、SEC-03、SEC-05、TS-04，B16 的 TS-05、TS-06、TS-07、TS-08、TS-09，B17 的 IDENTITY-01、IDENTITY-02、PRIVACY-01、PRIVACY-02，B18 的 NODE-01、NODE-02、NODE-04、AIDEV-01，B19 的 AIDEV-02、AIDEV-03、BIZ-01、BIZ-02，B20 的 BIZ-03、BIZ-04、BIZ-05、BIZ-06、BIZ-07，B21 的 BIZ-08、TEST-04、RENDER-01、RENDER-02，B22 的 DATA-01、DATA-02、REALTIME-01、COMP-01，B23 的 COMP-02、UX-01、ENG-08、LINUX-01，B24 的 LINUX-02、LINUX-03、LINUX-04、DOCKER-01、DOCKER-02，B25 的 ENG-06、DEPLOY-01、OBS-01、PERF-01，以及 B26 的 PERF-02、PERF-03、PERF-04、H5-01 和必要前置资料；其他批次不能据此宣称已完成同样的内容审校。

## 读者先得到什么

一篇主讲义应帮助读者建立可以解释代码的模型。开篇用真实的困惑说明学习理由，在解释术语之前给出足够的直觉；用独立可运行的例子呈现结果，再把结果与原因连起来。少见边界放在核心解释之后，参考资料放在文末并说明用途。

MDN 的 [学习内容写作指南](https://developer.mozilla.org/en-US/docs/MDN/Writing_guidelines/Learning_content)区分循序学习与详尽参考，强调学习目标和亲手尝试；[写作风格指南](https://developer.mozilla.org/en-US/docs/MDN/Writing_guidelines/Writing_style_guide)强调清晰、具体的表达与相关链接。本项目借鉴这种组织方法，中文正文与示例重新创作，不逐句翻译 MDN，不复制其整套例子，也不把资料改写成站内考题讲解。

## 三种资料各自承担一件事

| 层次 | 放什么 | 不承担什么 | B01 实例 |
| --- | --- | --- | --- |
| 主讲义 | 一个知识点的连贯解释、可观察的例子、使用边界 | 不把基础术语都塞在开头，不围着挑战题写 | 四份 `js-01/02/03/07-*.md` |
| 前置短文 | 可独立打开的基础概念，提供继续学习的入口 | 不要求读者先通读跨领域术语大全 | 变量、函数、对象、异常等八份短文 |
| 参考入口 | 官方定义、完整规则、兼容信息和进一步查询 | 不代替本站讲清核心知识 | 每篇末尾带用途说明的 MDN 链接 |

同一个重要概念只设一个主要解释入口。在别处需要它时，先给一句当前语境所需的提示，再链接到对应小节。允许从多个角度提及同一概念，但不要维护多份各自演化的“完整定义”。读者可以顺序学习，也可以沿引用补充背景后返回。

## 正文与目录怎样分工

- 正文真源：`docs/knowledge/chinese-guides/*.md`。定义、解释、示例、输出和参考来源保存在正文。
- 批次目录：[B01](../knowledge/materials/b01.json)、[B02](../knowledge/materials/b02.json)、[B03](../knowledge/materials/b03.json)、[B04](../knowledge/materials/b04.json)、[B05](../knowledge/materials/b05.json)、[B06](../knowledge/materials/b06.json)、[B07](../knowledge/materials/b07.json)、[B08](../knowledge/materials/b08.json)、[B09](../knowledge/materials/b09.json)、[B10](../knowledge/materials/b10.json)、[B11](../knowledge/materials/b11.json)、[B12](../knowledge/materials/b12.json)、[B13](../knowledge/materials/b13.json)、[B14](../knowledge/materials/b14.json)、[B15](../knowledge/materials/b15.json)、[B16](../knowledge/materials/b16.json)、[B17](../knowledge/materials/b17.json)、[B18](../knowledge/materials/b18.json)、[B19](../knowledge/materials/b19.json)、[B20](../knowledge/materials/b20.json)、[B21](../knowledge/materials/b21.json)、[B22](../knowledge/materials/b22.json)、[B23](../knowledge/materials/b23.json)、[B24](../knowledge/materials/b24.json)、[B25](../knowledge/materials/b25.json)、[B26](../knowledge/materials/b26.json)。登记顺序、阅读问题、摘要、学习目标、直接前置、概念主要归属与跨文档关系。
- 知识点入口：`docs/knowledge/knowledge-base/` 中 JavaScript/Web、TypeScript、React、Vue、工程质量与职业表达文件的对应学习资料记录。保留已有主讲义地址和名称，避免破坏既有入口与关联关系。
- 前置与发音范围：`apps/web/scripts/pronunciation-batches.json` 的 `b01` 至 `b26`；B04、B08、B12、B16、B20 和 B24 各包含五篇主讲义，B05、B06、B07、B09、B10、B11、B13、B14、B15、B17、B18、B19、B21、B22、B23、B25 和 B26 各包含四篇，均复用已有的独立前置入口。

目录中的 `concepts[].id` 是稳定概念标识；`heading` 对应正文中唯一的小节标题。`connections` 引用概念标识，并说明为什么值得交叉阅读。阅读器从同一份目录生成当前批次路线、阅读目标、知识连接和上一篇 / 下一篇，不另外维护一份页面标题表。

目录的 `reviewedOn` 按项目所在地 Asia/Hong_Kong 的日历日期，表示这一版内容完成语义审校的日期，不代表永远适用于所有浏览器版本。改变定义、示例或兼容结论时，需要重新核对来源与运行结果，不能只改日期。

## 前置和相关引用分开管理

“直接前置”表示进入当前讲义之前应具备的基础，只写在 `### 学习前先确认` 中，以 `- 直接前置：` 开头。已经能沿另一个直接前置找到的上游资料，不再重复列出。前置图必须无环。

“相关知识”解释当前概念与其他知识的联系，可以相互引用，不用于阻挡学习路线。例如，JS-01 的闭包指向 JS-03 的对象共享，JS-03 也可以返回 JS-01 的绑定说明；它们是补充理解的关联，不能因此生成循环前置要求。

链接统一使用相对于讲义目录的地址，例如：

```text
../chinese-guides/js-03-types-equality-copy-immutability.md#赋值之后谁和谁共用对象
```

小节链接使用实际标题生成的锚点。注册为目录概念的小节在站内打开时会保留完整讲义，只定位到对应标题，阅读记录仍使用 `js-03` 等主入口。原有 `/知识资料文件/小节锚点` 形式的站内概念链接也会转到对应完整文章。未登记的小节沿用旧的章节提取行为，不能靠随意改写地址掩盖坏链接。

从已登记的小节链接进入时，阅读器保留整篇正文并进入查阅状态，不把直接跳转的位置保存成整篇阅读进度。选择“从头阅读本篇”后，恢复正常阅读记录。通过整篇入口开始阅读时，原有的阅读进度规则保持有效。

改小节标题之前，要同时检查正文引用和目录。当前不维护任意标题的永久别名；若已有对外分享的深链接需要保留，应明确补充迁移规则。

## 中文表达的具体做法

先写“谁做了什么、结果怎样”，再引入术语。把连续几个抽象名词拆成可观察的动作：例如“两个变量指向同一个对象，改属性会影响两边”，比“对象身份关联产生共享可变状态”更容易读懂。

保留已经广泛使用或翻译后更别扭的英文，例如 `this`、Promise、Proxy、Reflect、receiver、trap。第一次出现时用中文解释它在当前代码中做什么，之后保持同一种叫法。普通句子中的代码名用行内代码；只对值得辨识或发音的关键术语加粗，避免每个英文变量旁边都出现播放按钮。

段落之间要有因果关系，不连续堆“注意”“边界”“约束”。标题描述这一节帮助读者解决的问题。表格用于比较可并列的规则，提示块用于少量会改变判断的重要前提；不要用大段清单代替讲解。自然中文不等于把精确条件省掉，特别是严格模式、生产者与消费者错误、属性描述符和资源关闭的边界。

## 示例就是可以核对的内容

已登记主讲义的每个 JavaScript 代码块都带唯一标识；可独立执行的示例把变量、模拟资源和调用在块内定义，不需要猜测前一段有没有运行。预期输出使用 `// =>`；复杂调用顺序可以把输出按实际顺序统一列在代码块末尾。

````text
```js example=js01-example-name
const count = 2;
console.log(count + 1); // => 3
```
````

标识只用于维护，阅读器的代码栏仍只显示语言。示例以现代浏览器为执行环境，异步示例显式等待完成。异常示例捕获并展示错误，不在后台留下未处理异常；涉及资源的例子优先使用内存模拟，不向读者的真实服务发请求。需要真实浏览器能力时标记 `runtime=browser`，例如 B02 的 Blob Worker 往返，并在 finally 中清理 Worker、定时器和临时 URL。带标识的 Nginx 配置以 `runtime=project` 登记，阅读器提供 Nginx 高亮；登记不代表已在 Nginx 实机执行。带标识的 Dockerfile 以 `runtime=project` 登记，只提取来源、不作为 JavaScript 执行；阅读器补充 Dockerfile 与 INI 高亮。带标识的 Bash 片段以 `runtime=project` 登记，和 PowerShell 一样不进入通用 JavaScript 执行器；没有标识的历史命令说明保持原有行为。需要展示平台差异时，单独声明前提和结果，不能让示例依赖某个未说明的控制台习惯。

`learning-material-examples.mjs` 从实际正文提取代码和预期输出。`check-curated-materials.mjs` 复用有超时限制的独立 worker，核对 B01 至 B26 的通用示例；浏览器专用示例和框架项目按实际环境另行核对。这验证的是示例与标注是否一致，无法替代对概念、语言质量和知识覆盖的审校。

## 修改后的核对顺序

1. 对照知识点范围逐项检查解释入口，确认重要知识没有被更流畅的文风一起删掉。
2. 从零阅读每个示例：输入是什么，预期看到什么，哪个改动能揭示规则，读者是否拥有全部定义。
3. 核对官方来源；遇到 MDN、规范或不同引擎结果不一致时，记录版本与实际行为，收窄结论。
4. 更新目录、正文引用和知识点资料说明。只更新当前批次的发音资源。
5. 运行 `pnpm content:check`，检查内容结构、直接前置、概念归属、小节链接、示例输出与发音一致性。
6. 系统面向桌面，检查本次改动涉及的桌面章节导航、交叉引用与浏览器专用示例。以内容和引用核对、必要构建为主；只有关键功能有改动或发现问题时才增加针对性检查，不默认扩展移动端适配或完整测试套件。

字符数下限只用于发现意外截断，不作为内容质量评分。完整检查同时包含可追溯的概念入口与实际运行结果，也需要编辑者通读中文。B01 至 B26 当前采用这种管理方式；推广到后续批次时，先逐批审校内容，再加入目录，不能批量套模板后就标记完成。

## B01 这一版的审校重点

| 知识点 | 保留并重新组织的核心内容 | 特别核对的边界 |
| --- | --- | --- |
| JS-01 | 调用、作用域、初始化、闭包、独立状态、循环与回调清理 | 闭包读取绑定；脚本和模块顶层不同；解除监听不等于所有引用消失 |
| JS-02 | 属性与原型、普通调用与箭头、getter、构造、class | 严格性取决于函数定义；receiver 与属性所在对象不同；bind 与 new |
| JS-03 | 类型、身份、比较、转换、浅拷贝、克隆、更新与冻结 | 结构共享不允许以后随意修改；克隆不等于保留自定义行为 |
| JS-07 | 迭代入口、生成器、next/throw/return、异步、取消、Proxy/Reflect | next 抛错由生产者清理；finally 可以暂停；代理须尊重不变量 |

## B02 这一版的审校重点

先给具体输入，再展示中间过程，最后归纳规则。抽象部分优先使用可计算的小数据、对照表和会改变答案的反例；完整示例不能依赖未定义的业务函数。

| 知识点 | 主要讲解入口 | 特别核对的边界 |
| --- | --- | --- |
| CS-01 | 订单查找计数、规模翻倍、摊还扩容和分配成本 | Map 不承诺严格 O(1)；重复键可能改变查找含义；复杂度的界不等于最坏情况 |
| CS-02 | 二分区间、BFS 发现顺序、拓扑入度、贪心交换和 DP 表 | lower_bound 的答案边界可等于右端点；窗口示例要求非负数；重复边去重；剩余节点不一定全在环上 |
| CS-03 | 增量计数、分块、完整 Worker、转移和任务版本 | 在途上限也要约束上游；取消消息不能打断同步循环；terminate 不运行普通清理；转移影响同 buffer 的视图 |
| JS-04 | 同步与回调顺序、微任务队列、await、启动与提交 | resolved 未必 fulfilled；微任务让步不保证绘制；all 不取消其他工作；拒绝旧结果不等于停止旧计算 |

## B03 的多文件与类型示例

JavaScript、TypeScript 代码都保留在正文，不另外维护一份手写示例源码。ES Modules 示例用相同 example 标识组成一组，每个代码块声明 `runtime=browser file=文件名.mjs`，组内必须有 `main.mjs` 入口；读者可见的正文同时标出文件名和 HTTP 加载方式。一组文件作为一个示例核对，不能把其中的 import/export 当成普通函数体执行。

TypeScript 示例使用 `ts example=唯一标识`。现有内容检查先在内存中为每段创建独立模块，用项目 TypeScript 5.7.2 和 strict、noUncheckedIndexedAccess、exactOptionalPropertyTypes 核对真实源码，再转换并比对运行输出。预期非法写法用 @ts-expect-error 标明，并解释原因；不执行的错误函数只服务于观察诊断，不让学习页面留下未处理异常。编译通过不能替代语义审校，尤其显式类型谓词可能错误地承诺真假分支。

| 知识点 | 主要讲解入口 | 特别核对的边界 |
| --- | --- | --- |
| JS-05 | 外层提前结束、return await、两种 finally、取消与旧清理 | Promise.finally 的普通返回值不替换原结果；try/finally 中 return 会覆盖；超时不等于远端回滚 |
| JS-06 | 八组多文件程序、依赖图、循环失败与拆分 | 静态导入是绑定；普通解构是取值；循环读取未初始化 const 会失败；导入缓存不等于业务初始化幂等 |
| TS-01 | 静态视图与实际值的对照、明确的类型错误、完整解析入口 | strict 不自动开启索引和精确可选属性两项；as、readonly 不验证或冻结；结构赋值不删字段 |
| TS-02 | 分支可能性表、真假反例、守卫、穷尽检查与请求状态机 | 谓词的 false 分支也必须正确；保留旧数据刷新可以是合法状态；类型不证明异步提交资格 |

本批次仍以桌面阅读和内容质量为主，验证集中于正文示例、模块加载、引用、必要构建与关键阅读路径，不默认增加移动端或完整回归测试。

## B04 的页面与框架示例

B04 从四篇一组扩展到五篇，路线栏按目录中的篇数生成桌面列数。React 与 Vue 的关联用于对照理解，不把互相引用误写成循环前置。

TS-03 的 14 段 TypeScript 示例沿用严格类型与输出核对。WEB-01 的 FormData 和 VUE-02 的 13 段响应式模块需要浏览器；后者在带 Vue 导入映射或 Vite 的环境中执行。模块源码保留实际 import，不能把 Vue 伪造成手写替身来验证。

页面与工程片段使用 `runtime=project`，必要时声明 `file=src/App.vue` 等路径；TSX、Vue 和 HTML 示例默认归为这一类。它们由正文说明运行前提、文件如何组合、操作与可见结果，不硬凑 console 输出。读取器会登记这些片段，但通用 worker 不执行它们；内容检查的成功不能宣称已经验证框架交互。桌面核对从正文提取完整表单、React 组件、Vue SFC 与依赖文件，在实际框架中查看关键行为。配置片段按其工具环境理解，不作为独立浏览器脚本执行。

| 知识点 | 讲解入口 | 特别核对的边界 |
| --- | --- | --- |
| TS-03 | 数组取值、字段读取、外部允许列表、类型化列 | 泛型约束不允许制造任意子类型；keyof 不等于运行时键枚举；列构造器保留关联 |
| WEB-01 | 检索、错误提示、保存与取消设置 | name 与 id 不同；同名字段使用 getAll；每次打开清空 dialog 返回值，取消不保存草稿 |
| REACT-01 | 两种加三次、延迟消息、草稿重置与派生列表 | setter 不改本轮绑定；快照不是深拷贝；Strict Mode 额外计算不是额外点击 |
| VUE-01 | 三个入口文件、两张独立卡片、别名与资源 | script setup 按实例执行；公开 env 不是秘密；代理与 HMR 不进入静态产物 |
| VUE-02 | 原对象绕写、解构、筛选、浅层替换与 nextTick | 普通解构与 props 编译特例不同；浅层变更需通知；nextTick 不保证绘制 |

继续优先投入中文内容、解释与例子；仅进行必要构建和桌面阅读/示例核对，不为本批新增移动端或全面回归任务。

## B05 的组件协作与状态示例

B05 以“数据由谁保存、子层怎样提出修改、用户当前能看见什么”为主线，连接 React 组件协作、Vue 模板和组件接口，最后回到状态建模。49 个概念入口和 16 条目录关联既支持顺序阅读，也支持跨框架对照；React 与 Vue 之间的对照不增加直接前置。

| 知识点 | 讲解入口 | 特别核对的边界 |
| --- | --- | --- |
| REACT-02 | 完整工作台、互斥面板、children、render prop、列表备注和异步保存 | 筛选是否清除选择由业务决定；稳定 key 保留记录身份；异步回调需要约定完成与失败 |
| VUE-03 | DOM 绑定、显示与销毁、列表重排、事件、表单中间值和中文输入 | 布尔属性与 ARIA 字符串不同；prevent 与 stop 不同；组合输入结束不应重复提交 |
| VUE-04 | 七组父子文件，逐步展示 props、model、slots、attrs 和 dialog | emit 不自动冒泡也不能等待保存；类型不替代运行时解析；Teleport 不负责模态与焦点 |
| REACT-03 | 数量输入、刷新状态、受控值、版本草稿、归一化集合和撤销 | 合法中间态不等于有效提交值；草稿保留基线；同一记录更新不应随版本 key 丢弃编辑 |

本批 34 个 TSX/Vue 代码块组成 27 组完整场景：12 个 React 组件、8 个 Vue 模板页面、7 组 Vue 父子组件。多文件例子在正文明确给出文件名及其组合方式，代码直接从正文提取核对，避免维护另一份悄悄修好的示例。核对环境为 React 19.2.0、Vue 3.5.39、TypeScript 5.7.2 与桌面 Chrome 152；包括 React 严格类型检查、Vue SFC 编译与严格模板检查，以及场景中的关键操作。

Vue 示例明确使用 Vue 3.5：useId 需要 3.5，defineModel 与 defineSlots 等宏按对应版本理解。中文输入场景用合成 composition/键盘事件核对提交守卫，这不等于已经验证真实系统输入法的全部事件顺序。读者在实际项目中仍需用目标桌面输入法体验选词、回车和提交。

维护检查集中于正文、引用、发音和桌面阅读，不扩展移动端工作或完整测试套件。

## B06 的副作用与逻辑复用

B06 按 Effect、Vue 生命周期、自定义 Hook、Composable 的顺序展开。每篇先解释具体页面中的工作，再讨论复用；47 个概念入口与 16 条目录关联连接本批内容和前面的状态、组件、渲染基础。清理、暂停和共享各有独立含义，跨框架对照不作为额外前置。

| 知识点 | 讲解入口 | 特别核对的边界 |
| --- | --- | --- |
| REACT-04 | 键盘监听、暂停计数、快慢请求和通知格式 | cleanup 管理本轮资源；取消与结果资格不同；Effect Event 不能省略同步身份依赖 |
| VUE-05 | DOM 观察、watch 来源、请求、flush、KeepAlive 和加载重试 | 停用不等于卸载；参数 onCleanup 与 onWatcherCleanup 限制不同；Suspense 不代替错误恢复 |
| REACT-05 | 独立计数、条件工作、防抖搜索和共享快照 | 始终调用 Hook，在内部控制工作；清理等待和请求；快照不变时保持身份 |
| VUE-06 | 独立计数、四种输入、显式作用域、嵌套注入和延后发布 | toValue 在追踪中读取；共享来自同一实例；独立作用域显式停止；readonly 不冻结原值 |

正文的 27 个项目代码块组成 19 组完整场景，包括 8 个 React 页面和 11 组 Vue 页面及其依赖文件。复用正文提取器，按实际 import 递归组合文件，运行 React 严格类型、Vue SFC 编译和严格模板检查；桌面操作只核对本批关键行为和卸载清理，不扩展完整测试套件。

核对环境为 React 19.2.0、Vue 3.5.39、TypeScript 5.7.2 与 Chrome 152。异步组件示例保留动态 import 的独立 ESM 分块，让浏览器按真实模块加载；把动态模块简单内联成普通对象不能替代该场景。示例的模拟请求用于解释失效和恢复，不声称覆盖真实网络、后端写入、SSR 或所有宿主暂停行为。

仅更新 B06 的 22 个重点术语发音。阅读器沿用桌面路线、概念目录、相关引用和查阅状态，不新增移动端投入。

## B07 的共享状态、恢复与路由数据

B07 先说明共享状态的变化规则和寿命，再讨论失败后的恢复与页面导航。四篇共 48 个概念入口、16 条目录关联；既能顺序学习，也能回到 B05 的状态建模、B06 的副作用与复用小节。直接前置和跨框架对照继续分开维护。

| 知识点 | 讲解入口 | 特别核对的边界 |
| --- | --- | --- |
| REACT-06 | 删除与选择联动、手动交付乱序回执、嵌套 Provider | ID 在事件中生成；旧回执失去资格；Context 传播不改变状态所有者；拆分读写不保证组件永不重渲染 |
| VUE-08 | 两处共享清单、快慢读取与重置、版本化显示设置 | Setup Store 返回原始 state ref；卸载视图不销毁 store；重置不等于取消；未知存储不自动覆盖 |
| REACT-08 | 渲染与 Effect 失败、可控 Promise、保留旧内容、按需代码 | 同步 Effect setup 与普通异步回调不同；只重置边界不重做资源；新 Promise 才开始新一轮演示；区域外笔记保留 |
| REACT-10 | URL 标签、嵌套布局、loader/action 表单、fetcher、离开草稿 | 返回字段错误与抛出路由错误不同；写入后重新读取；草稿保留版本基线；导航取消不保证远端回滚；框架模式不等于只能 SSR |

正文的 23 个项目代码块组成 13 组完整场景，包括 10 个 React 页面和 3 组 Vue 页面。共享文件在正文标明组合方式，提取后使用实际依赖核对 React 严格类型、Vue SFC 编译与严格模板；桌面操作覆盖各例的关键输入、状态变化和恢复结果。模拟错误边界会产生预期诊断，核对时须与未处理的页面异常区分。

核对环境为 React/React DOM 19.2.8、React Router 8.3.1、Vue 3.5.39、Pinia 2.3.1、TypeScript 5.7.2 与 Chrome 152。React Router 放在临时示例环境中，不改变本站 Vue 应用的依赖。其官方在线文档随版本更新，维护时仍要核对当前模式与 API。

浏览器内的模拟数据只解释路由读取、提交、校验和重验证，刷新后恢复初始数据；不作为真实后端、授权、并发写入或 SSR 的验证。正文示例的直达、刷新和历史由页面入口配合核对，也不替代生产服务器的深链配置检查。Pinia 持久化只读写本例的显示设置键，旧版先迁移到内存，损坏和未知版本须保留原记录直到明确保存。

本批更新 22 个重点术语发音，并接入桌面阅读路线、章节目录、相关引用与查阅状态。维护投入集中于内容、必要构建和关键阅读路径，不扩展移动端或完整测试套件。

## B08 的可靠导航、性能与服务端交接

B08 的五篇共 59 个概念入口、20 条目录关联，从 Vue 导航进入性能和产物，再连接 React 服务端边界与 Nuxt。主讲义保留直接前置，跨框架引用帮助对照，避免把关联关系变成额外的学习门槛。

| 知识点 | 讲解入口 | 特别核对的边界 |
| --- | --- | --- |
| VUE-07 | 四文件阅读台、查询参数、参数切换与笔记确认 | 复用组件需要观察参数；离开与更新守卫分别覆盖；导航失败、数据失败与授权不同 |
| REACT-07 | 缓存工作台、错误比较器、窗口列表、延后结果 | Profiler 时间点不等于耗时；函数闭包不能漏比；窗口示例为固定高度只读列表；不宣称预设性能收益 |
| VUE-10 | 可控保存结果、两个行为用例、配置解析 | 等待 Vue 与交付异步结果不同；替身不证明后端保存；产物、base、配置和浏览器成本各有证据 |
| REACT-09 | Compiler 源码变换、开关对照、Activity、服务端规则 | 指令须配合真实编译器；隐藏会清理 Effect；可信主体不能来自客户端参数；安全版本追踪后续公告 |
| VUE-11 | 六文件 Nuxt 阅读台、HTML/payload 与客户端导航 | 首屏交接避免重复读取；key 不负责授权；API 503 与页面状态不同；私有字段不进入客户端 |

正文有 5 段通用输出例子和 21 个项目代码块，组成 9 组完整场景：6 个 React 页面、2 组 Vue 页面和 1 个 Nuxt 工程。其中 Compiler 对同一源码另跑编译后的浏览器变体，Vue 表单配两个正文行为测试。例子直接从讲义提取，运行结果不依赖另一份手工修过的源码。

React 页面使用 React/React DOM 19.2.8、TypeScript 5.7.2，并完成严格类型核对；Vue 页面使用 Vue 3.5.39、Vue Router 4.6.4，完成 SFC 编译与严格模板检查。Compiler 使用 Babel 7 与 babel-plugin-react-compiler 1.0.0，实际执行正文检查脚本；组件用例使用 Vitest 2.1.8、Vue Test Utils 2.4.11 与隔离的 JSDOM 26 环境。浏览器操作使用 Chrome 152，只检查本批关键行为，没有增加完整回归套件。

Nuxt 4.5.2 示例单独安装并构建为 Node 产物，核对首屏已有正文和 payload、接管时没有重复 API、参数导航与历史、503 局部反馈、404 响应及笔记保留。演示私有标记与服务端备注没有进入检查过的 HTML 和客户端产物。该公开工程没有私人账号，不能据此宣称完成双身份授权、生产 CDN 或实验 streaming 验证。

安全内容在 2026-09-09 核对 React 官方 4 月、5 月和 7 月后续公告，避免继续沿用 1 月修补数字作为当前结论；Nuxt 版本、支持期限与安全说明也保留官方入口。文档日期和版本表只表示审校依据，不替代实施时的依赖与制品检查。

本批更新 31 个重点术语发音。沿用桌面五篇路线、概念目录、相关引用和查阅状态，不新增移动端适配；检查投入集中于正文可靠性、必要构建与实际阅读路径。


## B09 的 Git 现场与调试证据

B09 的四篇共 44 个概念入口、16 条目录关联。Git 三篇按对象与暂存、分支整合、协作推进；DEBUG-01 无硬前置，按问题需要连接 Git、React 请求身份和性能章节。相关概念可双向查阅，不形成额外的学习门槛。

| 知识点 | 讲解入口 | 特别核对的边界 |
| --- | --- | --- |
| GIT-01 | 同一文件三个版本、三种 diff、对象读取、恢复与救援 | add 捕获当时内容；restore 的默认来源因落点不同；reflog 是有期限的本地记录 |
| GIT-02 | 同一分叉上的 merge、rebase、快进和 cherry-pick | 文件结果相同不等于历史相同；ours/theirs 随操作角色变化；冲突解决需要同时理解需求 |
| GIT-03 | 本地裸远端、双副本、lease 拒绝和独立 worktree | fetch 不自动整合；明确预期 ID 不被后台 fetch 替换；工作文件独立但对象和多数引用共享 |
| DEBUG-01 | 零值、受控响应、完整 HTML 页面、语言缓存与版本二分 | 只改变一个变量；HEAD 不保证停在首个异常提交；工具现场与根因判断分开 |

23 个 PowerShell 代码块按正文顺序组成 4 组连续仓库练习，另有 4 个通用 JavaScript 输出例子和 1 个完整 HTML 页面。终端片段声明 powershell example=... runtime=project，只登记来源与边界，不作为通用脚本自动执行；代码栏提供 PowerShell 高亮及已有复制能力。审核时从实际 Markdown 提取代码，在独立临时目录运行，远端也是本地目录，未改真实项目引用或执行外部推送。

仓库例子使用 PowerShell 7、Git 2.43.0.windows.1 与 Node.js 22.23.0。核对三层状态、父提交关系、恢复 ID、同步前后内容、显式 lease 拒绝、worktree 文件与对象的独立/共享关系，以及首个异常引用和 bisect reset。合并、变基、cherry-pick 各一次预期冲突，以及普通 push、lease 各一次预期拒绝均被核对；审核环境为提交编辑步骤设置一次性非交互编辑器，正文说明正常编辑器的保存与关闭流程。

HTML 页面使用 Chrome 152 核对逆序响应的错误/修复对照、顺序完成、重新发起与一次性交付。它直接调用交付回调，不模拟真实 HTTP；Promise 顺序另由通用例子演示。Source Map、生产缓存、跨仓库交付和低频统计以解释、示例和官方入口给出，未把这些边界宣称为已验证的生产环境。

参考来源按技术归属检查：Git 对应 git-scm.com，调试对应 Chrome DevTools，并补充 MDN 运算符说明。审校日期为 2026-09-09，本批更新 35 个重点术语发音。继续使用桌面阅读路线、概念索引、关联跳转和查阅状态；检查投入集中于正文例子、内容完整性、必要构建及本批阅读路径。

本批桌面核对还发现并修正代码注释被当成章节标题的问题：服务端提取正文时跳过完整代码围栏，前端目录使用 Markdown 实际标题节点，避免 PowerShell 注释截断讲义或使后续锚点错位。为此补充两处针对围栏和真实讲义的回归断言。资料目录改为进入阅读路由时再加载，避免后续批次持续扩大首屏；首屏资源预算继续执行原有阈值。

最终桌面核对覆盖四篇完整正文、44 个概念锚点、四列路线、九批切换、跨篇与跨批返回、旧小节地址归一化、PowerShell 高亮、复制后的代码一致性及发音文件。内容检查和前后端构建通过；首屏 JS 为 278.5 KiB，仍低于既有 350 KiB 预算。


## B10 的工程观察与失败对照

B10 包含 ENG-01、ENG-02、ENG-03、ENG-05 四篇，44 个概念入口、16 条目录关联、24 处正文内部引用。前两篇沿同一资料应用推进，后两篇使用独立 workspace 和检查项目，避免读者需要猜测未给出的仓库配置。只保留原有直接前置：ENG-01 → JS-06、ENG-02 → ENG-01、ENG-05 → ENG-03；ENG-03 无硬前置。

| 知识点 | 讲解入口 | 实际核对的观察 |
| --- | --- | --- |
| ENG-01 | 完整浏览器应用、manifest、静态与动态导入、错误映射 | 点击前后 JS/CSS 请求、必要副作用、未用标记移除、生成堆栈回到 report.js 的 throw 行 |
| ENG-02 | HMR、两组环境值、子路径、本地 HTTP 服务 | dispose 与缺失清理对照，production/staging 替换，子路径资源、文档回退、资源/API 真 404、ETag/304 与缓存头 |
| ENG-03 | 两个本地包、冻结安装、pack 与独立消费者 | workspace 缺包拒绝、清单变更的冻结拒绝、归档文件、exports 拒绝深层路径、workspace 范围转换 |
| ENG-05 | 小统计包、五层检查、六种故障对照 | 格式、规则、类型、行为和构建入口分别失败；转译保留运行时字符串；子检查失败使统一入口非零并停止；产物可被消费 |

正文登记 35 个项目片段和 2 个通用输出示例。项目登记补充 json、css、yaml，要求显式 project；不带示例标识的说明性配置不纳入执行。与此前规则一致，配置只用于项目材料提取，不进入通用 JavaScript worker。文件名仍检查相对路径边界；环境文件使用正文中明确命名的文本块，抽取核对时从这些块取得真实内容。

本批使用 Node.js 22.23.0、Vite 6.4.3、pnpm 11.19.0、TypeScript 5.7.2、ESLint 9.39.5、Prettier 3.6.2 与 Chrome 152.0.7977.83。构建和浏览器案例复用已安装的同版本 Vite；质量练习在独立临时目录安装其固定工具。安装缓存放在该临时范围中，不修改业务依赖清单。所有验证材料来自实际 Markdown，故障变体按正文明确的单处修改构造，结束后恢复基线。

可复现性按条件表述：冻结锁文件不等于包内容已经离线可用，语义化版本不等于消费者验证，构建成功不等于生产交付。HTTP 服务只观察路径与简单 ETag 精确匹配，不实现完整条件请求、TLS 或访问控制；HMR 对照与浏览器资源请求在本机核对，不宣称覆盖正式 CDN、Service Worker 升级、灰度平台或远端 CI。CI 以执行关系和平台官方入口解释，不提供未核实的账号配置。

参考按 Vite、pnpm、ESLint、TypeScript、Node、MDN 和 GitHub Actions 的技术归属核对；旧项目版本与线上文档当前主版本分开说明。审校日期为 2026-09-10，发音目录同步 29 个重点术语。继续使用现有桌面阅读路线、概念索引、查阅状态、返回和代码复制；检查围绕正文例子、内容引用、必要构建和本批桌面阅读，不扩展移动端或完整测试套件。


## B11 的测试证据与项目表达

B11 包含 TEST-01、TEST-02、TEST-03、CAREER-01 四篇，47 个概念入口、16 条目录关联与 28 处正文内部引用。前三篇用“修改资料标题”推进，从独立规则到组件行为，再接上真实 HTTP 与刷新读取；第四篇用明确标为虚构的资料后台案例，演示证据整理与不同长度的表达。直接前置保持 TEST-02 → TEST-01、REACT-02，TEST-03 → TEST-01、WEB-01；其他两篇无硬前置。TEST-03 指明复用组件文件的位置，不把整篇组件测试方法新增为硬性前置。

| 知识点 | 讲解入口 | 实际核对与边界 |
| --- | --- | --- |
| TEST-01 | 标题码点长度、独立样例、属性、受控时间与异步 | 9 个规则用例与 100 组属性输入通过；四种手工变异被独立样例发现；“始终拒绝”仍通过两条弱属性，明确性质不等于完整需求 |
| TEST-02 | 完整 React 表单、键盘校验、失败恢复和只读 | 3 条组件用例通过；清空草稿、取消忙碌按钮禁用分别导致预期失败；jsdom 不代表布局、真实 HTTP 或服务授权 |
| TEST-03 | 本地服务、保存后刷新、一次 500 后真实重试、视觉比较 | 3 条桌面浏览器路径通过；漏写入在刷新后失败，Trace 显示 PUT 200 后 GET 仍读旧值；按钮平移产生已查看的视觉差异，恢复后通过 |
| CAREER-01 | 模拟台账、时间线、贡献、指标口径与简历口述 | 两段比例计算与正文输出一致；区分文件字节与 LCP、次数与比率、同期变化与因果；所有案例事件与数字均为教学模拟 |

正文登记 18 个项目片段与 6 个通用输出例子。提取器补充 JSX 项目登记；组件测试分三段给出，后两段明确追加到同一测试文件，查询说明片段不当作独立文件运行。审校从实际 Markdown 提取源码和配置，不维护另一份手工修订的案例。项目分别放入独立临时目录，依赖安装与缓存也在该范围内；不修改业务依赖。

实际环境为 Node.js 22.23.0、React 19.2.8、Vitest 2.1.8、React Testing Library 16.3.0、user-event 14.6.1、jest-dom 6.6.3、jsdom 26.1.0、fast-check 4.3.0、Vite 6.4.3、Playwright 1.61.1 与桌面 Chrome。浏览器示例使用独立 UUID 数据空间和仅监听回环地址的服务；内存记录只跨页面刷新，不跨服务重启。本批不宣称核对生产数据库、身份授权、CDN、跨浏览器、真实输入法或完整可访问性。

视觉基线生成后先查看图片，再禁止更新进行比较；故障后的 actual、expected、diff 与 Trace 保留在本批临时审校目录中。两种浏览器故障修改均已恢复。沙箱内首次运行不能正常结束测试服务，确认进程归属后关闭，后续允许工具正常清理其本地服务；该环境问题不作为产品错误或依靠重试掩盖的 flaky 结果。

参考依据按 Testing Library、React、Playwright、fast-check、Node、Stryker、MDN 与职业表达相关官方资料分别核对，链接放在对应讲义末尾并说明用途。审校日期为 2026-09-10，本批同步 28 个重点术语发音。正文将 contract 按语境写为约定，少见术语同时保留英文，避免机械直译。

内容结构、直接前置图、跨篇锚点与通用输出检查通过，前端类型检查、构建与既有资源预算通过。检查集中在正文例子与本批桌面阅读路径，不新增移动端工作，也不扩展完整业务测试套件。

桌面阅读核对覆盖四篇完整正文、47 个小节锚点、16 条目录关联、四列路线与 11 批切换，检查发音文件、JSX/JSON 高亮、复制文本一致性、跨篇与跨批查阅后返回、旧概念地址归一化。1600 × 1000 视口无横向溢出或页面异常，首屏 JS 278.5 KiB，全部 JS 7.85 MiB，均通过既有预算。


## B12 的工程沟通与桌面 CSS

B12 包含 CAREER-02、CAREER-04、CAREER-05、WEB-02、WEB-03 五篇，61 个概念入口、20 条目录关联与 27 处正文内部引用。三篇职业表达材料围绕独立的教学设定组织事实、判断和沟通；两篇 CSS 用完整桌面 HTML 呈现尺寸与来源机制。保留直接前置 CAREER-04 → DEBUG-01、CAREER-05 → GIT-03、WEB-02 → WEB-01、WEB-03 → WEB-02，CAREER-02 无硬前置。关联图不额外阻挡学习。

| 知识点 | 本次重构的主要材料 | 核对依据与边界 |
| --- | --- | --- |
| CAREER-02 | 统一事实清单、上下文图、容器图、保存时序、完整 ADR 与部署表 | 三张 Mermaid 在实际阅读器生成并查看；图中文字与成功、拒绝、版本冲突路径相互对应。全部为教学设定，不是当前系统的架构审计 |
| CAREER-04 | 历史资源缺失的模拟事件卡、角色、缓解比较、时间线、状态更新、行动项 | 时间算术的四项输出一致；区分回查时间、当时认知、进入观察与确认恢复。未向真实系统注入事故或发送状态消息 |
| CAREER-05 | 读取逆序完成的错误与修复、评审评论、分级、证据和批准范围 | 从正文直接运行两个例子，分别输出 B 后 A 与仅 B；明确读取提交资格不等于取消或写入撤销 |
| WEB-02 | 独立布局页、宽度滑块、溢出对照、方向切换、局部表格与 dialog | 680 px 区域基线与故障对照、1024 px 窗口单列、RTL 边框、键盘打开/Escape/焦点返回、打印与强制颜色分支 |
| WEB-03 | 280/520 px 卡片、增强开关、主题、级联实验与 Subgrid | 419/421 px 条件两侧、增强关闭后的单列与操作、普通/未分层/important 三项颜色、共享行位置、主题与打印分支 |

正文登记 3 个通用 JavaScript 输出例子与 2 个完整 HTML 项目片段。HTML 由现有提取器取得，浏览器直接运行真实正文，不增加运行依赖，也不维护手工修过的另一版。CSS 说明片段明确标明用途；scope、颜色、锚点、滚动动画与 content-visibility 以机制、回退和官方参考解释，不宣称已经逐项完成兼容验证。

本次使用 Node.js 22.23.0、Playwright 1.61.1 与 Chrome 152.0.7977.84。例子重点为桌面区域宽度、内容、键盘和样式；1024 px 窗口用于桌面变窄场景，未扩展手机、触摸或移动浏览器测试。禁用增强是当前浏览器的基线分支检查，不能作为真实旧引擎支持的证据；打印为浏览器样式分支观察，不代表真实打印机分页；字体与页面放大机制已讲解，但未宣称完成全部系统字体与真实缩放组合。

参考按 C4、ADR 原始文章、Google 工程实践与 SRE、MDN 的具体技术页面核对，中文叙述与示例自行组织。正文的虚构事件、人员、版本和数字均有明确标注；没有把材料重写成考题解答。审校日期为 2026-09-12，发音目录同步 23 个重点术语。

资料目录、正文锚点、直接前置图、通用输出与发音检查通过，前端类型检查、构建和既有体积预算通过。桌面阅读核对覆盖五篇完整正文、61 个小节、五列路线、12 批切换、四项跨篇与跨批查阅及返回、旧概念地址归一化、HTML 高亮与复制、发音文件和三张图形。1600 × 1000 页面没有横向溢出或运行异常；首屏 JS 278.5 KiB，全部 JS 7.86 MiB。预览采用独立临时数据，不修改用户学习记录。


## B13 的可访问操作与浏览器协作

B13 包含 A11Y-01、BROWSER-01、BROWSER-02、WEB-04 四篇，52 个概念入口、16 条目录关联、21 处正文内部引用。按完整用户任务、平台机制、可运行例子、边界与官方参考组织内容，保留现有直接前置：A11Y-01 → WEB-01；BROWSER-01 → WEB-01、JS-04；BROWSER-02 → BROWSER-01；WEB-04 → BROWSER-01、A11Y-01。没有新增传递性前置或改变考核内容。

| 知识点 | 本次主要材料 | 实际核对与边界 |
| --- | --- | --- |
| A11Y-01 | WCAG 查阅表、完整标题编辑与排序页、焦点落点、错误和通知对照 | 空白提交保留原值、错误关联、有效输入恢复、Space 排序与焦点保留；DOM/键盘结果不等于真实读屏验证 |
| BROWSER-01 | 操作分支图、事件日志页、真实 IndexedDB 回滚例子 | 捕获/冒泡/委托日志符合正文；重新绑定两次后仍只处理一次，停止后不处理；请求 success 后 abort，再读仍为 step=1 |
| BROWSER-02 | 三类观察器页、可取消计算、双标签锁与广播页 | 尺寸 360/520、交叉与属性记录、disconnect；scheduler 与定时器处理 200000 项得到相同校验和 11934403207，响应和取消有效；同源双标签竞争、释放与接替通过 |
| WEB-04 | 内联基础路径、popover、dialog、过渡开关、重复名称和跳过按钮 | Escape、错误与归焦；正常/准备失败/跳过/关闭动画/减少运动均只提交一次；关闭 API 分支仍可内联修改，无脚本仅确认内容可读与真实页内链接 |

从实际 Markdown 提取并运行六份完整 HTML 与一段 IndexedDB 代码，不维护另一份修订后的示例源码。另有三段明确标为接入说明的项目代码：布局读写、性能条目采集、导航拦截；不宣称完成真实 Performance 优化或完整路由验证。原生模态键盘核对允许焦点进入浏览器自身界面，但不能落到页面背景控件，没有把浏览器 chrome 的焦点行为误判为背景可交互。

实际运行发现根元素的整页视图快照会阻止跳过按钮收到点击；正文修复为排除根快照，只让命名资料项参与过渡，并解释覆盖层与参与快照元素的区别。未使用强制点击绕过这个问题。动画被跳过与内容更新失败分别处理，三个过渡 Promise 都及时挂上结果处理器。

本次使用 Node.js 22.23.0、Playwright 1.61.1 与 Chrome 152.0.7977.84。示例服务仅监听回环地址，IndexedDB 使用随机教学库并在结束后清理；站内预览采用独立临时数据。未修改用户学习记录，不扩展移动端、完整业务测试、真实屏幕阅读器、浏览器冻结或跨设备协调检查。API 关闭仅验证当前引擎的基础分支，不能当作旧引擎兼容证据。

参考按 W3C/WAI、MDN 与 Chrome 官方资料核对，审校日期为 2026-09-14。正文明确区别 WCAG 最低要求与更好的体验、广播与持久状态、取消与续算、事务提交与单个请求成功、导航与视觉过渡。28 个重点术语的离线发音及清单已同步。

内容解析、直接前置图、目录与锚点、通用示例输出和发音检查通过，前端类型检查、构建与既有体积预算通过。B01–B13 累计 55 篇主讲义、643 个概念入口、216 条目录关联与 324 处正文内部引用。桌面阅读核对涵盖本批四篇、52 个小节、四列路线、13 批切换、跨篇/跨批查阅返回、旧概念锚点地址归一化、HTML 高亮与复制、发音资源和机制图；1600 × 1000 页面没有横向溢出或运行异常。首屏 JS 278.5 KiB，全部 JS 7.87 MiB。


## B14 的组件接口、请求与安全边界

B14 包含 WEB-05、NET-01、SEC-01、SEC-02 四篇，48 个概念入口、16 条目录关联与 18 处正文内部引用。依次解释组件公开接口、请求结果、数据与权限边界、浏览器策略如何共同工作。保留直接前置 WEB-05 → BROWSER-01、NET-01 → BROWSER-01、SEC-01 → NET-01、SEC-02 → SEC-01；跨文档关联用于补充解释，不增加硬性前置或改变考核内容。

| 知识点 | 本次主要材料 | 实际核对与边界 |
| --- | --- | --- |
| WEB-05 | 完整评分控件、注册升级、Shadow DOM、事件、原生表单与框架适配 | 提前赋值保留为 4；一次 FormData 值、键盘修改与事件重定向、重置为 2、重复连接无重复监听、禁用字段集、非法输入与无增强回退；React 适配片段用于说明边界，未宣称完成 React/Vue 集成与 SSR 验证 |
| NET-01 | 独立 HTTP 服务与请求页、同一进行中请求复用、UTF-8 分块解码 | 迟到的 A 不覆盖 B；404、解析和结构错误分别呈现；取消读取；写入超时后查询已有结果，以同一键重发仍只保存一次；演示内存去重不代表生产分布式幂等方案 |
| SEC-01 | 评论纯文本与富文本对照、信任边界图、返回地址、原型与写入门槛 | 正常粗体保留，事件属性与图片去除且不发起图片请求，净化器缺失回退文本；URL 白名单和三个通用输出示例通过；CSRF/授权代码是纯判断模型，不是跨站浏览器或真实鉴权验证 |
| SEC-02 | 本地 CSP 服务、每次响应 nonce、Trusted Types policy、报告接收 | Enforce 拒绝未获准脚本及普通字符串，Report-Only 允许执行并产生报告；净化 policy 可用；nonce 每次变化且头与正文一致；报告只保留允许字段分类，拒绝错误类型和 JSON |

正文登记 14 个示例：5 个通用输出例子与 9 个项目片段。项目片段组成四个完整 HTML 体验、四个配套脚本文件和一个 React 适配片段，其中两组体验包含本地 Node.js HTTP 服务。材料直接从实际 Markdown 提取运行；属性反射、用户事件与程序赋值、取消与结果未知、转义与净化、认证与授权、记录与阻止分别说明，没有用笼统的“成功”或“安全”替代条件。

实际环境为 Node.js 22.23.0、Playwright 1.61.1、Chrome 152.0.7977.84 与 DOMPurify 3.4.12。示例仅在回环地址和教学内存数据上运行，验证后关闭示例服务；预览使用独立临时数据，不修改用户学习记录。没有扩展移动端、完整业务测试、跨浏览器或生产安全策略迁移。报告接收器演示容量限制和字段筛选，不包含生产认证、限流与持久化；正文明确这些边界。

参考按 MDN、OWASP Cheat Sheet Series、DOMPurify、React 和 Vue 官方资料核对，审校日期为 2026-09-14。保留 Web Components、Shadow DOM、nonce、sink、Trusted Types 等有助于查阅的英文，用具体输入、输出和失败对照连接中文解释。25 个重点术语的离线发音与清单已同步。

内容解析、直接前置图、正文与目录锚点、通用示例输出和发音检查通过，前端类型检查、构建与既有资源预算通过。B01–B14 累计 59 篇主讲义、691 个概念入口、232 条目录关联与 342 处正文内部引用。桌面阅读核对覆盖四篇完整正文、48 个小节、四列路线、14 批切换、跨篇/跨批查阅与返回、旧概念地址归一化、发音、HTML 高亮和复制、信任边界图；1600 × 1000 页面无横向溢出或运行异常。首屏 JS 278.5 KiB，首屏 CSS 30.0 KiB，全部 JS 7.87 MiB。


## B15 的跨源协作、凭证、密码学与类型派生

B15 包含 SEC-04、SEC-03、SEC-05、TS-04 四篇，48 个概念入口、16 条目录关联与 20 处正文内部引用。按实际困惑、可观察例子、机制解释和适用边界组织，保留直接前置 SEC-04 → SEC-02、SEC-03 → NET-01、SEC-05 → SEC-01、TS-04 → TS-03；不改变考核内容或增加硬性前置。

| 知识点 | 本次主要材料 | 实际核对与边界 |
| --- | --- | --- |
| SEC-04 | 三个本地 origin、两张策略不同的图片、三个消息窗口 | 隔离开启与关闭、SharedArrayBuffer/ArrayBuffer 分支、CORP 图片准入、跨源 DOM 读取拒绝、origin/source/schema 与重复消息检查；camera 仅查询策略，不访问设备；多端口不代表跨站 Cookie 验证 |
| SEC-03 | 认证时序图、逐步操作的状态页、独立真实 API 接入函数 | 模拟 options/认证器/服务端阶段、拒绝和成功、取消及旧回复；没有创建真实凭证或建立会话，API 函数明确依赖已有的完整验证服务，未宣称完成认证器、签名验证或恢复集成 |
| SEC-05 | 一条内存笔记的加密、篡改、导出、复用与迁移观察页 | 使用浏览器真实 AES-GCM；修改密文、IV、上下文均拒绝，原记录可读；导出被拒绝、应用拒绝复用 IV、新写 v2、失败保留 v1、成功迁移与重复迁移；不把页面集合当多设备唯一性证明 |
| TS-04 | 12 段独立 TypeScript 示例及合法/非法写法 | 映射修饰符、浅只读、真实字段投影、局部可选、有限配置、事件参数、键过滤、字符串解析、satisfies、Date 叶子和数组元组；严格类型与实际输出一致，不把静态类型当净化或冻结 |

正文登记 18 个示例：12 个通用 TypeScript 输出例子与 6 个项目片段。项目片段包括跨源实验的服务器、父页和子页，Passkey 模拟页及单独的客户端接入函数，以及 Web Crypto 观察页。浏览器案例直接从实际 Markdown 提取，未维护一份另行修订的示例源码。使用 Node.js 22.23.0、TypeScript 5.7.2、Playwright 1.61.1 与 Chrome 152.0.7977.84。

内容审校核对 MDN、W3C WebAuthn、web.dev、OWASP 与 TypeScript 官方资料，审校日期为 2026-09-14。保留必要的英文术语并同步 47 个重点发音。说明性安全模型与实际 API 实验分别标注；WebAuthn 服务端验证、生产持久事务、密钥托管、真实恢复与跨浏览器不在本批验证结论内。

本批正文增加可直达小节的引用，发现内容解析器的链接审计仅保留 Markdown 风格的中文标点，未识别站内读取服务已支持的无标点地址。仅为 chinese-guides 的目标文档补充真实小节地址别名，其他 Markdown 仍保留原规则；内容门禁和实际正文点击均已验证，未修改业务读取逻辑或放宽路径边界。

内容解析、前置图、正文与目录锚点、通用输出、类型及发音检查通过，前端类型检查、构建和既有资源预算通过。B01–B15 累计 63 篇主讲义、739 个概念入口、248 条目录关联与 362 处正文内部引用。桌面阅读核对覆盖四篇完整正文、48 个小节、四列路线、15 批切换、发音、HTML 高亮与复制、认证时序图、跨篇/跨批查阅返回、正文小节直达及旧概念地址归一化；1600 × 1000 页面无横向溢出或运行异常。首屏 JS 278.5 KiB，首屏 CSS 30.0 KiB，全部 JS 7.88 MiB。

验证集中于正文行为和本批桌面阅读，没有扩展移动端或完整业务测试。演示服务只监听回环地址并在验证结束后关闭，密码学案例只使用固定教学文字和临时内存数据；预览复用独立临时数据，不修改用户学习记录。


## B16 的类型推导、接口契约与业务边界

B16 于 2026-09-15 接入 TS-05、TS-06、TS-07、TS-08、TS-09 五篇，新增 60 个概念入口、20 条目录关联和 29 处正文内部引用。阅读路线从逐项类型推导推进到函数调用、外部数据解析、状态授权与工具迁移。保留 TS-05/TS-06 → TS-03、TS-07 → TS-02、TS-08 → TS-07、TS-09 → TS-01 与 ENG-03 的直接前置，不调整原考核和材料标签。

| 知识点 | 主要重构 | 实际例子与边界 |
| --- | --- | --- |
| TS-05 | 逐步代入与分布对照，明确 never、unknown、any、重载和深度预算 | 11 段独立类型示例，包含预期非法写法；说明整体判断也可能推断出联合，最后签名不必是总括签名，简化递归不等同于 Awaited |
| TS-06 | 从调用者与回调关系解释接口 | 9 段独立例子，含 rest 元组、重载、this、协变逆变、方法双变的真实失败、void/async、受控来源和泛型列表 |
| TS-07 | 从字节、JSON、unknown 到内部模型和恢复路径 | 6 段独立例子，含完整订单解析、未知状态只读分支、错误映射、数值与归一化、真实 Response 流字节上限；纵向契约流程图 |
| TS-08 | 状态、身份、版本各自判断，按钮与纯转换复用动作表 | 3 段独立类型例子与 model.ts、配置、桌面观察页、启动命令；验证提交、审核、拒绝、陈旧版本、非法终态与退回原因，幂等收据和数据库事务仅解释设计边界 |
| TS-09 | 从真实产物理解模块配置，再安排逐包迁移 | ESM 小包、包外 JavaScript 与声明消费、NodeNext 缺扩展名诊断、bundler 编译通过但 Node 加载失败的对照；6.0/7.0 仅作为已核验的官方版本快照 |

正文登记 42 个示例：29 个独立通用 TypeScript 示例与 13 个项目或终端片段。通用例子从 Markdown 直接提取，严格类型检查与预期输出一致，预期错误注释有效。项目例子也从正文提取：审核观察页复用实际编译后的纯函数；迁移小包实际生成本地 tarball、安装到独立消费目录，再执行 JavaScript 和类型消费。反例使用独立输出目录，确认编译拒绝未生成新产物，以及 bundler 产物在 Node 中真实报模块未找到。

版本基线为 Node.js 22.23.0、TypeScript 5.7.2、Chrome 152.0.7977.84。资料审校核对 TypeScript Handbook 与配置参考、6.0/7.0 官方发布说明、MDN、Node.js 和 OWASP 原始资料；保留自然中文解释与必要英文，同步 39 个重点术语发音。未修改仓库 TypeScript 版本，也未宣称当前 Vue/Volar 已完成 6.0/7.0 迁移。

内容解析的 13 项检查、160 份资料和 211 条直接前置、正文与概念锚点、发音清单均通过。B01–B16 累计 68 篇主讲义、799 个概念入口、268 条目录关联和 391 处正文内部引用；208 个通用例子输出一致，其中 89 个为严格检查的 TypeScript 示例。前端类型检查、构建与既有体积预算通过，首屏 JS 278.5 KiB、首屏 CSS 30.0 KiB、全部 JS 7.89 MiB。

1600 × 1000 桌面阅读核对覆盖五篇完整正文、60 个概念小节、五列路线与 16 批切换、每篇四条关联展示、代表性跨篇和跨批跳转及返回、旧概念地址归一化、正文小节引用、发音、TypeScript 高亮和复制；契约流程图单独核对可读性，无横向溢出或页面异常。审核观察页在 1440 × 1100 下验证关键决定与显示结果。

验证集中于本批内容、可运行例子和桌面阅读，未扩展移动端或完整业务测试。实验使用临时目录、局部包缓存、本地 tarball 和回环服务器；实验服务器验证后关闭，阅读预览复用独立临时数据。未实施真实授权服务、数据库并发事务、幂等存储或生产版本升级，不修改用户正式学习记录。

## B17 的浏览器身份与数据处理边界

B17 于 2026-09-19 接入 IDENTITY-01、IDENTITY-02、PRIVACY-01、PRIVACY-02 四篇，新增 48 个概念入口、16 条目录关联和 17 处正文内部引用。阅读路线从浏览器如何携带会话，推进到第三方授权、处理活动与跨区域控制。直接前置保持 IDENTITY-01 → SEC-01、IDENTITY-02 → IDENTITY-01、PRIVACY-01 无直接前置、PRIVACY-02 → PRIVACY-01；保留原材料标签和考核边界。

| 知识点 | 主要重构 | 实际例子与边界 |
| --- | --- | --- |
| IDENTITY-01 | 区分认证、会话、授权，解释轮换、期限、CSRF、退出与浏览器能力 | 两段独立代码及真实本地 HTTP 会话实验；观察 HttpOnly、Cookie 自动携带、旧标识失效、错误 CSRF/Origin 被拒绝、第二标签页显示与服务器状态的差异 |
| IDENTITY-02 | 沿授权码去向解释 PKCE、事务与声明校验 | 六段独立例子，包括真实 S256 计算、固定授权请求、返回路径、一次领取、声明关系与发行方/主体绑定；没有模拟成真实签名验证或完整 OAuth 登录 |
| PRIVACY-01 | 从字段必要性到撤回、期限、权利与副本 | 四段独立例子及删除观察页，明确可选诊断、重新同意不恢复旧任务、本人导出、供应商失败、备份等待和删除墓碑 |
| PRIVACY-02 | 将主体、分类、区域和处理方事实落实到版本化控制 | 三段独立例子，核对出口判断、日志字段投影与队列重核；未知、高风险、策略过期及拒绝不静默放行，批准编号为虚构教学数据 |

正文登记 18 个片段：15 个通用代码示例（其中 7 个 TypeScript）与 3 个项目文件。通用代码直接从 Markdown 提取，严格类型检查与预期输出通过。两个实验也直接提取正文文件：会话实验在回环地址启动 Node HTTP 服务，浏览器核对真实请求和 Cookie；删除观察页在本地打开，仅使用合成元数据。临时实验服务器验证后关闭。

浏览器能力与规范按 2026-09-19 官方资料核验，更新 Chrome Immediate UI 的参数与能力检查，明确 Digital Credentials 的 Working Draft 状态，以及 OAuth 2.1 第 16 版仍是活跃草案。隐私资料核对个人信息保护法、跨境流动规定、网络数据安全管理条例、2026 年施行的出境认证办法及当年官方答问。正文区分法律适用、内部保守策略和实际运行证据；不以教学模型代替专业结论。

同步 30 个重点术语的英文发音。内容解析的 13 项检查、160 份资料和 211 条直接前置、概念与正文链接、发音资源均通过。B01–B17 累计 72 篇主讲义、847 个概念入口、284 条目录关联、408 处正文内部引用；223 个通用例子输出一致，其中 96 个 TypeScript 示例通过严格检查。

运行基线为 Node.js 22.23.0、TypeScript 5.7.2、Chrome 153.0.8010.52。前端类型检查、生产构建与既有体积预算通过：首屏 JS 278.5 KiB、首屏 CSS 30.0 KiB、全部 JS 7.90 MiB。1600 × 1000 桌面核对四篇完整正文、48 个小节、四列路线、17 批切换、每篇四条关联、四幅流程图、发音资源、代码高亮与复制、代表性跨篇跳转及返回和旧概念地址归一化，无页面异常或横向溢出；两个实验在 1440 × 1050 下核对显示与操作。

验证限于本批内容、例子和桌面阅读。预览使用独立临时数据，没有改动正式学习记录。会话实验不包含真实认证、TLS、第三方 Cookie 或跨站能力验证；删除页不执行真实数据删除；OAuth、跨区域策略和法律适用均未被宣称为已落地的生产服务。没有扩展移动端或完整业务测试。

## B18 的 Node 执行、文件管线与协作交付

B18 于 2026-09-19 接入 NODE-01、NODE-02、NODE-04、AIDEV-01 四篇，新增 48 个概念入口、16 条目录关联和 19 处正文内部引用。保持原直接前置：NODE-01 → JS-04；NODE-02 → NODE-01、JS-05；NODE-04 → NODE-02、NET-01、TS-07；AIDEV-01 → ENG-05。原材料标签与考核文本不变，知识库的四段覆盖说明同步更新。

| 知识点 | 主要重构 | 例子与观察边界 |
| --- | --- | --- |
| NODE-01 | 从工作位置与调用上下文解释异步、公平性和资源退出 | 同步解析、CJS/ESM 顶层与 I/O 回调顺序、微任务与循环让出、worker 成功/失败/取消、延迟窗口、AsyncLocalStorage；不以偶然 timer 顺序或单次性能值作通用结论 |
| NODE-02 | 把字节、字符、记录、管线与文件提交分开 | 跨块 UTF-8、共享与复制、write false 的真实消费、有限 NDJSON 解析、临时文件 flush/rename、坏输入与运行中取消保留旧结果；未宣称完成断电、全平台文件系统或生产大文件压测 |
| NODE-04 | 从一次聚合请求解释总预算、身份边界与故障结果 | 本地 HTTP 服务与桌面观察页，覆盖正常、可选降级、必需失败、截止时间、客户端断开、容量拒绝和恢复、控制台关闭；正确区分 IncomingMessage 完成与响应提前关闭 |
| AIDEV-01 | 用资料筛选任务解释规格、执行范围和当前证据 | 任务包、筛选函数、允许列表与证据关系、桌面状态观察页；版本或内容改变让旧绿灯失效，越界被拒绝、预算停止，模拟判断不冒充实际沙箱或真实代理运行 |

正文登记 24 个片段：8 个通用代码示例（3 个 TypeScript）和 16 个项目文件/模板。代码和项目文件直接从 Markdown 提取，未维护另一份实现。Node 项目使用当前本机 Node.js 22.23.0、libuv 1.51.0，浏览器使用 Chrome 153.0.8010.52。

调度、Buffer、背压、worker 与上下文例子已执行并核对预期输出。文件实验执行成功替换、跨块中文、超长行、非法 UTF-8、末行截断和运行中取消，确认取消与解析失败后的旧目标未变，任务自己的暂存目录已清理。worker 另核对运行中取消。BFF 实验使用真实本地 HTTP 与可取消的模拟下游；核对取消原因及在途数量回落、过载后的正常请求恢复、重复参数拒绝和本地控制台关闭。AIDEV 页面只使用合成事件，核对当前证据、失效、拒绝、预算耗尽，以及最后一次预算内验证通过仍可结束的边界。

事件循环测量保留前后等待作为观察窗口，修正过早重置短窗口可能漏掉阻塞样本的写法。一次本机记录中，异步等待的最大延迟为 22 ms、利用率 0.002；受控同步占用为 136 ms、利用率 0.604。正文不固定这些数值，不把此实验当生产容量证明。

官方参考核对 Node 的事件循环、工作池、process、worker_threads、perf_hooks、AsyncLocalStorage、Stream、fs、Buffer、StringDecoder 与 HTTP 相关文档；AIDEV 参考 GitHub 的代理限制/风险说明及 Anthropic 的工作流与评估文章。动态 API 根页会随版本变化，正文标注实际运行基线，不据此升级仓库运行时。

同步 18 个重点术语发音。内容解析的 13 项检查、160 份资料及 211 条直接前置、概念归属与内部锚点、发音清单均通过。B01–B18 累计 76 篇主讲义、895 个概念入口、300 条目录关联和 427 处正文内部引用；231 个通用例子输出一致，99 个 TypeScript 示例通过严格检查。

前端类型检查、构建和体积预算通过：首屏 JS 278.5 KiB、首屏 CSS 30.0 KiB、全部 JS 7.91 MiB。1600 × 1000 桌面核对四篇正文、48 个小节、四列路线、18 批切换、每篇四条关联、四幅流程图、发音、代码高亮与复制、跨篇/跨批查阅和返回、旧概念地址归一化，无页面异常和横向溢出。两个观察页在 1440 × 1050 下核对操作和显示。

验证集中于本批内容与示例，没有扩展移动端或完整业务测试；预览继续使用独立临时数据，未修改正式学习记录。没有实施真实身份服务、外部下游、生产幂等存储、强制停机故障注入、文件断电恢复或实际 AI 工具权限系统。

## B19 的上下文证据与业务模型

B19 于 2026-09-19 接入 AIDEV-02、AIDEV-03、BIZ-01、BIZ-02 四篇，新增 48 个概念入口、16 条目录关联与 26 处正文内部引用。直接前置保持为 AIDEV-02 → AIDEV-01，AIDEV-03 → AIDEV-01、TEST-03，BIZ-01 无直接前置，BIZ-02 → BIZ-01、TS-02。知识库仅同步四段资料覆盖说明，保留资料标签、外部资料适用声明及原考核文本。

| 知识点 | 主要重构 | 例子与边界 |
| --- | --- | --- |
| AIDEV-02 | 由名单导出串起目标、现状、未知项、地图、指令、冲突、来源和恢复 | 可运行事实记录、预算选择与缓存对照；选择器只处理合成元数据，文字约定不冒充真实权限，未进行模型对照评测 |
| AIDEV-03 | 先展示同源预期制造的错误绿灯，再建立独立 oracle 与风险分层 | 修复前后名单、蜕变关系、固定种子及数组删减、三类错误修改；微型生成器不冒充完整 fuzz 工具或全局最小反例证明 |
| BIZ-01 | 从业务决定组织实体、值、关系、聚合、语言和上下文 | 身份比较、时间范围、录取与候补、旧枚举及未知状态映射；明确内存规则与持久化并发保证、展示提示与服务端授权的不同责任 |
| BIZ-02 | 用邀请生命周期解释状态、转移、守卫、不变量和提交 | 判别状态、截止边界、完整桌面观察室；旧版本冲突、成功重放、同键异动作、终态拒绝、通知失败与恢复；无真实认证、数据库、名额扣减或通知服务 |

正文登记 15 个片段：14 个通用代码示例（5 个 TypeScript）与 1 个完整 HTML 观察页。所有示例直接从正文提取，通用示例输出及严格类型检查通过。观察页使用本地文件与合成内存状态，在 1440 × 1050 桌面核对确认、过早到期、恰好截止、过期与放弃终态、旧版本冲突、成功意图重放、同键载荷变化、通知失败和恢复。重复成功不新增通知，重复投递已完成任务不再次发送；这些结果仅属于本地模型，不证明真实系统的事务隔离或外部 exactly-once。

参考核对 GitHub 的按产品入口区分的指令支持和优先级、Anthropic 的上下文管理思路、fast-check 的生成/属性/运行配置、Microsoft 的领域与战术建模，以及 Stately 的 guard 和 parallel states。审校时 Stately 文档显示 v6 alpha 入口，正文不依赖框架 API。内部实例、场景、选择器与状态观察页均为自编，不逐句翻译官方文档。

同步 25 个重点术语发音。内容解析的 13 项检查、160 份资料与 211 条直接前置、目录归属、正文锚点及发音清单通过。B01–B19 累计 80 篇主讲义、943 个概念入口、316 条目录关联和 453 处正文内部引用；245 个通用示例输出一致，104 个 TypeScript 示例通过严格检查。检查中还修正两处 B18 审校记录指向开发文档的中文标点锚点，未扩大讲义重构范围。

运行基线为 Node.js 22.23.0、TypeScript 5.7.2、Chrome 153.0.8010.52。前端类型检查、生产构建与既有体积预算通过：首屏 JS 278.5 KiB，首屏 CSS 30.0 KiB，全部 JS 7.92 MiB。1600 × 1000 桌面核对四篇正文、48 个小节、四列路线、19 批切换、每篇四条关联、四幅流程图、发音资源、代码高亮与复制、跨篇和跨批查阅返回、旧小节地址归一化，无页面异常或横向溢出；已查看阅读器、流程图和观察页截图。

验证集中在本批内容与桌面使用，未增加移动端或完整业务测试。预览继续使用独立临时数据，未修改正式学习记录；没有实施真实 AI 指令加载器、模型效果基准、缓存权限系统或生产业务状态服务。

## B20 的权限合同与一致性恢复

B20 于 2026-09-20 接入 BIZ-03、BIZ-04、BIZ-05、BIZ-06、BIZ-07 五篇，新增 60 个概念入口、20 条目录关联和 27 处正文内部引用。五篇以课程资料管理为共同背景，从授权走到接口、编辑、长任务及故障恢复。直接前置保持为 BIZ-03 → BIZ-01，BIZ-04 → BIZ-01、TS-07，BIZ-05 → BIZ-02、BIZ-04，BIZ-06 → BIZ-02、JS-05，BIZ-07 → BIZ-02、BIZ-04；知识库仅同步五段资料覆盖说明，保留材料标签、MDN 外部入口与原考核文本。

| 知识点 | 主要重构 | 例子与边界 |
| --- | --- | --- |
| BIZ-03 | 从具体授权决定解释角色、属性、对象范围、字段能力、执行与缓存 | 完整策略判断、先过滤后分页、关系版本和到期失效；默认拒绝、跨机构与自审反例；可信输入来自服务端，本地函数不提供真实认证 |
| BIZ-04 | 从调用承诺连接 DTO、领域和展示模型，再到错误、版本与演进 | 缺失/null/零值、完整进度解析映射、局部修改；区分自编 PATCH 合同与 Merge Patch、缓存验证与写入前提，补充 Protobuf、Webhook 和未知枚举 |
| BIZ-05 | 区分已确认事实、比较基线、当前草稿与发送快照 | 输入规范化、字段修订、三方比较及完整编辑观察页；补充列表投影、乐观覆盖层、自动保存和导航恢复；自动合并后仍要验证跨字段约束 |
| BIZ-06 | 将业务任务、连接观察、执行尝试与结果留存分别建模 | 有限退避轮询、阶段进度、取消检查点；快照与增量推送、旧 worker 隔离、导入报告、领取凭据和文件期限；客户端 Abort 不证明后台停止或回滚 |
| BIZ-07 | 从结果未知连接幂等、并发版本、原子效果与消息恢复 | 同意图重放、同键异载荷、版本冲突、重复投递、增量缺口；继续解释 Outbox、补偿的新意图、重试预算、对账与故障位置；内存模型不证明分布式 exactly-once |

正文登记 17 个片段：16 个通用代码示例（6 个 TypeScript）和 1 个完整 HTML 编辑观察页。通用示例直接从正文提取，输出与严格类型检查通过。观察页在 1440 × 1050 桌面核对保存后继续输入、未改字段的规范化、旧版本冲突、保留草稿后显式再保存、采用远端、拒绝保留输入、切换资料后的迟到响应、发送后恢复基线、空输入拒绝和零值保存。服务端事实、基线、草稿与发送快照可同时观察，页面无异常或横向溢出；这只验证合成本地状态，不覆盖真实数据库和会话服务。

参考核对 OWASP 授权建议、NIST RBAC、RFC 9110/7396/9457、MDN 条件请求与 AbortController、Protobuf 字段存在性、gRPC-Web、Microsoft 异步请求响应模式、AWS 幂等重试及 Transactional Outbox 原始模式说明。正文用自编场景讲解，不逐句翻译资料；协议语义、业务合同和实现假设分别说明。

同步 16 个重点术语发音。13 项内容解析检查、160 份资料与 211 条直接前置、唯一概念归属、正文锚点和发音清单通过。B01–B20 累计 85 篇主讲义、1003 个概念入口、336 条目录关联、480 处正文内部引用；261 个通用例子输出一致，110 个 TypeScript 示例严格检查通过。

运行基线为 Node.js 22.23.0、TypeScript 5.7.2、Chrome 153.0.8010.52。前端类型检查、生产构建与既有体积预算通过：首屏 JS 278.5 KiB、首屏 CSS 30.0 KiB、全部 JS 7.93 MiB。1600 × 1000 桌面核对五篇、60 个小节、五列路线、20 批切换、每篇四条关联、五幅流程图、发音资源、代码高亮和复制、跨篇与跨批查阅返回、旧概念地址归一化；阅读器与观察页截图已查看。验证投入集中于内容和桌面阅读，未增加移动端或完整业务测试；预览使用独立临时数据，不修改正式学习记录。

## B21 的交付追踪与渲染边界

B21 于 2026-09-20 接入 BIZ-08、TEST-04、RENDER-01、RENDER-02 四篇，新增 48 个概念入口、16 条目录关联与 25 处正文内部引用。通过课程资料的复核、接口消费和页面呈现，连接业务承诺、当前证据及用户实际看到的结果。直接前置保持为 BIZ-08 → BIZ-01、ENG-05，TEST-04 → BIZ-04、TEST-01，RENDER-01 → NET-01，RENDER-02 → RENDER-01；知识库仅同步四段覆盖说明，保留原材料标签和考核文本。

| 知识点 | 主要重构 | 例子与边界 |
| --- | --- | --- |
| BIZ-08 | 从代班复核到期的执行缺口，串起规则身份、验收、双向追踪、矩阵和变化 | 到期边界、候选影响图、证据基线；区分链接存在、局部规则返回与实际接口拒绝，不把图遍历当完整影响证明 |
| TEST-04 | 从旧阅读页被字段改名破坏，解释真实消费依赖、提供者状态、制品与版本矩阵 | 消费函数、必要字段匹配、通过/失败/未知组合；覆盖 HTTP、Protobuf、消息及兼容窗口；自编模型未实现 Pact、Broker 或真实代理验证 |
| RENDER-01 | 将导航方式、HTML 生成、数据更新、共享范围与交互成本分别决定 | 简化时间表、陈旧预算、共享资格与键；明确再生间隔不等于严格陈旧上限，私有内容不能靠静态化或加键自动获得隔离 |
| RENDER-02 | 从等待屏障解释流式、状态码窗口、首次快照、序列化和 Islands | 确定性输出、安全 JSON、导航归属及完整 Node 分段观察页；原生增强不是框架 Hydration，本机分段也不证明生产代理不缓冲 |

正文登记 12 个通用示例（4 个 TypeScript）和 1 个完整 Node 项目文件。全部从实际正文提取，通用输出与严格类型检查通过。Node 观察页在独立随机端口启动，核对原始响应首段含正文而无延伸阅读、尾段完成、完整模式文档、脚本先于次要内容就绪且按钮可用、次要区域失败仍保留正文、关闭 JavaScript 后原生表单提交 42 分钟有效，以及断开连接后服务端停止等待。1440 × 1050 桌面无页面异常或横向溢出，正常、部分失败和无脚本截图已生成；已查看代表版面。项目使用合成资料和 GET 展示参数，不写数据库或真实学习进度，也不提供业务写入回滚保证。

参考核对 NASA 需求管理、Pact 的工作流程/验证范围/提供者状态/版本矩阵/消息验证、Protobuf 演进、web.dev 渲染概念、RFC 9111 缓存语义、Next.js ISR、React 流式和 Hydration API，以及 Astro Islands。官方入口承担语义核验，自编模型与特定工具 API 分开说明，不逐句翻译文档，也没有安装契约服务或迁移应用框架。

同步 16 个重点术语发音。13 项内容解析检查、160 份资料与 211 条直接前置、唯一概念归属、内部锚点及发音清单通过。B01–B21 累计 89 篇主讲义、1051 个概念入口、352 条目录关联、505 处正文内部引用；273 个通用示例输出一致，114 个 TypeScript 示例严格检查通过。

运行基线为 Node.js 22.23.0、TypeScript 5.7.2、Chrome 153.0.8010.52。前端类型检查、生产构建与既有预算通过：首屏 JS 278.5 KiB、首屏 CSS 30.0 KiB、全部 JS 7.94 MiB。1600 × 1000 桌面核对四篇、48 个小节、四列路线、21 批切换、每篇四条关联、四幅流程图、发音、代码高亮和复制、跨篇与跨批查阅返回、旧概念地址归一化及正文定向链接；截图已查看，无页面异常。验证集中在内容与桌面，未新增移动端或完整业务测试；站点预览继续使用独立临时数据。

## B22 的查询写入与实时组件

B22 于 2026-09-21 接入 DATA-01、DATA-02、REALTIME-01、COMP-01 四篇，新增 48 个概念入口、16 条目录关联与 28 处正文内部引用。以资料列表、标题编辑、实时副本与搜索面板串起同一组用户任务，先解释可观察的例子，再区分通用规则、库的约定与简化模型。直接前置保持为 DATA-01 → NET-01，DATA-02 → DATA-01、BIZ-07，REALTIME-01 → NET-01、BIZ-07，COMP-01 → A11Y-01；知识库仅更新四段覆盖说明，原材料标签、链接、考核、通过标准和耗时保持。

| 知识点 | 主要重构 | 例子与明确边界 |
| --- | --- | --- |
| DATA-01 | 从目录与详情不一致建立远端副本、查询身份、新鲜度、去重、失效、分页和会话责任 | 查询视图、规范化键、在途读取与会话代次；区分失效与重取、普通分页与无限查询、缓存恢复与 DOM Hydration；缓存键不执行授权 |
| DATA-02 | 从旧失败抹掉新输入解释确认基线、覆盖层、意图身份、未知结果、冲突与离线 outbox | 覆盖层归属、四种结果恢复、离线记录信封；同一对象示例只覆盖明确顺序，不冒充任意并发合并；恢复模型不实现 IndexedDB 事务和服务端幂等 |
| REALTIME-01 | 从连接恢复但资料缺失解释消息确认、SSE 帧、传输保证、连续游标、快照、背压和授权变化 | 确认阶段、连续游标、有界光标合并与完整 Node SSE 页；应用游标和 EventSource 接收 ID 分开，固定内存日志不证明生产重放、持续快照竞态或代理可达 |
| COMP-01 | 从同一个面板的不同用法解释职责、公共 API、状态、事件、组合、语义、焦点、适配、样式与迁移 | 结果判别联合、不可变查询意图、完整双实例 HTML 页；只读可选择且不提交，重试旧条件保留新输入；组件不能执行服务端授权，状态所有权仍归 COMP-02 |

正文登记 12 个通用示例（4 个 TypeScript）、1 个完整 Node 文件和 1 个独立 HTML 页面；示例从实际正文提取，输出和严格类型检查通过。SSE 页在独立随机端口验证响应帧、Last-Event-ID 从 102 续接、重复事件只应用一次、102 后断线自动补齐到 105、缺少 103 时停在 102、历史过期转向快照、快照后续接以及手动停止保留副本。组件页验证 Enter 单次意图、规范化条件与原输入分离、只读聚焦选择且不提交、组合动作不触发查询、失败后的新输入保留、重试使用旧条件、按钮消失后焦点返回、空结果与唯一标签关联。两页在 1440 × 1050 下无页面异常或横向溢出，已查看正常与失败的代表版面。这些观察只针对演示合同，不代表真实数据库、网络部署或所有辅助技术均已验证。

参考核对 TanStack Query v5 的查询键、默认行为、取消与乐观更新，WHATWG EventSource 解析与重连，MDN SSE、WebSocket、WebTransport 与 IndexedDB，React 的组件拆分与 props、Vue slots、WAI-ARIA Button Pattern 和 MDN readonly。以自编中文场景解释规则，保留必要英文；没有安装查询库或迁移项目的数据层。

同步 17 个重点术语发音。13 项内容解析检查、160 份资料与 211 条直接前置、唯一概念归属、内部锚点及发音清单通过。B01–B22 累计 93 篇主讲义、1099 个概念入口、368 条目录关联、533 处正文内部引用；285 个通用示例输出一致，118 个 TypeScript 示例严格检查通过。

运行基线为 Node.js 22.23.0、TypeScript 5.7.2、Chrome 153.0.8010.52。前端类型检查、生产构建与既有预算通过：首屏 JS 278.5 KiB、首屏 CSS 30.0 KiB、全部 JS 7.94 MiB。1600 × 1000 桌面核对四篇、48 个小节、四列路线、22 批切换、每篇四条关联、四幅流程图、发音、高亮和复制、跨篇与跨批查阅返回、旧概念地址与正文定向链接；截图已查看。验证集中于内容、示例和桌面阅读，没有扩展移动端或完整业务测试，站点预览使用独立临时数据。

## B23 的交互所有权与交付证据

B23 于 2026-09-22 接入 COMP-02、UX-01、ENG-08、LINUX-01 四篇，新增 48 个概念入口、16 条目录关联与 20 处正文内部引用。用输入回退、失败恢复、发布文件和只读文件删除等具体问题引出概念，串联组件、业务状态、构建产物与文件系统。直接前置保持为 COMP-02 → COMP-01，UX-01 → A11Y-01、BIZ-02，ENG-08 → ENG-03，LINUX-01 无直接前置；知识库仅同步四段覆盖说明，原材料入口、考核、通过标准和耗时保持。

| 知识点 | 主要重构 | 例子与明确边界 |
| --- | --- | --- |
| COMP-02 | 从输入跳回解释状态所有者、受控与非受控、模式、事件、草稿、重置及命令句柄 | 所有者决策、类型合同、保存快照与完整 HTML 观察页；旧保存确认不覆盖新输入，重置隔离迟到结果；原生页面演示所有权协议，不冒充 React/Vue 运行时，单字段重新初始化不等同完整实例重建 |
| UX-01 | 从失败后继续任务解释信息架构、状态、反馈、恢复、焦点、渐进披露、原型与可用性研究 | 部分失败、按结果重试、研究分母与完整 HTML 恢复页；保留成功行和未提交输入，未知结果先查证；离线开关是本地模拟，三条研究观察明确为合成数据，没有实际用户研究 |
| ENG-08 | 从扫描通过却不能发布解释制品身份、依赖、SBOM、扫描、Provenance、签名、构建与响应 | 证据对象匹配、三态检查、消费方追踪与完整 Node 签名实验；真实 SHA-256 与 Ed25519 验证制品和声明，另行检查身份和来源策略；不冒充完整 SLSA、Sigstore 或生产发布系统 |
| LINUX-01 | 从只读文件可删除解释路径、inode、目录权限、身份选择、umask、链接、参数边界、原子切换与归档 | 硬链接模型、权限类别、位掩码与完整 Bash 实验；说明 ACL、挂载、安全策略和检查使用竞争；实验仅操作自己的临时目录，不自动递归清理，不能以 Windows Bash 的检查代替 Linux 实机验证 |

正文登记 12 个通用示例（3 个 TypeScript）、2 个完整 HTML 页面、1 个 Node 文件和 1 个 Bash 文件。通用示例直接从正文提取，输出与严格类型检查通过。示例登记器补充 Bash 项目片段支持，明确只登记、不交给 JavaScript 执行；既有未登记的 Bash 说明片段保持原处理方式。

两个 HTML 页在 1440 × 1050 桌面核对：组件页的父级接受与拒绝、初值变化不覆盖草稿、显式重新初始化、成功确认旧快照但保留新输入、失败保留输入、重置后忽略迟到失败及聚焦句柄；恢复页的错误摘要焦点、键盘定位、发送前离线保留输入、恢复后仅提交待处理行，以及成功后焦点和只读状态。页面没有异常或横向溢出，已查看代表版面。Node 实验核对正常通过、制品变化、声明篡改、不受信任密钥与来源不合策略五种结果。

本机没有可用的 Linux 运行环境。Bash 实验仅使用 Git 附带的 Bash 完成语法检查，尚未执行 Linux 权限、链接、重命名及归档实验。正文说明了 Linux、Bash 和 GNU 工具的运行前提；这里不把 Windows 上的语法通过记录成 Linux 行为通过。

参考核对 React 受控输入与命令句柄、Vue 组件 v-model、W3C 表单通知、GOV.UK 可用性研究、CycloneDX/SPDX、SLSA v1.2、GitHub attestation 验证及 Linux man-pages；GNU 工具入口保留在相关正文。各篇使用自编中文场景和例子，保留必要英文，并区分工具文档、简化模型与工程保证。

同步 18 个重点术语发音。13 项内容解析检查、160 份资料与 211 条直接前置、概念归属、内部锚点及发音清单通过。B01–B23 累计 97 篇主讲义、1147 个概念入口、384 条目录关联、553 处正文内部引用；297 个通用示例输出一致，121 个 TypeScript 示例严格检查通过。

运行基线为 Node.js 22.23.0、TypeScript 5.7.2、Chrome 153.0.8010.52。前端类型检查、生产构建与既有预算通过：首屏 JS 278.5 KiB、首屏 CSS 30.0 KiB、全部 JS 7.95 MiB。1600 × 1000 桌面核对四篇、48 个小节、四列路线、23 批切换、每篇四条关联、四幅流程图、发音、高亮和复制、跨篇与跨批查阅返回、旧概念地址及正文定向链接；截图已查看。验证集中于本批内容和桌面阅读，预览使用独立临时数据，没有扩大移动端或完整业务测试范围。

## B24 的服务诊断与容器交付

B24 于 2026-09-22 接入 LINUX-02、LINUX-03、LINUX-04、DOCKER-01、DOCKER-02 五篇，新增 60 个概念入口、20 条目录关联和 29 处正文内部引用。用网页访问失败、定时任务失效、账号间接权限、镜像生成和服务重建串起基础概念，不按站内考题安排正文。直接前置保持为 LINUX-02 → NET-01、LINUX-01，LINUX-03 → LINUX-01，LINUX-04 → LINUX-01、SEC-01，DOCKER-01 → LINUX-01，DOCKER-02 → DOCKER-01、LINUX-02。知识库仅更新这五段覆盖说明，学习入口、考核、通过标准、耗时及先前批次内容保持。

| 知识点 | 重构后的解释入口 | 示例与关键条件 |
| --- | --- | --- |
| LINUX-02 | 沿浏览器、代理和上游连接解释进程、unit、socket、DNS、双栈、TLS、阶段耗时、日志、资源和信号 | 命令与示意输出、耗时差值、完整本地 HTTP 实验；500 与 502 区分产生层，curl 状态码与退出码分开，137 不独自证明 OOM |
| LINUX-03 | 从参数数组和失败状态解释引用、管道、errexit、环境、预览、锁、提交、幂等、清理与定时任务 | 三份 Bash 语义实验和完整临时目录发布脚本；dry-run 不预订未来状态，原子切换不等于持久，协作锁不提供恶意目录安全边界，Persistent 不逐条补跑 |
| LINUX-04 | 用允许矩阵连接间接能力、SSH 两种身份、有效配置、key 生命周期、sudo、网络、硬化和恢复 | SSH/sudo/systemd 片段、正反访问矩阵与第二会话流程；注释不自动使 key 到期，已有连接不证明新认证，Docker 发布路径与 UFW 分开核对 |
| DOCKER-01 | 从镜像与实例进入摘要、上下文、缓存、多阶段、权限、入口、健康、秘密和发布 | 完整无第三方依赖资料页项目；教学基础 tag 会移动，没有包安装步骤所以不宣称依赖缓存收益；unhealthy 不等于普通 restart policy 自动重启 |
| DOCKER-02 | 用网关与文件计数服务连接项目归属、服务名、变量、就绪、卷、合并、恢复与数据版本 | 完整 Node/Dockerfile/Compose 文件；单进程串行写入不提供多副本或数据库事务，POST 超时不自动重试，卷不是备份，配置解析不代替 Engine 行为 |

正文登记 1 个通用计算示例和 12 份项目文件：HTTP 诊断文件 1 份、Bash 文件 4 份、镜像页项目登记 4 份、Compose 项目登记 3 份；两个项目的 dockerignore 另在正文说明。登记器新增 Dockerfile 项目来源支持，不将其作为 JavaScript 运行。阅读器补充 Dockerfile 与 INI 高亮，代码复制仍保持原文。

从实际正文提取并执行 HTTP 实验，得到 200 正常、500 应用内部失败、502 未取得上游响应。Git 附带的 Bash 执行三份纯 Shell 语义实验，核对引用与拆词、默认管道/pipefail/PIPESTATUS、if 调用函数时的 errexit 例外。发布脚本仅通过 Bash 语法检查。Node 原生运行核对生成页面、文本转义、健康/404/405、网关转发、四次并发递增、进程重建保留本地计数、上游退出返回 502 和损坏数据拒绝覆盖。Compose YAML 使用现有解析库检查语法和关键字段，这不是 Docker Compose 引擎检查。

本机没有可用的 Linux 或 Docker Engine。未执行 Linux 发布/锁/信号实验、systemd/SSH/sudo/防火墙变更或容器构建、网络、卷、健康依赖、权限与资源限制。Windows 子进程结束仅用于清理本次原生实验，不记为 Linux 优雅退出证据。没有安装 Docker/WSL，没有修改真实服务器配置，预览使用独立临时数据。

官方资料核对 curl、Bash、Linux/systemd 手册、OpenSSH、Docker 的上下文、缓存、多阶段、secret、Compose 插值、启动、网络、卷、合并和重启规则；sudo 策略保留官方完整手册入口。采用自编中文情境、输出解释和反例，保留必要英文术语，没有逐句翻译资料。

同步 24 个重点术语发音。13 项内容解析检查、160 份资料及 211 条直接前置、概念归属、内部锚点和发音清单通过。B01–B24 累计 102 篇主讲义、1207 个概念入口、404 条目录关联、582 处正文内部引用；298 个通用示例输出一致，既有 121 个 TypeScript 示例严格检查通过。初次内容检查要求 Compose 定义更明确，已补清 service 与 container 的含义，未修改检查规则。

运行基线为 Node.js 22.23.0、TypeScript 5.7.2、Chrome 153.0.8010.52。修改的高亮模块 lint、前端类型检查、生产构建和体积预算通过：首屏 JS 278.5 KiB、首屏 CSS 30.0 KiB、全部 JS 7.97 MiB。1600 × 1000 桌面核对五篇、60 小节、五列路线、24 批切换、每篇四条关联、五幅流程图、每篇代表发音资源、JS/Dockerfile/INI 高亮、复制、跨篇跨批查阅返回、旧概念地址与正文定位；页面无异常和横向溢出，截图已查看。流程图两处断行已按词义调整。验证集中于本批内容与桌面阅读，没有扩展移动端和完整业务测试。

## B25 的版本交付与用户体验

B25 于 2026-09-23 完成 ENG-06、DEPLOY-01、OBS-01、PERF-01 四篇重构，接入 48 个概念入口、16 条目录关联和 30 处正文内部引用。以资料站的新版本、旧页面、保存进度和桌面交互串联解释，保留必要英文，不按考题展开正文。直接前置保持 ENG-06 → GIT-03、TEST-03、TEST-04，DEPLOY-01 → LINUX-02，OBS-01 → DEBUG-01、ENG-06，PERF-01 → OBS-01。对照本次修改前快照，知识库仅改变这四处学习资料覆盖说明，入口、其他参考、考核、通过标准、耗时与既有批次内容均保留。

| 知识点 | 重构后的主要解释 | 例子与关键边界 |
| --- | --- | --- |
| ENG-06 | CI/CD、提交/运行/制品、晋级与可重现、门禁、权限、并发、灰度、观察、迁移和恢复 | 摘要比较、未知门禁、过期计划、旧页面懒加载、扩展收缩；同一提交不等于同一产物，取消不撤销外部写入，回滚不自动恢复数据 |
| DEPLOY-01 | 请求分类、location/root/alias/try_files、URI 转发、超时缓冲、TLS、缓存、共享键、清理和发布顺序 | 完整隔离 Nginx 配置、真实 JSON 上游、响应矩阵；资源 404 不回退 HTML，proxy_cache off 不约束 CDN，旧目录存在不等于旧 URL 仍可访问 |
| OBS-01 | 用户任务、RUM/实验室、文档/页面/任务归属、错误、日志指标链路、采样、SLO、预算、告警、生命周期和隐私 | 本地保存观察页、采样偏差和燃烧率计算；捕获原页面上下文，撤回再开启不补记旧任务，零分母未知，sendBeacon 入队不是接收确认 |
| PERF-01 | LCP/INP/CLS、p75、实验室口径、预算、LCP 分段、关键路径、主线程与位移窗口 | 分位数不可平均、LCP 四段、CLS 窗口、完整主线程交互页；区分一次访问的 INP 与跨访问 p75，TBT 不替代 INP，演示事件不冒充标准指标 |

正文登记 8 个通用 JavaScript 输出示例和 5 份项目文件（2 个 Node 文件、2 个完整 HTML 观察页、1 个 Nginx 配置）。示例登记器增加 Nginx 配置来源，要求 project 类型，不将配置当 JavaScript 执行。阅读器新增 Nginx 语法高亮，保留原文复制。同步 34 个重点术语发音。

从实际正文提取并运行：制品副本字节一致、改变字节后摘要不同；Node 上游的健康路径、查询参数、错误路径、方法、JSON 类型与 no-store 响应正确。Chrome 中核对保存任务在切换页面后仍归原页面，撤回后重新开启不复活旧任务，未开启记录时不记录，两个观察页均无外部请求和页面异常。主线程实验一次执行约 120 ms、期间回调增加 0 次；分段执行本次约 136 ms、期间回调增加 17 次。这是本机一次教学观察，不是实际绘制帧数、标准 INP 或产品性能收益。

本机没有可用的 Nginx 或 Docker，未进行 Nginx 实机启动、TLS、CDN、真实流水线部署或真实用户性能采样。配置按官方指令规则核对，Node 上游通过不代表完整代理链路通过。没有安装服务或修改真实服务器，预览沿用隔离临时数据库；旧预览已停止，本次恢复到本地 43731 端口。

官方资料核对 GitHub Actions 部署环境、并发和制品证明，Nginx 核心/代理/响应头/HTTPS，MDN 缓存与 PerformanceObserver/sendBeacon/scheduler.yield，OpenTelemetry JavaScript 状态、Google SRE 燃烧率，以及 web.dev 的 Vitals/LCP/INP/CLS/TBT 和 Lighthouse 评分。补清并发默认待运行替换与可选排队的区别，未把平台功能名称等同于业务恢复保证。

内容检查通过：13 项解析检查、160 份资料和 211 条直接前置、概念归属、内部锚点、术语音频一致。B01–B25 累计 106 篇主讲义、1255 个唯一概念入口、420 条关联、612 个正文引用、306 个通用示例，其中 121 个 TypeScript 示例严格检查通过。高亮模块 lint、前端类型检查、生产构建与体积预算通过：首屏 JS 278.5 KiB、首屏 CSS 30.0 KiB、全部 JS 7.98 MiB；构建仍有大分块提示。

运行基线 Node.js 22.23.0、TypeScript 5.7.2、Chrome 153.0.8010.53。1600 × 1000 桌面核对四篇、48 小节、四列路线、25 批切换、每篇四条关联、四幅图示、每篇代表发音资源、JS/Nginx 高亮、复制、跨篇跨批查阅与返回、旧地址规范化和正文小节定位；无页面异常与横向溢出。阅读页、图示、Nginx 代码和两个实验页面截图均已查看。验证聚焦本批内容与桌面阅读，没有扩展移动端或完整业务测试。

## B26 的加载响应与资源布局

B26 于 2026-09-24 完成 PERF-02、PERF-03、PERF-04、H5-01 四篇重构，接入 48 个概念入口、16 条目录关联和 28 处正文内部引用。以资料页的请求、搜索、预览与备注表单串联机制，使用自然中文、具体输入输出和反例，保留必要英文术语。直接前置保持 PERF-02 → NET-01、PERF-01，PERF-03 → PERF-01、BROWSER-02，PERF-04 → JS-03、BROWSER-02，H5-01 → WEB-02、A11Y-01。与修改前快照逐行比较，知识库仅四处学习资料的覆盖范围变化；入口、其他参考、考核、通过标准与耗时均保留。

| 知识点 | 重构后的主要解释 | 例子与关键边界 |
| --- | --- | --- |
| PERF-02 | 资源发现、依赖链、连接与 TTFB、传输/执行、图片字体、缓存、提示、推测导航和恢复 | 依赖时间、缓存年龄、完整条件响应实验；下载完成不等于呈现，304 复用已有正文，bfcache 与 HTTP 缓存不同，预取请求也可能产生副作用 |
| PERF-03 | 主线程、任务/微任务/帧机会、交互三段、Long Task/LoAF、削减工作、布局、分块、Worker 和采集 | 合成时间线、缓冲转移、完整可取消搜索；取消与任务身份分工，success/catch/finally 均保护新任务，Worker 消息不抢占长同步循环 |
| PERF-04 | 可达性、同周期比较、快照/保留路径、监听身份、闭包/分离节点、定时器、外部资源、缓存/弱引用与框架生命周期 | 引用图、监听移除、条目上限 LRU、完整预览组件；循环引用不必然泄漏，Object URL 单独释放，计数回零不等于堆审计通过 |
| H5-01 | CSS 像素/DPR、布局/视觉视口、内容约束、容器查询、动态单位、安全区、定位、方向与状态、文字/重排 | 像素预算、留白算术、完整可调表单；桌面窄容器不等于移动设备，演示留白不是实际安全区，文字变大不等于页面缩放 |

正文登记 9 个通用 JavaScript 示例和 4 份完整项目文件（1 个 Node 条件响应实验、3 个 HTML 页面），复用现有示例登记与高亮规则。同步 31 个重点术语发音，本批生成 20 份新增音频并清理 5 份不再引用的生成文件。

从正文提取后实际执行：本机 HTTP 依次得到 200 保存版本 1、304 正文为空并复用版本 1、200 取得版本 2；快速替换搜索后只展示 CSS 的 80,000 个匹配中的前 8 条，取消后进度停止；预览连续替换仍只有各 1 份监听、定时器、Object URL，重复关闭回零，尺寸与计时回调不再增加。桌面 1440 × 1000 中将卡片设为 320px、文字 200%、演示底部留白 40px，卡片和页面无横向溢出；恢复 900px 与 100% 后变成两列，备注保持一致。三个页面没有外部 HTTP 请求或页面异常，截图已查看。

这些结果验证教学示例的行为，不是实际产品的提速、浏览器 HTTP 缓存命中、标准 INP 或完整内存泄漏审计。未采集实际堆快照，未执行 CDN、真实手机键盘、安全区或各 WebView 的实机验证。系统面向桌面，本次重点放在容器、文字和输入连续性，没有扩展移动产品界面或完整业务测试。

官方资料核对 MDN 的 preload、Resource Timing、Speculation Rules、Worker 转移/终止、监听移除、内存管理、VisualViewport、视口单位、容器查询和 env，Chrome 的 LoAF 与堆快照，web.dev 的 bfcache，以及 W3C 的 Resize Text/Reflow。明确 LoAF blockingDuration 不等于 duration 减 50ms，视觉视口不等于布局视口，安全区实际为零时不使用 env 回退值，终结回调不能承担确定性资源释放。

内容检查通过：13 项解析检查、160 份资料和 211 条直接前置、唯一概念归属、内部锚点和术语音频一致。B01–B26 累计 110 篇主讲义、1303 个概念入口、436 条目录关联、640 处正文内部引用，315 个通用示例输出一致，其中 121 个 TypeScript 示例严格检查通过。前端类型检查、生产构建和体积预算通过：首屏 JS 278.5 KiB、首屏 CSS 30.0 KiB、全部 JS 7.99 MiB；构建保留既有大分块提示。

运行基线为 Node.js 22.23.0、TypeScript 5.7.2、Chrome 154.0.8037.57。1600 × 1000 桌面核对四篇、48 小节、四列路线、26 批切换、每篇四条知识关联、四幅流程图、每篇代表发音、JS 高亮、复制原文、跨篇跨批查阅与返回、旧地址规范化和正文指定小节定位；无页面异常或横向溢出。阅读页、图示和实验截图均已查看。预览使用既有独立临时数据库，恢复到本机 43731 端口，不修改正式学习记录。

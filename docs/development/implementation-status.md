# Career Atlas 实施与验收状态

更新时间：2026-07-29

## 当前基线

| 模块 | 状态 | 验收口径 |
| --- | --- | --- |
| 知识体系 | 通过 | 20 个领域、219 个唯一知识点 |
| 学习资料 | 通过 | 每点至少两份资料；必读和独立首考题源均为中文，英文原文仅作已标注的版本核验材料 |
| 知识关系 | 通过 | 推荐路线与硬前置分离；硬前置源节点不晚于目标节点 |
| 计划模板 | 通过 | 64 周 × 7 天，共 448 条模板事件 |
| 内容覆盖 | 通过 | 前 60 周覆盖全部 219 点，最后 4 周综合验证 |
| 学习容量 | 通过 | 每日预计投入大于 0 且不超过 390 分钟 |
| 延迟复测 | 通过 | 首考至少 7 天后排入复测，计划容量不足时直接失败 |
| 本地导入 | 通过 | Windows/macOS/Linux 路径解析与 LF/CRLF 内容解析 |
| 权威同步 | 通过 | Markdown 为唯一真源；导入时自动清理已删除或已合并的旧领域、知识点与级联关系 |
| 学习重置 | 通过 | 重建知识与 64 周计划，保留岗位、项目资产和备份 |
| 自动备份 | 通过 | 启动检查、手动创建、校验和恢复 |
| 代码题安全 | 通过 | 没有隔离执行器时拒绝在宿主机执行答案 |

## 本轮体系改革

- 从早期 16 个领域、153 点扩展为当前 20 个领域、219 点；先合并重复合同，再以多轮职责反向审计持续补齐稳定缺口。
- 新增 `NODE-04`、`EDITOR-01`、`LOCALFIRST-01`、`EMBED-01`，补齐 Node HTTP/BFF 生产服务、复杂编辑器/IME 与 AI 创作、本地优先数据库/同步引擎、嵌入式 SDK/第三方宿主/多租户隔离。
- `TEST-01`/`AIDEV-03` 补入属性、变异、fuzz、差分、蜕变与测试预言；`CAREER-04`/`OBS-01`/`ENG-06` 补入值班、严重度、事故指挥、状态沟通、恢复验证和行动项闭环。
- `MEDIA-01` 补入屏幕/区域捕获、PiP、设备热切换和权限生命周期；`BIZ-04`/`TEST-04` 补入 Protocol Buffers/gRPC-Web；`ENG-08` 补入许可证/NOTICE/AI 代码来源；`PWA-02` 补入 Share Target 安全。
- 新增 `DX-01`、`MEDIA-01`、`WASM-01`、`AIDEV-11`，补齐前端平台工程与 Golden Path、专业 Web 音视频、通用 WebAssembly、AI 驱动的浏览器运行时证据调试。
- 重构 `BROWSER-02`、`H5-03`、`AIAPP-09`、`WEBAI-03/04/05/07`、`WEB-03/04`、`SEC-01/02/04`、`OBS-01`、`IDENTITY-01`、`LEAD-01`，补齐现代调度、短期签名与分片上传、模型成本、OPFS、软导航指标、前沿 Web 能力的成熟度与降级边界。
- 新增渲染与数据、实时与离线、图形与实验、国际化、身份隐私、AI 产品安全治理、架构领导力和后端/云素养。
- MCP 主线升级为 2026 协议和 TypeScript SDK v2，移除旧会话模型作为当前实现的表述。
- 修正浏览器内置 AI 能力检测语义，补充实验性和降级边界。
- 替换已知失效或迁移的 React、Sigstore、Ant Design、Copilot、SRE、技术写作、Nuxt 和 Docker 资料。
- 将学习计划从早期 23 周方案重构为 64 周完整路线，容量从强制填满改为每日最多 390 分钟，并为补弱与变化保留缓冲。
- 修复 Windows 项目根目录判断和 CRLF Markdown 解析。
- 内容门禁新增资料数量、陈旧链接和换行兼容检查。
- 中文必读资料覆盖率提升至 219/219；`WEB-01` 移除 WAI-ARIA APG 英文必读资料并改用中文 MDN 语义、表单、ARIA 和 `dialog` 文档。
- 新增 `GIT-01`～`GIT-03`，覆盖 Git 对象与引用、暂存区与恢复、分支合并与变基、冲突处理、PR、提交治理以及 AI Agent worktree 隔离。
- 将过载的 `CS-01` 拆为复杂度、常用结构/算法正确性和前端大数据三个合同；将多运行时拆为浏览器扩展、Electron/Tauri 和跨平台原生三个合同。
- 新增 `SEC-04`、`AIAPP-12`、`WEBAGENT-01`，补齐跨源隔离/嵌入权限、AI 会话状态与 Agent 可操作 Web/WebMCP。
- 新增 `JS-07`、`BROWSER-02`、`SEC-05`、`TEST-04`、`PWA-02`、`WEBAI-11`、`AGENT-11`、`AIUI-01`、`AIAPP-13`、`AIMEDIA-01`、`SUSTAIN-01`，补齐元编程、浏览器生命周期、密码学、契约测试、安装/推送、WebNN、A2A、Agent UI 协议、长期记忆、生成式媒体和可持续工程。
- 扩充 `PERF-02`、`REALTIME-01`、`IDENTITY-01`、`PWA-01`、`AIAPP-08`、`AIGOV-01`，覆盖推测导航、WebTransport、CHIPS/Storage Access、OPFS、Agent 分层评估和 C2PA 内容来源。
- 合并 AI 研发总论、重复方案、无障碍、部署、worktree、MCP 基础和工具治理七组重叠知识合同。
- 缺少稳定中文版的知识点均补充逐点中文核心讲义；保留的英文原文逐条标为“仅用于版本核验”，不作为必读资料。
- 内容门禁升级为逐链接、逐讲义章节/深度、逐考核边界和弱模板检查；保留英文原文的知识点，其首考资料定位题只能使用《中文核心讲义》；覆盖范围不得短到无法界定机制、反例与验收边界。
- 导入从“只增不减”改为权威同步，旧数据库升级时会自动删除已经合并的七个幽灵知识点，不再需要手工清库。
- 端到端测试使用专用端口和一次性数据目录，并由测试生命周期显式启停服务，避免复用本机旧服务或残留测试进程。

## 验证命令

```bash
pnpm content:check
pnpm content:links
pnpm test
pnpm lint
pnpm build
pnpm test:e2e
```

当前已确认：

- 内容解析：20 个领域、219 个知识点全部通过。
- 资料链接：223 个本地链接与 569 个唯一远程链接全部确认存在；0 个失效、0 个未确认。
- 服务端：70 项测试通过，覆盖权威同步、导入、关系、排期、重置、评分、备份和执行器安全。
- 完整工作区：85 项自动化测试通过。
- 代码规范与生产构建：全部通过。
- 浏览器端到端：11 项用例全部通过并正常退出；使用独立的 219 点、64 周、448 天验收数据。

## 仍需外部环境的事项

1. DeepSeek 真实计费判卷需要用户在考核页主动触发。
2. Docker 执行器启用前需完成网络隔离、资源限制和逃逸测试。
3. 备份恢复需要重启服务以原子替换 SQLite，避免覆盖运行中连接。

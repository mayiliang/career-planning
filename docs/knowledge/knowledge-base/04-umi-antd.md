# 04 Umi/Max、Ant Design 与中后台应用

所有“通过”均需满足[统一考核规则](00-assessment-rules.md)。考核优先使用现有 Umi/Max 项目，不要求脱离真实技术栈造玩具。

### 应用初始化与路由

## UMI-01 路由、布局与页面生命周期

- [ ] 自评已掌握
- [ ] 已通过严格考核
- 学习资料：[Umi 路由](https://umijs.org/docs/guides/routes)、[中文核心讲义：UMI-01](../chinese-guides/umi-antd-engineering-workbench.md#umi-01)。覆盖范围：Umi 4 的配置式/约定式路由、嵌套布局、`base`、动态参数、拆包和 404；中文讲义补齐 `/console` 深链、服务器回退、SSR/hydration、旧地址迁移、可选参数不受支持的边界与五类证据。宽泛目录结构页不再作为必读。
- 严格考核：挑战类型：CODING；首考题 1（资料定位）：只允许使用《Umi 路由》《中文核心讲义：UMI-01》，分别定位路由声明、布局与 `base`，以及深链、SSR 首次匹配、服务器回退和失败验证的依据；首考题 2（机制解释）：闭卷按“深链请求→`base` 截取→路由匹配→外层布局→页面数据→404/登录分支→卸载”画 `/orders/:id` 时序，解释 history 与布局复用各在何处生效；首考题 3（最小产出）：以 `/orders/42`、`/orders/not-a-number`、`/legacy-orders/42` 为固定输入，实现嵌套路由、动态参数校验、404、登录页无布局与路由级拆包，交付路由表、可运行页面、深链刷新和旧路径迁移的菜单/跳转检查记录；预期前者进入订单页、后两者分别进入明确错误/迁移结果，并用浏览器地址、网络产物和导航录像验证；首考题 4（受限排错）：给定失败日志“部署到 `/console` 后刷新 `/console/orders/42` 返回 404，旧菜单仍跳 `/legacy-orders/42`”，只可检查 `base`、路由匹配顺序、静态服务器回退和菜单配置；提交请求 URL、匹配结果、两项被证伪假设、根因、最小修复与深链/前进后退回归；首考题 5（学习复述）：3 分钟向产品解释“为什么登录页不继承后台布局、为什么深链不是点击菜单的同义词”，并追问：Umi 4 不支持 `:id?` 时应如何表达可选页面并维持 404 与菜单高亮？复测变式：仅将部署前缀从 `/console` 改为 `/ops`，保留三条订单 URL 的相对路由、布局和静态回退；预期 `/ops/orders/42` 刷新成功、旧 `/console` 链接明确失败或迁移、拆包产物不变，提交新的地址栏/网络/导航录像。命题边界：只在所列资料和覆盖范围内命题。
- 通过标准：提交路由表、运行页与三条固定 URL 的地址栏/网络/导航证据；正常深链、非法参数和旧路径迁移均符合预期；失败案例能由限定配置复现并回归；`base`、history、配置式/约定式路由与布局边界解释准确。否决项：只点击站内菜单、以重定向掩盖 404，或未提交深链刷新证据。 评估边界：只接受本点题目输入与可复核产出。
- 预计耗时：资料 105 分钟；练习 165 分钟；项目 150 分钟；考核 90 分钟；复测 75 分钟

## UMI-02 initialState、运行时配置与应用初始化

- [ ] 自评已掌握
- [ ] 已通过严格考核
- 学习资料：[Umi Max 数据流与全局初始状态](https://umijs.org/docs/max/data-flow#全局初始状态)、[中文核心讲义：UMI-02](../chinese-guides/umi-antd-engineering-workbench.md#umi-02)。覆盖范围：官方目标章节承担 `getInitialState`、`@@initialState`、首次阻塞、loading/error/refresh/setInitialState；中文讲义补齐运行时配置职责、A 300ms/B 50ms 代次、401、重试、SSR 快照和敏感菜单不闪现。完整运行时配置 API 页因范围过宽而移除。
- 严格考核：挑战类型：CODING；首考题 1（资料定位）：只允许使用《Umi Max 数据流与全局初始状态》《中文核心讲义：UMI-02》，分别定位 `getInitialState`/全局消费，以及运行时扩展职责、session 恢复、SSR 边界、旧响应竞态和失败恢复的依据；首考题 2（机制解释）：闭卷按“token→用户→权限派生→layout/菜单→首屏请求→失败重试”画初始化状态机，说明代次如何阻止旧用户响应覆盖新会话；首考题 3（最小产出）：固定 fixture 为 A 用户请求 300ms、退出后 B 用户请求 50ms、`/me` 返回 401；实现带请求代次或取消的初始化链路，交付状态图、运行页、A/B 切换日志及重试入口；预期 B 的菜单和权限最终可见、401 回登录且不白屏，用网络瀑布和 DOM 快照验证；首考题 4（受限排错）：给定日志“`initialState A resolved` 在 `B login` 后写入菜单”，只可检查 initialState 缓存键、请求取消/代次、权限派生与 layout 消费点；提交时间线、排除项、根因、修复和刷新/切换用户回归；首考题 5（学习复述）：3 分钟说明 initialState 为什么不是任意全局缓存，并追问：SSR 首次状态与浏览器恢复状态冲突时，哪个来源可先渲染、哪个必须再校验？复测变式：仅将题 3 中 B 的 50ms 成功响应改为“50ms 失败、一次重试后成功”，保留 A 300ms、退出和代次；预期重试前为可恢复错误态、成功后仅显示 B 菜单且 A 永不回写，提交新的瀑布图、DOM 快照与代次日志。命题边界：只在所列资料、初始化 fixture 和可复核输出内评分。
- 通过标准：提交状态机、固定竞态日志、正常/401/切换用户/刷新证据和失败恢复 UI；初始化只产生可解释的请求，旧响应不能提交，layout/菜单不闪现敏感内容；能解释 SSR 与客户端恢复边界。否决项：用延时碰运气、白屏代替错误态，或只凭最终截图证明竞态已修复。 评估边界：只接受本点题目输入与可复核产出。
- 预计耗时：资料 105 分钟；练习 165 分钟；项目 150 分钟；考核 90 分钟；复测 75 分钟

### 请求、错误与权限

## UMI-03 请求层、错误处理与取消

- [ ] 自评已掌握
- [ ] 已通过严格考核
- 学习资料：[Umi Max Request](https://umijs.org/docs/max/request)、[AbortController](https://developer.mozilla.org/zh-CN/docs/Web/API/AbortController)、[中文核心讲义：UMI-03](../chinese-guides/umi-antd-engineering-workbench.md#umi-03)。覆盖范围：Umi 请求配置、拦截器、错误处理、`signal` 与 `AbortError`；中文讲义补齐响应 shape 验证、200/422/401/500/取消/离线六态、唯一反馈责任、请求 ID、Token 单飞与写操作结果未知边界。
- 严格考核：挑战类型：CODING；首考题 1（资料定位）：只允许使用《Umi Max Request》《AbortController》《中文核心讲义：UMI-03》，分别定位请求配置/拦截器、取消信号，以及响应 shape、六态映射、唯一反馈与写操作结果未知边界；首考题 2（机制解释）：闭卷按“请求配置→拦截器→响应 shape 解析→业务/HTTP/Abort 分类→唯一 UI 反馈”说明各状态在哪一层收口；首考题 3（最小产出）：固定接口返回依次为 200 `{code:0,data:{id:1}}`、422 字段错误、401、500、延迟 500ms 后取消；实现一个 service 和页面，交付统一配置、错误映射表、取消控制、测试及网络记录；预期每类只出现一次对应反馈、取消不提示失败，且未经验证的响应不能写 UI；首考题 4（受限排错）：给定失败日志“401 被拦截器 toast 一次、页面 catch 又 toast 一次；取消搜索显示 500”，只可检查错误映射责任、`AbortError` 分支和请求拦截器；提交调用栈/网络记录、排除项、根因、修复及六种状态回归；首考题 5（学习复述）：3 分钟说明“传输成功、业务成功、界面可提交”为何不同，并追问：静默请求为何不能借机吞掉 401？复测变式：仅将题 3 的 422 字段错误改为网络断开后恢复连接并重试，保留 200/401/500/取消、映射责任与单次反馈规则；预期离线显示可恢复反馈且重试只成功一次，取消仍无错误，提交新的网络记录、映射断言与六态回归。命题边界：只在所列资料、六种响应 fixture 和可复核输出内评分。
- 通过标准：交付 service、映射表、六种 fixture 的正常/边界/失败记录；401、403、422、500、取消和离线各有唯一且正确的 UI 行为，响应形状先验证再消费；故障能在限定层定位。否决项：重复 toast、把取消当错误，或以 `catch {}` 隐藏失败。 评估边界：只接受本点题目输入与可复核产出。
- 预计耗时：资料 105 分钟；练习 165 分钟；项目 150 分钟；考核 90 分钟；复测 75 分钟

## UMI-04 页面、按钮与数据权限

- [ ] 自评已掌握
- [ ] 已通过严格考核
- 学习资料：[Umi Max Access](https://umijs.org/docs/max/access)、[中文核心讲义：UMI-04](../chinese-guides/umi-antd-engineering-workbench.md#umi-04)。覆盖范围：官方中文页承担 `access.ts`、initialState 依赖、路由 access、`useAccess` 与 `<Access>`；中文讲义补齐认证/授权、RBAC/ABAC、最小权限、默认拒绝、四层矩阵、服务端最终裁决、降权清理与攻击回归。英文 OWASP 长页已由精确中文讲义替代。
- 严格考核：挑战类型：DESIGN_CASE；首考题 1（资料定位）：只允许使用《Umi Max Access》《中文核心讲义：UMI-04》，分别定位 access 映射 API，以及认证/授权、权限快照、服务端数据范围、默认拒绝与越权回归的依据；首考题 2（机制解释）：闭卷按“权限快照→路由/按钮/字段条件→service 请求范围→服务端最终拒绝”区分四层，并说明前端 access 更新不等于授权；首考题 3（最小产出）：固定角色为 auditor、editor、guest 和订单 `ownerId`；实现四层矩阵及直接访问 `/orders/42/edit`、伪造 local state、权限刷新三个检查，交付矩阵、页面记录、服务端拒绝模拟和回归测试；预期 guest 不见敏感字段、editor 不可编辑他人订单，伪造前端状态仍由服务端拒绝；首考题 4（受限排错）：给定失败现象“刷新时删除按钮闪现 200ms，直接 URL 返回空白但请求仍发出”，只可检查 access 初始值、路由守卫、按钮渲染条件和 service 数据范围；提交 DOM/网络证据、排除项、根因、修复和越权回归；首考题 5（学习复述）：3 分钟解释“隐藏按钮与授权”之间的差别，并追问：权限从 editor 降为 guest 时，正在编辑的表单应如何收口？复测变式：仅新增只读 reviewer，保留 auditor/editor/guest、同一 ownerId 与四层规则；预期 reviewer 可读不可编辑且伪造 local state 仍被服务端拒绝，提交新增矩阵行、URL/网络记录与降权回归。命题边界：只在所列资料、角色矩阵和可复核证据内评分。
- 通过标准：提交矩阵、三类攻击路径和正常/降权证据；前端体验与服务端最终授权边界清楚，权限来源单一且无敏感闪现；受限故障可定位。否决项：仅隐藏按钮、只测正常角色，或把服务端拒绝解释为前端权限成功。 评估边界：只接受本点题目输入与可复核产出。
- 预计耗时：资料 105 分钟；练习 165 分钟；项目 150 分钟；考核 90 分钟；复测 75 分钟

### 中后台交互组件

## ANTD-01 Form 数据流、联动与校验

- [ ] 自评已掌握
- [ ] 已通过严格考核
- 学习资料：[中文核心讲义：ANTD-01](../chinese-guides/umi-antd-engineering-workbench.md#antd-01)。覆盖范围：Form store、`initialValues`/回填/重置、`preserve`、`Form.List`、0/null、A 300ms/AB 50ms 校验代次、422 字段映射和防重复提交；Ant Design Form 全页约 2283 行且包含大量无关 API/示例，已移出初级必读。
- 严格考核：挑战类型：CODING；首考题 1（资料定位）：只允许使用《中文核心讲义：ANTD-01》，定位 Form store、回填/重置、`preserve`、动态数组、异步校验代次和固定验证；首考题 2（机制解释）：闭卷按“initialValues/回填→受控字段值→条件字段 preserve→异步校验代次→提交 payload”解释新建、编辑、只读间的值与错误归属；首考题 3（最小产出）：固定编辑记录 `{id:1,name:'A',quota:0,tags:['x']}`，异步名称校验 A 延迟 300ms、AB 延迟 50ms；实现动态 tags、条件额度、回填/只读与防重复提交，交付表单、提交 JSON、两次校验日志和键盘操作录像；预期 0 不被当空、AB 的结果生效且切回新建不串值；首考题 4（受限排错）：给定现象“编辑 A 后切新建仍带 tags；慢校验 A 覆盖 AB 错误”，只可检查 form 实例重置、`preserve`、initialValues 更新方式和校验代次；提交表单状态快照、排除项、根因、修复与回归；首考题 5（学习复述）：3 分钟说明 initialValues 为什么不是每次 props 更新都应覆盖输入，并追问：条件字段卸载后何时保留、何时清除值？复测变式：仅将固定记录的 quota 从 `0` 改为 `null`，保留 name/tags、校验时序与表单结构；预期 null 与 0 有不同显示/提交语义，AB 仍胜过 A，提交新的 DOM、payload 和校验时间线。命题边界：只在所列资料、固定记录和可复核表单输出内评分。
- 通过标准：提交运行表单、固定输入的 JSON/校验日志和新建、编辑、只读、慢快校验、重复提交证据；值、错误与接口契约一致，故障可在限定状态层定位。否决项：以 `any`/全局重置掩盖串值、把 0 当空，或没有异步校验的时间证据。 评估边界：只接受本点题目输入与可复核产出。
- 预计耗时：资料 105 分钟；练习 165 分钟；项目 150 分钟；考核 90 分钟；复测 75 分钟

## ANTD-02 Table/ProTable 查询、分页与导出

- [ ] 自评已掌握
- [ ] 已通过严格考核
- 学习资料：[中文核心讲义：ANTD-02](../chinese-guides/umi-antd-engineering-workbench.md#antd-02)。覆盖范围：Table/ProTable 的 URL 查询源、分页/排序/筛选、43 条数据与两次乱序请求、页码归一、跨页主键、列权限和导出任务状态机；包含 pageSize 20→10 复测与只下载一次证据。
- 严格考核：挑战类型：CODING；首考题 1（资料定位）：只允许使用《中文核心讲义：ANTD-02》，定位查询源、URL 序列化、响应代次、页码归一、跨页选择和导出状态机；首考题 2（机制解释）：闭卷按“筛选/排序→URL 序列化→服务端参数→响应代次→页码归一→导出任务状态”说明查询状态如何单向映射；首考题 3（最小产出）：固定总数 43、pageSize 20，先请求 `keyword=a&page=3` 后 300ms 返回，再请求 `keyword=ab&page=1` 后 50ms 返回；实现分页/排序/筛选 URL 同步、列权限、批量操作与导出任务，交付 URL、请求日志、导出进度与下载结果；用请求顺序、刷新恢复、筛选回第 1 页和下载一次的断言验证慢响应不能覆盖快响应；首考题 4（受限排错）：给定失败现象“删除本页最后一行后空表；刷新丢筛选；导出完成两次下载”，只可检查页码归一、URL 序列化、请求代次和导出任务 id；提交网络/URL 证据、排除项、根因、修复与回归；首考题 5（学习复述）：3 分钟说明“表格当前页”为什么不能当数据真相，并追问：长导出为何应是任务而非一次前端请求？复测变式：仅将 pageSize 从 20 改为 10，保留总数 43、列权限、两次请求时序和导出规则；预期筛选仍回第 1 页、URL 参数为 10、慢响应不覆盖快响应且只下载一次，提交新的 URL/网络记录与断言。命题边界：只在所列资料、固定查询 fixture 与可复核输出内评分。
- 通过标准：提交固定竞态的 URL/请求记录、分页回退、刷新恢复、权限列与导出状态证据；查询参数可追踪，慢响应不覆盖，导出只下载一次；故障可在限定状态映射定位。否决项：本地一次性加载全部数据、刷新靠内存恢复，或仅以最终表格截图证明正确。 评估边界：只接受本点题目输入与可复核产出。
- 预计耗时：资料 105 分钟；练习 165 分钟；项目 150 分钟；考核 90 分钟；复测 75 分钟

## ANTD-03 Modal、Drawer、详情与反馈

- [ ] 自评已掌握
- [ ] 已通过严格考核
- 学习资料：[中文核心讲义：ANTD-03](../chinese-guides/umi-antd-engineering-workbench.md#antd-03)。覆盖范围：Modal/Drawer/路由页选型、容器状态机、A 300ms/B 立即完成的会话归属、请求取消、表单/错误清理、Esc/遮罩脏数据确认、焦点圈定与恢复、危险操作结果未知；Ant Design Modal 全页约 672 行，已由固定中文讲义替代。
- 严格考核：挑战类型：CODING；首考题 1（资料定位）：只允许使用《中文核心讲义：ANTD-03》，定位 Modal/Drawer/路由选型、会话键、A/B 结果归属、脏数据关闭与焦点恢复；首考题 2（机制解释）：闭卷按“触发器→打开/焦点进入→编辑/保存请求→成功或失败状态→关闭→焦点返回”说明详情、编辑和确认状态保留边界；首考题 3（最小产出）：固定记录 A、B，A 保存延迟 300ms 后失败、B 立即成功；实现共用详情/编辑容器和危险删除二次确认，交付状态图、键盘脚本、保存日志与重开快照；预期 Esc 在未保存时提示、焦点回到触发 B 的按钮且 A 的失败不污染 B；首考题 4（受限排错）：给定现象“关闭 A 后打开 B 仍显示 A 的错误；按 Esc 后焦点落到 body”，只可检查 key/卸载策略、open 状态、异步结果归属和焦点恢复；提交 DOM 焦点记录、排除项、根因、修复与回归；首考题 5（学习复述）：3 分钟说明 Drawer 与 Modal 的任务边界，并追问：保存中的危险操作为何不应允许静默关闭？复测变式：仅将 A 的 300ms 保存结果从失败改为成功，保留 A/B、容器、Esc 和焦点路径；预期 A 成功后不遗留错误、再打开 B 焦点仍返回 B 触发器，提交新的保存日志、焦点记录与 A/B 断言。命题边界：只在所列资料、A/B fixture 和可复核输出内评分。
- 通过标准：提交可运行容器、状态图、A/B 结果、键盘/焦点和关闭保护证据；危险操作有二次确认，重复打开不串状态，失败定位可复现。否决项：只测鼠标路径、依赖动画结束碰巧清状态，或未记录焦点恢复。 评估边界：只接受本点题目输入与可复核产出。
- 预计耗时：资料 105 分钟；练习 165 分钟；项目 150 分钟；考核 90 分钟；复测 75 分钟

### 移动业务组件

## ANTD-04 Ant Design Mobile 与移动业务组件

- [ ] 自评已掌握
- [ ] 已通过严格考核
- 学习资料：[中文核心讲义：ANTD-04](../chinese-guides/umi-antd-engineering-workbench.md#antd-04)。覆盖范围：375×667/667×375、34px/0px 安全区、视觉视口、软键盘、触控与弹层、3s 上传状态机、断网恢复、字体放大与屏幕阅读器；明确高密度桌面任务不应硬搬移动端及文件跨刷新恢复限制。
- 严格考核：挑战类型：TOOL_OPERATION；首考题 1（资料定位）：只允许使用《中文核心讲义：ANTD-04》，定位视觉视口、安全区、软键盘、触控、上传状态机、可访问性和桌面迁移边界；首考题 2（机制解释）：闭卷按“视口/安全区/软键盘→滚动与弹层定位→触控操作→上传状态机→弱网恢复”说明移动业务组件的约束链；首考题 3（最小产出）：固定设备条件为 375×667、横屏 667×375、底部安全区 34px、上传接口延迟 3s；实现预约表单、列表、日期选择和上传，交付真机或模拟器录像、视口截图、上传进度与弱网失败重试记录；预期键盘不遮提交、弹层可关闭、触控目标可达、上传失败不丢已选文件；首考题 4（受限排错）：给定失败现象“键盘弹出后提交按钮被遮挡；横屏底部操作栏盖住列表；弱网重复上传”，只可检查安全区/视口样式、滚动容器、弹层定位和上传状态机；提交设备条件、DOM/网络证据、排除项、根因、修复与回归；首考题 5（学习复述）：3 分钟说明为什么桌面 Modal 不可直接搬到移动端，并追问：网络从 4G 变为离线时，上传列表的可恢复状态应保存什么？复测变式：仅将底部安全区从 34px 改为 0，保留两种视口、上传 3s 延迟与弱网失败重试；预期键盘下提交仍可见、横屏操作栏不遮列表且已选文件保留，提交新的真机/模拟器录像、视口截图和上传状态记录。命题边界：只在所列资料、设备 fixture 和可复核操作证据内评分。
- 通过标准：提交固定设备条件下的录像/截图、上传状态记录及正常/横屏/键盘/弱网证据；触控、滚动、安全区和失败恢复均可观察，不照搬桌面交互。否决项：仅桌面缩放预览、以固定高度遮盖键盘，或失败后清空用户选择。 评估边界：只接受本点题目输入与可复核产出。
- 预计耗时：资料 105 分钟；练习 165 分钟；项目 150 分钟；考核 90 分钟；复测 75 分钟

## 领域综合考核

- [ ] 已通过领域综合考核
- 任务：用 Umi Max + Ant Design 实现一个审核业务模块，包含路由、初始化、权限、查询表格、详情、编辑、审核、异步导出和异常处理。
- 通过标准：8 小时内完成；刷新与深链正常；权限绕过测试失败；自动测试覆盖核心流；能够说明项目目录、状态、请求和组件边界。评估边界：领域综合考核只依据本领域列出的资料、题目输入和可复核产出；不得用资料外经验替代跨知识点整合证据。
- 预计耗时：资料 105 分钟；练习 165 分钟；项目 150 分钟；考核 90 分钟；复测 75 分钟

# 14 浏览器 AI、本地推理与新 Web 平台

这一领域关注 AI 能力向浏览器和边缘设备迁移后的前端职责：算力检测、模型下载、WebGPU、Worker、隐私、离线与云端回退。

### 浏览器 AI 能力与推理后端

## WEBAI-01 Chrome Built-in AI API、分级可用性与能力检测

- [ ] 自评已掌握
- [ ] 已通过严格考核
- 学习资料：[校订中文讲义（WEBAI-01）](../chinese-guides/content-audit-13-14.md#webai-01)、[Chrome Built-in AI 入门](https://developer.chrome.com/docs/ai/get-started?hl=zh-cn)、[Built-in AI API 状态](https://developer.chrome.com/docs/ai/built-in-apis?hl=zh-cn)、[Prompt API](https://developer.chrome.com/docs/ai/prompt-api?hl=zh-cn)。覆盖范围：Prompt 及其他内置 AI API 的 Stable/Origin Trial/Developer Trial/停用状态；`availability()`、模型下载、用户激活、硬件/存储条件、语言能力、会话创建/销毁、流式/取消和错误；Chrome/桌面限制、状态随版本变化、版本冻结、遥测、隐私以及云端或非 AI 渐进降级；全部必读资料均为中文。
- 严格考核：挑战类型：CODING；首考题 1（资料定位）：只允许使用《校订中文讲义（WEBAI-01）》《Chrome Built-in AI 入门》《Built-in AI API 状态》《Prompt API》，分别定位降级/隐私实验、硬件与下载条件、各 API 状态和 Prompt 会话机制，制作带核验日期、Chrome 版本、平台和状态的能力矩阵；首考题 2（机制解释）：闭卷解释安装/首次下载、模型可用性、会话和 API 成熟度如何共同决定功能；反例说明全局对象存在或缓存值为 `available` 时，为什么仍不能跳过浏览器、语言、存储和会话条件的重新判定；首考题 3（最小产出）：固定夹具 `WEBAI-01-v1`：输入为 Chrome 148 已下载/未下载、非 Chrome、语言不支持、空间不足五个能力快照及同一 Prompt 选项；实现 `availability()`—`create()` 状态机、取消/销毁日志和非 AI/云端分支，交付能力矩阵、UI 截图和五例自动断言；预期已下载快照进入原生会话，未下载快照仅经用户激活进入下载路径，其余三例不复用原生会话并给出预定降级原因；以五例断言、状态/遥测日志和截图验证；首考题 4（受限排错）：仅把 `WEBAI-01-v1` 的模型状态由“已下载”改为“未下载”，保持 Chrome 148、桌面平台、语言、存储、Prompt 选项和其余快照不变；预期不得复用已下载缓存，必须进入受用户激活约束的下载路径且取消后销毁会话；仅允许修改能力判定、缓存键和会话清理候选代码，提交该变更的新能力矩阵、取消/销毁日志、假设与证伪记录、最小修复 diff 和同一五例回归；首考题 5（学习复述）：3 分钟说明如何维护随浏览器版本变化的能力合同，并用一例“全局对象存在但不可用”解释不能只做特性探测。复测变式：仅将 `WEBAI-01-v1` 未下载快照的用户激活结果改为“取消”，保持五个能力快照、Chrome 148、桌面平台、语言、存储、Prompt 选项与预定降级规则不变；预期未下载快照不创建原生会话、取消后无遗留会话，其他四例结果不变；提交新的能力矩阵、取消/销毁与遥测日志、五例断言结果和 UI 截图。命题边界：每次命题必须重新核验官方状态页并记录日期；截至 2026-08-09，Prompt API 的 Web 路径可按 Chrome 148 核验，不能据此推导其他 API、其他平台或其他浏览器稳定。
- 通过标准：不把实验或 Chrome 专属 API 当全平台能力；状态矩阵带日期且与官方页面一致；模型下载需要用户激活；会话和资源可释放；移动端、其他浏览器、硬件/语言不支持时有清晰替代；遥测区分原生、云端和非 AI 路径；五例快照、截图、状态/遥测日志与断言可复核。否决项：只在当前开发机调用成功、硬编码一次状态表、复用跨浏览器缓存或没有 fallback 不能通过。评估边界：仅评估能力检测与降级，不以模型回答质量代替本点证据。
- 预计耗时：资料 135 分钟；练习 195 分钟；项目 180 分钟；考核 105 分钟；复测 90 分钟

## WEBAI-02 WebGPU 计算管线、张量与 AI 推理后端

- [ ] 自评已掌握
- [ ] 已通过严格考核
- 学习资料：[中文核心讲义](../chinese-guides/content-audit-13-14.md#webai-02)。覆盖范围：面向 AI 推理的 adapter/device 能力快照、张量布局、buffer/bind group、WGSL 计算管线、提交/读回、内存预算、数值正确性、设备丢失和 WebGPU/Wasm/云端降级；通用渲染与 WebGPU API 入门不在本点重复考核。截至 2026-08-25，本讲义沿用已核验的 2026-07-14 Candidate Recommendation Draft 状态，不得写作已成为 Recommendation 或据此跳过能力检测；实施时仍须重新核验规范状态。
- 严格考核：挑战类型：CODING；首考题 1（资料定位）：只允许使用《中文核心讲义》，定位能支撑「WebGPU 计算管线、张量与 AI 推理后端」的定义、关键机制、边界与反例并标明章节；首考题 2（机制解释）：闭卷解释张量如何从布局、buffer、bind group、WGSL pipeline、dispatch、staging readback 到后处理，并说明上传/读回、对齐和设备限制怎样传播为失败；反例说明即使 shader 内核较快，为什么一次端到端推理仍可能因读回或数据搬运更慢；首考题 3（最小产出）：固定夹具 `WEBAI-02-v1` 为固定种子的两层 `matmul → ReLU → matmul` 与 64×128、128×32 张量；实现 CPU/Wasm 金基线和 WebGPU 版本，交付误差/top-1、一次冷启动与 30 次热运行、上传/读回字节和峰值 buffer 记录；预期 WebGPU 的 top-1 与金基线一致且误差不超过夹具容差，能力不足或设备丢失时选择 Wasm；以自动断言、30 次样本和后端决策/资源释放日志验证；首考题 4（受限排错）：仅把 `WEBAI-02-v1` 的设备状态改为 `device.lost`，保持固定种子、张量、金样本、容差和 binding limit 不变；预期释放失效资源、重建失败后降级至 Wasm，且数值断言继续通过；仅允许修改 buffer 生命周期和降级分支，不改金样本或容差，提交该变更的设备/资源日志、假设与证伪记录、最小修复 diff 和同一夹具回归；首考题 5（学习复述）：3 分钟说明何时 WebGPU 能摊薄数据搬运，何时选择 Wasm 或云端，并指出读回为何会反转收益。复测变式：仅将 `WEBAI-02-v1` 的 `maxStorageBufferBindingSize` 改为小于该夹具所需 buffer 字节数，保持固定种子、两层张量、金样本、容差、设备状态与 30 次测量次数不变；预期能力判定在创建前选择 Wasm，数值/top-1 断言仍通过；提交新的 adapter limit 快照、后端决策与资源释放日志、30 次样本和自动断言结果。命题边界：通用渲染不作为 AI 推理产出；
- 通过标准：WebGPU 非 Baseline，实际后端和降级理由可观测；CPU/Wasm 与 WebGPU 数值误差有阈值且 top-1 一致；端到端测量包含创建、上传、提交和读回，不用单一 shader 内核时间代替；buffer 生命周期、错误和 `device.lost` 可处理；提交夹具、30 次原始样本、自动断言、后端决策和资源日志；能说明与 Wasm/云端的选择边界。否决项：只画图、只跑一次 compute shader、无金基线、无后端回退或用两项夹具变更归因不能通过。评估边界：仅评估 AI 推理计算管线，不以通用渲染替代本点证据。
- 预计耗时：资料 135 分钟；练习 195 分钟；项目 180 分钟；考核 105 分钟；复测 90 分钟

## WEBAI-03 WebAssembly、SIMD 与推理性能

- [ ] 自评已掌握
- [ ] 已通过严格考核
- 学习资料：[中文核心讲义](../chinese-guides/content-audit-13-14.md#webai-03)。覆盖范围：在完成 `WASM-01` 的通用运行时与宿主边界后，聚焦本地 AI 推理的 Wasm/SIMD/线程后端、模型算子、张量布局、量化、预热、内存复用、跨边界数据搬运，以及与 JavaScript、WebGPU、WebNN 和云端推理的端到端性能/兼容/功耗代理比较；不再承担通用 WebAssembly 基础教学。截至 2026-08-25，本讲义沿用已核验的 WebAssembly Core 3.0 版本边界；深层生成页日期不解释为版本发布日期，已归档的旧 SIMD 提案也不再作为学习资料，实施时须重新核验运行环境。
- 严格考核：挑战类型：CODING；首考题 1（资料定位）：只允许使用《中文核心讲义》，定位 SIMD、线程、量化、内存复用和推理后端比较边界；首考题 2（机制解释）：闭卷解释模型加载、预处理、Wasm 算子执行、后处理和 UI 之间的端到端成本，说明峰值算子更快为何不必然改善用户延迟；反例说明 SIMD 可用时，跨边界复制或量化尺度错误为何仍会使端到端结果不可接受；首考题 3（最小产出）：固定夹具 `WEBAI-03-v1`：同一 8 MB int8 文本分类模型、50 条冻结输入和 50 个标签金样本，在同一浏览器中分别运行 JavaScript、Wasm 标量、Wasm SIMD/线程及可用的 WebGPU/WebNN；交付每后端的冷启动下载/编译/预热、首结果、10 次稳态 p50/p95、峰值内存、包体、预测标签与相对数值误差表；预期在具备跨源隔离时才可选线程后端，所有可用后端的量化误差不超过金样本阈值；以 50 条标签断言、10 次样本、能力快照和后端选择日志验证；首考题 4（受限排错）：仅关闭 `WEBAI-03-v1` 的跨源隔离，保持模型、量化尺度、50 条输入、浏览器和金样本不变；预期线程后端降为单线程，预测标签与误差断言仍通过；仅允许修改隔离配置或后端选择候选代码，提交该变更的响应头、后端选择、误差表和 p95 新证据、假设与证伪记录、最小修复 diff 及同一 50 条输入回归；首考题 5（学习复述）：3 分钟说明设备、任务与隐私约束下的推理后端选择。复测变式：仅将 `WEBAI-03-v1` 的 WebGPU 能力快照改为不可用，保持 8 MB int8 模型、量化尺度、50 条冻结输入、金样本、浏览器、跨源隔离与 10 次测量次数不变；预期从可用后端集合移除 WebGPU，Wasm 路径继续满足 50 条标签和误差断言；提交新的能力快照、后端选择日志、Wasm 的 p50/p95 原始样本和 50 条断言结果。命题边界：通用模块/ABI/调试能力由 `WASM-01` 考核；不支持实验后端时允许比较可用子集但必须说明缺口。
- 通过标准：基准方法可复现且区分冷启动/稳态；结果误差有阈值；不只给一次测量或单算子吞吐；能解释序列化、布局和跨边界调用成本；线程有单线程降级；提交固定夹具、能力快照、50 条断言、原始样本和后端选择日志；按设备能力、隐私、功耗代理和用户延迟选择实现。否决项：重复 `WASM-01` 的加载示例、只证明 SIMD 跑通、没有端到端推理正确性或一次复测同时改变隔离和量化尺度不能通过。评估边界：仅评估 Wasm 推理性能，不以模块加载功能替代端到端证据。
- 预计耗时：资料 135 分钟；练习 195 分钟；项目 180 分钟；考核 105 分钟；复测 90 分钟

## WEBAI-11 WebNN 图计算、抽象设备偏好与推理后端

- [ ] 自评已掌握
- [ ] 已通过严格考核
- 学习资料：[中文核心讲义](../chinese-guides/content-audit-13-14.md#webai-11)。覆盖范围：WebNN Candidate Recommendation Draft 的 `MLContext`、操作数/常量、图构建、张量类型/布局、算子支持、运行时适配、回退、复制、热身和缓存；设备选择使用抽象 `powerPreference`、`accelerated` 偏好或显式 `GPUDevice`，不得教授已移除的 NPU/GPU/CPU 枚举。WebGPU 管线与资源机制直接复用前置 `WEBAI-02`，不在本点重复列为资料。
- 严格考核：挑战类型：CODING；首考题 1（资料定位）：只允许使用《中文核心讲义》，定位上下文、图构建、执行、设备偏好和降级规则；首考题 2（机制解释）：闭卷解释模型图如何映射到 WebNN 后端，以及设备、算子支持和数据搬运为何影响性能；反例说明 `powerPreference` 被记录时，为什么仍不能证明 `conv2d` 可执行或性能必然更好；首考题 3（最小产出）：固定夹具 `WEBAI-11-v1`：24×24 灰度输入、一个 `conv2d→relu→reshape→softmax` 图、10 个冻结样本及其概率金样本；实现 WebNN/WebGPU/Wasm 三后端适配、能力探测和 `powerPreference` 记录，交付图构建清单、每后端预热/10 次延迟/吞吐/内存/概率误差表及断言；预期支持图的 WebNN 输出在误差阈值内，不支持 `conv2d` 时不阻断而选择 WebGPU 或 Wasm；以能力快照、10 个概率断言、10 次样本和后端决策日志验证；首考题 4（受限排错）：固定失败日志为 `NotSupportedError: conv2d`、`layout NHWC expected/NCHW actual`、`compileCount=10` 与 `maxError=0.31`，对应三名候选：①「算子能力未探测」——若快照声明不支持仍尝试编译则成立，若已在编译前降级则证伪；②「布局转换遗漏」——若张量形状记录为 NCHW 且适配器期望 NHWC 则成立，若转换后误差仍超阈值则证伪；③「图缓存键缺失」——若相同图键下 `compileCount=10` 则成立，若每次图键不同则证伪。仅在被证实的候选处作最小适配、布局或缓存修复，不改图、输入或阈值，提交最小修复 diff、证伪观察和同一 10 样本夹具回归；首考题 5（学习复述）：3 分钟说明何时选择 WebNN、WebGPU、Wasm 或云端推理。复测变式：仅将 `WEBAI-11-v1` 的设备能力快照改为不支持 `conv2d`，保持图、10 个输入、概率金样本、误差阈值、`powerPreference` 与测量次数不变；预期选择 WebGPU 或 Wasm，提交新的 capability 快照、后端决策、10 样本误差表和原始样本证据。命题边界：不得假设设备偏好等于确定硬件，也不得把实验性能力承诺为全浏览器可用；
- 通过标准：功能检测和降级可靠；三后端结果误差有阈值；测试区分冷启动与稳态；资源可释放；不支持时不阻断主功能；提交固定夹具、能力快照、概率断言、原始样本、候选证伪观察和同夹具回归；结论同时包含性能、兼容性、隐私和功耗证据。否决项：只在单一设备跑通一次模型、把设备偏好当成确定硬件、无回退证据或未逐一证伪候选即修复不能通过。评估边界：仅评估 WebNN 图计算与后端选择，不把实验后端承诺为全浏览器能力。
- 预计耗时：资料 135 分钟；练习 195 分钟；项目 180 分钟；考核 105 分钟；复测 90 分钟

### 本地 AI 应用工程

## WEBAI-04 AI 推理 Worker、任务池与主线程响应

- [ ] 自评已掌握
- [ ] 已通过严格考核
- 学习资料：[中文核心讲义](../chinese-guides/content-audit-13-14.md#webai-04)。覆盖范围：在 `CS-03` 的 Worker、结构化克隆和 Transferable 基础上，聚焦 AI 预处理、Embedding、推理与后处理任务池：版本化消息、张量所有权、批处理、背压、优先级、进度、取消、崩溃恢复、线程规模和主线程 INP。Web Workers、SharedWorker、OffscreenCanvas、Atomics 与 RPC 库只是实现参考，通用基础不在本点重复考核。
- 严格考核：挑战类型：CODING；首考题 1（资料定位）：只允许使用《中文核心讲义》，定位 AI 任务池中的张量所有权、批处理、背压、进度、取消、崩溃恢复和线程规模边界；首考题 2（机制解释）：闭卷解释一次预处理—推理—后处理任务如何排队、转移数据、取消和回收，并说明增加 Worker 为何可能降低吞吐或 INP；反例说明把函数移入 Worker 后，为何转移后重用 buffer 或未校验取消令牌仍会破坏正确性；首考题 3（最小产出）：固定夹具 `WEBAI-04-v1`：8 个 4 MB 张量任务，优先级为 `3,3,2,2,1,1,0,0`，池大小 2、队列上限 4，第 3 个任务在运行中取消且第 5 个 Worker 崩溃；实现版本化消息、Transferable、优先级、批处理、背压、进度、取消和异常传播，交付任务状态表、消息序列、复制字节数、内存/吞吐/INP 测量；预期高优先级先出、取消任务无回调且崩溃后只重试未确认任务；以三项自动断言、所有权表、消息序列和任务快照验证；首考题 4（受限排错）：保留日志 `DataCloneError detached buffer`、`task-3 completion after cancel`、`seq 12 before seq 11`、`worker-2 exited`；候选根因限定为「转移后重用 buffer」「取消令牌未在回调校验」「序列号比较错误」「崩溃任务状态未持久化」，逐项以所有权表、取消断言、消息序列和任务快照证伪，定位后只修改所有权、取消/序列/恢复候选代码，交付最小修复 diff 和同一 8 任务回归；首考题 5（学习复述）：3 分钟说明如何根据模型、设备和交互预算配置 AI 任务池。复测变式：只在 `WEBAI-04-v1` 的组件卸载时刻改为第 4 个任务开始后，保持队列、张量和池大小不变；预期已取消任务不回调、未确认任务由重启 Worker 接手，提交新的状态表、序列日志和 INP 证据。命题边界：通用 Worker 类型、共享内存原语和 OffscreenCanvas 只作为 CS-03 前置，不在本点重复实现。
- 通过标准：主线程长任务/INP 改善且总成本有记录；大数据不重复复制；消息可关联、限流、取消和版本兼容；Worker 可终止/重启且错误不静默；共享状态有不变量和并发测试；提交固定夹具、任务快照、所有权/消息日志、三项断言和回归；Offscreen/SharedWorker/Atomics 不支持或跨源隔离缺失时有正确降级。否决项：只把函数搬进 Worker、无限创建线程、以共享内存绕过所有权设计或没有取消/崩溃证据不能通过。评估边界：仅评估 AI 任务池，不重复通用 Worker 原语。
- 预计耗时：资料 135 分钟；练习 195 分钟；项目 180 分钟；考核 105 分钟；复测 90 分钟

## WEBAI-05 浏览器模型下载、缓存与版本管理

- [ ] 自评已掌握
- [ ] 已通过严格考核
- 学习资料：[中文核心讲义](../chinese-guides/content-audit-13-14.md#webai-05)。覆盖范围：仅负责模型、分词器和配置等 AI 大型制品的清单、摘要/签名、Range/分片、断点、配额、临时态、校验后原子激活、旧版回滚、多标签协调和垃圾清理。Cache API、StorageManager 与 OPFS 只是可替换的存储实现参考；通用存储 API、Service Worker 生命周期与应用壳缓存不在本点重复考核。
- 严格考核：挑战类型：CODING；首考题 1（资料定位）：只允许使用《中文核心讲义》，定位模型清单、校验、存储选型、原子切换和回滚规则；首考题 2（机制解释）：闭卷解释为何 HTTP 请求成功、文件大小正确或缓存命中都不能单独证明模型可安全激活；反例说明摘要通过但原子切换中断时，为什么不能把临时制品当作当前版本；首考题 3（最小产出）：固定夹具 `WEBAI-05-v1` 为 128MB 模型、四段 Range、v1/v2 清单、已损坏第二段和 160MB 配额；实现带版本清单/摘要的分片下载器、临时态与原子激活、旧版回滚和多标签协调，交付下载状态表、激活日志及中断/损坏/配额/竞态四例自动验证；预期损坏段和配额不足不激活 v2，中断后清理临时态并保留 v1，多标签只产生一个下载；以清单/摘要记录、激活日志和四例断言验证；首考题 4（受限排错）：根据夹具 Range 不一致、摘要失败、半激活和多标签双下载日志定位修复；受限范围：仅允许修改段校验、临时态、锁和激活顺序，不改清单或预期版本，逐项提交假设、证伪、根因、修复与回归；首考题 5（学习复述）：3 分钟说明 Cache API、IndexedDB、OPFS 与重新下载的选择。复测变式：仅把 `WEBAI-05-v1` 摘要校验后原子切换前的电源状态改为中断，保持 128MB 模型、四段 Range、v1/v2 清单、损坏第二段、160MB 配额与预期版本不变；预期清理临时态且 v1 仍可用；提交新的下载状态表、激活日志、四例断言和回归证据。命题边界：浏览器存储不是永久承诺或可信密钥库；摘要验证完整性，签名/受信清单才建立来源信任。
- 通过标准：损坏、来源不明或版本不兼容模型不会激活；下载与激活状态可恢复且幂等；旧版本在兼容窗口可回滚；配额、驱逐、多标签和 Service Worker 更新有测试；不会无限占用磁盘；用户能看到体积、网络、版本和删除入口；提交固定夹具、清单/摘要、激活日志和四例断言。否决项：只放入 Cache API、没有临时态/原子切换、通过清空全部存储解决迁移或无损坏/中断证据不能通过。评估边界：仅评估 AI 制品下载与版本管理，不以应用壳缓存替代本点证据。
- 预计耗时：资料 135 分钟；练习 195 分钟；项目 180 分钟；考核 105 分钟；复测 90 分钟

## WEBAI-06 本地 Embedding 与语义搜索

- [ ] 自评已掌握
- [ ] 已通过严格考核
- 学习资料：[中文核心讲义](../chinese-guides/content-audit-13-14.md#webai-06)。覆盖范围：浏览器内 Embedding、向量归一化、相似度、索引、增量更新、持久化和隐私；覆盖模型版本不一致、召回评估和数据删除。
- 严格考核：挑战类型：CODING；首考题 1（资料定位）：只允许使用《中文核心讲义》，定位能支撑「本地 Embedding 与语义搜索」的定义、关键机制、边界与反例并标明章节；首考题 2（机制解释）：闭卷解释文本、预处理、模型版本、向量、索引命名空间、top-k 与原文之间的状态链，以及更新/数据撤回如何传播；反例说明 top-k 看似相关时，为什么仍可能混入已撤回文档或不同模型维度的向量；首考题 3（最小产出）：固定夹具 `WEBAI-06-v1`：60 篇带 tenant 标记的中文小语料、`embed-zh-v1`（384 维）、12 条标注查询、`topK=5`；在 Worker 建索引，交付 recall@5/MRR、首结果、索引体积和每文档重算计数；再按夹具更新 6 篇、撤回 4 篇并追加 8 条查询，断言仅 6 篇重算、20 条验收集无撤回文档命中；以夹具给定的新 hash/维度建 v2 命名空间，交付双读对照、切换记录及取消时继续由 v1 服务的断言；预期跨租户零命中，v2 未达到夹具阈值或重建取消时不切换且 v1 连续服务；以 20 条查询断言、候选/缓存快照、索引元数据和指标报告验证；首考题 4（受限排错）：固定失败日志为 `duplicate doc-08`、`vector dimension 384/768` 与 `cache hit removed-doc-12`，对应三名候选：①「导入去重键遗漏」——若同一文档键在索引元数据出现两次则成立，若唯一键已去重则证伪；②「命名空间未绑定 hash/维度」——若 384 与 768 向量共用同一命名空间则成立，若元数据隔离正确则证伪；③「撤回未清候选缓存」——若候选/缓存快照仍含 `doc-12` 则成立，若两层均为空则证伪。仅在被证实的候选处作最小索引、命名空间或缓存修复，不改语料、查询、`topK` 或阈值，提交最小修复 diff、证伪观察和同一 20 条查询夹具回归；首考题 5（学习复述）：3 分钟说明召回指标、版本迁移、数据撤回与本地隐私边界。复测变式：仅撤回 `doc-12`，保持 60 篇语料的其余内容、模型 hash/384 维、12+8 条查询、`topK=5`、v1/v2 状态与 v2 重建进度不变；预期所有候选层零 `doc-12` 且 v1 连续服务，提交新的候选/缓存快照、索引状态、20 条查询断言和服务日志证据。命题边界：不以通用数据库 CRUD 或“看起来相关”替代语义检索证据。
- 通过标准：UI 不冻结；索引命名空间与模型 hash/维度绑定；增量更新只重算变化文档；原文、向量、候选与缓存均不可命中已撤回数据；数据留在本地且跨租户零命中；至少 20 条标注查询报告 recall@5/MRR；v2 未达阈值或重建中断时可继续用/回滚 v1；提交夹具、候选/缓存快照、指标、候选证伪观察和同夹具回归。否决项：只有 top-k Demo、无版本/撤回/召回证据、静默混用向量或未逐一证伪候选即修复不能通过。评估边界：仅评估本地 Embedding 语义搜索，不以通用 CRUD 或主观相关性代替证据。
- 预计耗时：资料 135 分钟；练习 195 分钟；项目 180 分钟；考核 105 分钟；复测 90 分钟

## WEBAI-07 多模态输入、媒体管线与隐私

- [ ] 自评已掌握
- [ ] 已通过严格考核
- 学习资料：[中文核心讲义](../chinese-guides/content-audit-13-14.md#webai-07)、[MediaDevices](https://developer.mozilla.org/zh-CN/docs/Web/API/MediaDevices)、[WebCodecs](https://developer.mozilla.org/zh-CN/docs/Web/API/WebCodecs_API)。覆盖范围：在 `MEDIA-01` 的专业媒体管线基础上，聚焦图片、音频和视频作为模型输入时的授权、采集/选择、格式与尺寸标准化、抽帧/分段、降采样、预览、批处理、取消、元数据清理、本地/云端路由、数据最小化、留存和无障碍替代；WebCodecs 仅用于需要帧级预处理的路径，完整播放/编解码治理不在本点重复考核。通用 `File API` 只讲选择/读取，不能单独覆盖 AI 输入管线，已不列为本点资料；全部必读资料均为中文。
- 严格考核：挑战类型：CODING；首考题 1（资料定位）：只允许使用《中文核心讲义》《MediaDevices》《WebCodecs》，定位媒体输入、帧级预处理与隐私边界；首考题 2（机制解释）：闭卷解释从用户授权到采集、预处理、推理/上传、结果呈现和删除的生命周期，以及本地处理何时仍可能泄露敏感数据；反例说明用户曾授权或本地处理时，为什么仍不能省略上传确认、EXIF 清理和取消后的帧/轨道释放；首考题 3（最小产出）：固定夹具 `WEBAI-07-v1`：一张带 EXIF 的 12 MB 图片、18 秒音频、30 秒 1080p 视频、指定摄像头 `cam-A` 及低端设备能力快照；实现能力检测、每秒 1 帧抽帧/16 kHz 分段或 1024 px 降采样、预览、进度、取消、设备切换、元数据清理、上传确认和本地降级，交付处理清单、帧/轨道释放日志、上传确认记录；预期拒绝确认时零上传，取消后零活动轨道/VideoFrame，低端快照走预定本地降级；以三项断言、元数据扫描、资源计数和上传请求记录验证；首考题 4（受限排错）：固定失败日志为 `devicechange cam-A removed`、`frameQueue=93`、`EXIF GPS remains` 与 `VideoFrame leaked`，对应四名候选：①「设备变更未重建会话」——若 `cam-A` 移除后旧轨道仍活动则成立，若已停止并重建则证伪；②「背压阈值遗漏」——若队列计数超过夹具上限仍继续入帧则成立，若入帧被限流则证伪；③「清理步骤在序列化后」——若输出元数据扫描仍含 GPS 则成立，若序列化前扫描为空则证伪；④「取消未 close frame/track」——若取消后的资源计数非零则成立，若均为零则证伪。仅在被证实的候选处作最小会话、背压、清理或释放修复，不改媒体、帧率、尺寸、确认要求或预期，提交最小修复 diff、证伪观察和同一媒体夹具回归；首考题 5（学习复述）：3 分钟说明文件作为输入、媒体采集、WebCodecs 预处理和专业媒体管线的分工。复测变式：仅将 `cam-A` 在处理第 8 帧时改为不可用，保持图片、音频、视频、帧率、尺寸、本地模型、低端快照与拒绝上传确认不变；预期停止旧轨道并关闭帧、保留本地降级且零上传，提交新的轨道/帧资源日志、确认记录、三项断言和输出预览证据。命题边界：不得重复要求 `MEDIA-01` 的 MSE/EME/DRM/ABR 实现，也不得默认上传原始媒体。
- 通过标准：授权按需且用途透明；停止后关闭媒体轨和帧资源；输入格式、尺寸、时长和批次有上限；不默认上传原始内容；本地/云端路由和留存可解释；敏感元数据处理明确；提交固定夹具、权限/取消/资源释放/上传确认记录、元数据扫描、候选证伪观察和同夹具回归；低端或不支持 WebCodecs 的设备有可用降级。否决项：只调用文件选择器、只在单设备采集成功、没有删除/取消/上传确认，或未逐一证伪候选即修复不能通过。评估边界：仅评估 AI 媒体输入管线，不重复专业媒体播放/DRM 实现。
- 预计耗时：资料 135 分钟；练习 195 分钟；项目 180 分钟；考核 105 分钟；复测 90 分钟

## WEBAI-08 混合推理与端云路由

- [ ] 自评已掌握
- [ ] 已通过严格考核
- 学习资料：[校订中文讲义（WEBAI-08）](../chinese-guides/content-audit-13-14.md#webai-08)、[Network Information API](https://developer.mozilla.org/zh-CN/docs/Web/API/Network_Information_API)。覆盖范围：按设备能力、隐私、延迟、网络、成本、质量和功耗在本地/云端间路由；覆盖探测误差、权限变化、回退、结果协议一致性、可观测决策和用户告知。
- 严格考核：挑战类型：DESIGN_CASE；首考题 1（资料定位）：只允许使用《校订中文讲义（WEBAI-08）》《Network Information API》，定位设备/网络探测的非承诺性质、隐私优先规则、路由回退与用户告知依据；首考题 2（机制解释）：闭卷说明能力、敏感级别、p95 延迟预算、网络质量、成本与质量阈值如何依次进入路由决策，及本地失败为何不能静默上传；反例说明网络探测声称高速时，为什么它不能覆盖敏感标记或用户确认；首考题 3（最小产出）：固定夹具 `WEBAI-08-v1` 输入为四档设备、离线/慢网/误报高速网络、敏感/非敏感任务和两组质量成本预算；交付可审计决策表、路由状态图、用户确认文案与 16 例路由测试；预期敏感任务始终留在本地或等待显式确认，离线任务不尝试云端，其他组合的结果协议和回退原因与决策表一致；以 16 例断言、路由事件日志和确认记录验证；首考题 4（受限排错）：固定错误日志 `route=cloud sensitive=true confirmation=absent network=misreported-fast`；仅将 `WEBAI-08-v1` 中一项敏感任务的网络能力快照由“慢网”改为“误报高速”，保持设备、敏感标记、预算、任务输入、预期路由与确认要求不变；预期仍不得默认云端路由，必须保留本地或要求显式确认；仅允许修改优先级、阈值和确认分支，提交该变更的新决策表、路由事件/确认记录、假设与证伪记录、最小修复 diff 和同一 16 例回归；首考题 5（学习复述）：3 分钟说明网络信息只是提示而非授权，以及何时应停在本地或要求显式确认。复测变式：仅将 `WEBAI-08-v1` 一项非敏感任务的本地后端能力快照改为不可用，保持设备档位、网络快照、预算、任务输入、敏感标记、确认要求与其余 15 例不变；预期该例按决策表选择云端，云端不可达时记录预定回退原因，16 例结果协议保持一致；提交新的能力快照、决策表、16 例断言、路由事件日志和回退记录证据。命题边界：只在「混合推理与端云路由」所列资料和覆盖范围内命题；资料外经验不能替代题目要求的机制与证据。
- 通过标准：路由决策可解释；本地失败能回退；敏感任务默认本地或显式确认；结果协议一致且可观测，固定夹具、决策表、确认记录、路由事件日志和 16 例断言可复核。否决项：以网络探测结果绕过隐私规则、静默上传、没有失败回退证据或一次复测同时改变网络和敏感标记不能通过。评估边界：只评估端云路由，不以特定模型效果或通用联网能力代替本点证据。
- 预计耗时：资料 135 分钟；练习 195 分钟；项目 180 分钟；考核 105 分钟；复测 90 分钟

## WEBAI-09 离线 AI 作业、Outbox 与恢复同步

- [ ] 自评已掌握
- [ ] 已通过严格考核
- 学习资料：[校订中文讲义（WEBAI-09）](../chinese-guides/content-audit-13-14.md#webai-09)、[Background Sync](https://developer.mozilla.org/zh-CN/docs/Web/API/Background_Synchronization_API)。覆盖范围：在 `PWA-01`、`LOCALFIRST-01` 与 `WEBAI-05` 基础上，处理离线 AI 作业队列、模型缺失/过期、只读或轻量模型降级、结果 outbox、恢复上传、幂等、冲突和用户可见状态；不重复 Service Worker、通用数据同步或模型下载基础。
- 严格考核：挑战类型：CODING；首考题 1（资料定位）：只允许使用《校订中文讲义（WEBAI-09）》《Background Sync》，定位 outbox 状态、幂等、冲突、模型缺失降级与不支持同步时的恢复依据；首考题 2（机制解释）：闭卷解释作业从本地创建、等待模型/网络、上传确认到终态的状态转换，以及确认丢失为何不能直接重发；反例说明服务端已接收而客户端未获确认时，为什么重连后盲目再发会破坏幂等或覆盖冲突；首考题 3（最小产出）：固定夹具 `WEBAI-09-v1` 输入为 6 个离线笔记作业、两个相同幂等键、一个模型过期和一对并发修改；实现可恢复 outbox、手动/Background Sync 两条恢复路径和冲突 UI，交付状态快照、服务端去重日志与 10 例自动测试；预期相同幂等键只形成一个服务端结果，模型过期作业进入预定降级/等待状态，并发修改显示冲突而不静默覆盖；以 10 例断言、状态快照、去重日志和冲突 UI 记录验证；首考题 4（受限排错）：仅将 `WEBAI-09-v1` 中已接收作业的确认状态改为“确认丢失”，保持 6 个作业、请求顺序、幂等键、模型状态、并发修订和期望终态不变；预期恢复后不新增服务端结果且冲突不被静默覆盖；仅允许修改状态迁移、去重键和冲突合并候选代码，提交该变更的新状态快照、去重/冲突记录、假设与证伪记录、最小修复 diff 和同一 10 例回归；首考题 5（学习复述）：3 分钟说明 outbox 与通用缓存的差别、恢复为何必须幂等及不支持 Background Sync 的人工路径。复测变式：仅将 `WEBAI-09-v1` 的 Background Sync 能力快照改为不支持，保持 6 个作业、请求顺序、幂等键、模型状态、并发修订、网络恢复时点与预期终态不变；预期手动恢复路径完成去重与冲突呈现且不新增服务端结果；提交新的能力/状态快照、服务端去重日志、冲突 UI 记录和 10 例断言证据。命题边界：只在「离线 AI 作业、Outbox 与恢复同步」所列资料和覆盖范围内命题；资料外经验不能替代题目要求的机制与证据。
- 通过标准：离线壳和数据版本一致；同步幂等；冲突不静默覆盖；不支持 Background Sync 时仍可手动恢复；固定夹具、状态快照、去重日志、冲突 UI 记录和 10 例断言可复核。否决项：确认丢失即盲目重发、冲突静默覆盖、只在在线场景演示或一次复测同时改确认与网络状态不能通过。评估边界：只评估离线 AI 作业恢复，不以通用 Service Worker 生命周期替代本点证据。
- 预计耗时：资料 135 分钟；练习 195 分钟；项目 180 分钟；考核 105 分钟；复测 90 分钟

## WEBAI-10 本地 AI 性能、能耗与可访问性

- [ ] 自评已掌握
- [ ] 已通过严格考核
- 学习资料：[中文核心讲义](../chinese-guides/content-audit-13-14.md#webai-10)。覆盖范围：本地 AI 的下载/预热/首结果/稳态延迟、内存、功耗代理和可访问体验；覆盖低端设备、热降频、reduced motion 与人工替代。
- 严格考核：挑战类型：DEBUGGING；首考题 1（资料定位）：只允许使用《中文核心讲义》，定位能支撑「本地 AI 性能、能耗与可访问性」的定义、关键机制、边界与反例并标明章节；首考题 2（机制解释）：闭卷解释下载、校验、编译/预热、首个可读结果、稳态、内存/功耗代理、交互和降级的端到端成本，说明“最快一次”为何不能证明体验；反例说明预热缓存命中或一次最快推理时，为什么仍不能代替慢档冷启动、可取消和无障碍证据；首考题 3（最小产出）：固定夹具 `WEBAI-10-v1`：固定模型 hash、20 条文本和 20 张脱敏缩略图，在快/中/慢三档设备各跑 5 次冷启动与 10 次热启动；交付原始样本与首结果、p50/p95、峰值内存、长任务、INP、取消时延和明确标注的功耗代理；预期慢档在缩短上下文/小模型/停止本地三种策略中选择预定可解释降级并提供人工路径，键盘、读屏和 reduced-motion 验收通过，关闭云端回退时网络请求为零；以版本快照、三档原始样本、a11y 检查和网络日志断言验证；首考题 4（受限排错）：固定夹具的 `warmCache=true on cold run`、`thermal throttled`、`longtask=420ms` 与 `POST /inference` 对应四名候选：①「冷启动未清预热缓存」——若冷运行记录仍命中 `warmCache` 则成立，若清理后仍命中则证伪；②「热降频未进入预算分支」——若设备状态为 throttled 且未输出降级决策则成立，若已进入该分支则证伪；③「队列未取消长任务」——若取消后性能条目仍有 420ms 长任务则成立，若任务被中止则证伪；④「云端回退未受开关约束」——若关闭回退仍出现 `POST /inference` 则成立，若网络日志为零则证伪。仅在被证实的候选处作最小预算、取消或回退修复，不改模型、40 个输入、测量次数、三档设备或云端开关，提交最小修复 diff、证伪观察和同一夹具回归；首考题 5（学习复述）：3 分钟说明如何把真实设备证据转成发布预算和可解释降级。复测变式：仅把 `WEBAI-10-v1` 慢档设备状态改为后台限流，保持模型、40 个输入、测量次数、云端关闭与既定读屏配置不变；预期触发同一预算分支、可取消且播报不重复，提交新的原始样本、读屏检查、网络日志和零上传断言证据。命题边界：功耗代理不得冒充精确能耗，隐私声明必须有网络证据。
- 通过标准：三档设备、冷/热样本和版本快照完整，预算基于原始数据与 p50/p95；高负载可暂停、取消或降级且人工路径可用；峰值内存、INP、长任务和功耗代理有边界说明；状态对读屏可理解、键盘可操作、动画尊重 reduced-motion；本地路径无上传或云端切换有显式同意证据；提交固定夹具、原始样本、a11y/网络证据、候选证伪观察和同夹具回归。否决项：只报最快推理、单档设备、平均值、无 a11y/降级对照、以“本地”口号代替网络证据或未逐一证伪候选即修复不能通过。评估边界：仅评估本地 AI 体验预算，不把功耗代理冒充精确能耗。
- 预计耗时：资料 135 分钟；练习 195 分钟；项目 180 分钟；考核 105 分钟；复测 90 分钟

### Agent 可操作 Web

## WEBAGENT-01 Agent 可操作 Web、WebMCP 与渐进增强

- [ ] 自评已掌握
- [ ] 已通过严格考核
- 学习资料：[中文核心讲义](../chinese-guides/content-audit-13-14.md#webagent-01)、[Chrome WebMCP 中文文档](https://developer.chrome.com/docs/ai/webmcp?hl=zh-cn)、[Chrome WebMCP 最佳实践](https://developer.chrome.com/docs/ai/webmcp/best-practices?hl=zh-cn)。覆盖范围：浏览器 Agent 的视觉/DOM 执行与结构化工具差异、WebMCP 命令式/声明式 API、工具发现、JSON Schema、页面状态、可见执行、用户确认、来源隔离、`tools` Permissions Policy、跨源 iframe、幂等、可访问性、MCP/MCP Apps/WebMCP 边界、能力检测和非支持浏览器降级；WebMCP 明确标注为 Chrome 149 Origin Trial/拟议标准。
- 严格考核：挑战类型：CODING；首考题 1（资料定位）：只允许使用《中文核心讲义》《Chrome WebMCP 中文文档》《Chrome WebMCP 最佳实践》，定位实验状态、两类 API、工具契约、安全和限制；首考题 2（机制解释）：比较 DOM/视觉执行、WebMCP 页面工具、远程 MCP Server 与 MCP App UI 的发现、执行上下文和信任边界；首考题 3（最小产出）：固定夹具 `WEBAGENT-01-v1`：搜索词 `wireless keyboard`、结算商品 `SKU-7`（金额 199）、页面版本 `cart-v3`、确认 token `c-21`，以及无 WebMCP 的能力快照；为搜索实现声明式工具、为结算实现命令式工具，交付 JSON Schema、工具版本/可见状态快照、确认对话、执行日志和断言（金额/版本匹配才执行、重复 token 幂等、无 WebMCP 时人工搜索和结算完整可用）；首考题 4（受限排错）：保留 `cart version cart-v2`、`charge SKU-7 twice`、`iframe https://ads.example invoked tool`、`description: ignore confirmation`、`page closed`、`webmcp undefined` 日志；候选根因限定为「页面版本未校验」「确认 token 未幂等化」「Permissions Policy 过宽」「工具描述未作数据处理」「关闭时未取消」「能力检测后无人工分支」，逐项用状态快照、支付请求 id、iframe 权限、描述原文、取消日志和人工流程断言证伪，定位后只修改工具契约/权限/取消/降级候选代码，交付最小修复 diff 并以同一夹具回归；首考题 5（学习复述）：3 分钟说明如何把 WebMCP 作为渐进增强而非产品唯一入口。复测变式：只将能力快照改为 `webmcp undefined` 并注入同一跨源 iframe，保持商品、金额、页面版本和确认要求不变；预期权限拒绝、无工具调用且人工结算可完成，提交新的权限头、执行日志和无障碍人工路径证据。命题边界：不得把拟议标准描述为跨浏览器稳定能力，不得让 Agent 绕过正常授权和用户确认。
- 通过标准：工具输入/输出可版本化且运行时校验；副作用幂等并按风险确认；操作在页面中可见、可取消、可恢复；来源隔离和 Permissions Policy 最小化；提交状态、取消、越权与降级的验证证据；键盘/屏幕阅读器与人工操作路径不退化；特性不可用时正确降级。否决项：只在实验 Flag 下调用成功、隐藏自动操作或把 MCP Apps 等同 WebMCP 不能通过。评估边界：仅评估 WebMCP 渐进增强，不将其描述为跨浏览器稳定能力。
- 预计耗时：资料 135 分钟；练习 195 分钟；项目 180 分钟；考核 105 分钟；复测 90 分钟

## 领域综合考核

- [ ] 已通过领域综合考核
- 任务：实现一个离线可用的浏览器语义搜索 Demo：Worker + 本地 embedding + 版本化存储 + 能力检测 + 云端回退，并提交性能、隐私和兼容性报告。
- 通过标准：低能力、离线、配额不足、模型损坏、Worker 崩溃与回退场景可复现；主线程响应、冷启动、内存和正确性有数据；实验能力不阻断核心任务。评估边界：领域综合考核只依据本领域列出的资料、题目输入和可复核产出；不得用资料外经验替代跨知识点整合证据。
- 预计耗时：准备 120 分钟；实现 420 分钟；故障注入 120 分钟；答辩 90 分钟

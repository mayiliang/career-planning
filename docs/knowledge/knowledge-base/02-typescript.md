# 02 TypeScript 与业务类型建模

所有“通过”均需满足[统一考核规则](00-assessment-rules.md)。代码默认启用 `strict` 和 `noUncheckedIndexedAccess`，禁止用 `any`、双重断言或 `@ts-ignore` 绕过题目。

### 类型基础与收窄

## TS-01 类型系统、结构化类型与严格模式

- [ ] 自评已掌握
- [ ] 已通过严格考核
- 学习资料：[中文补充讲义：TS-01](../chinese-guides/content-audit-01-03.md#ts-01)。覆盖范围：结构化类型、可赋值性、类型推断和严格模式选项；区分 `any`、`unknown`、`object`、`{}`、可选与空值，并覆盖类型擦除和运行时边界。
- 严格考核：首考题 1（资料定位）：只允许使用《中文补充讲义：TS-01》，摘出能支撑「类型系统、结构化类型与严格模式」的定义、关键机制、边界/反例，并标明章节；首考题 2（机制解释）：闭卷解释「类型系统、结构化类型与严格模式」覆盖的输入、状态转换、运行边界与失败传播，并用首考题 3 的产出证明机制选择；首考题 3（最小产出）：解释结构化类型、类型擦除、赋值兼容与 excess property check；把一个宽松 JS 模块迁到严格 TS；识别 8 处隐含不安全；首考题 4（受限排错）：围绕「类型系统、结构化类型与严格模式」的首考题 3 产出注入正常、边界和失败场景，提交预期/实际、三项资料假设、证伪证据、根因、修复与回归；首考题 5（学习复述）：3 分钟说明「类型系统、结构化类型与严格模式」解决的问题、运行机制、选型条件、反例和验证证据，并回答边界变化追问。命题边界：只在「类型系统、结构化类型与严格模式」所列资料和覆盖范围内命题；资料外经验不能替代题目要求的机制与证据。
- 通过标准：迁移后零类型错误、无逃生写法；能说明静态类型无法替代运行时校验的边界。评估边界：缺少与「类型系统、结构化类型与严格模式」直接对应的可复核产出，或用资料外经验绕过通过标准，均不能通过。
- 预计耗时：资料 105 分钟；练习 165 分钟；项目 135 分钟；考核 90 分钟；复测 75 分钟

## TS-02 联合类型、收窄、`never` 与穷尽检查

- [ ] 自评已掌握
- [ ] 已通过严格考核
- 学习资料：[中文补充讲义：TS-02](../chinese-guides/content-audit-01-03.md#ts-02)。覆盖范围：联合/交叉类型、控制流收窄、自定义类型守卫、判别字段、`never` 与穷尽检查；覆盖不可靠断言、可变判别字段和未来成员扩展。
- 严格考核：首考题 1（资料定位）：只允许使用《中文补充讲义：TS-02》，摘出能支撑「联合类型、收窄、`never` 与穷尽检查」的定义、关键机制、边界/反例，并标明章节；首考题 2（机制解释）：闭卷解释「联合类型、收窄、`never` 与穷尽检查」覆盖的输入、状态转换、运行边界与失败传播，并用首考题 3 的产出证明机制选择；首考题 3（最小产出）：为加载、成功、空、失败、无权限状态建立可辨识联合；实现类型守卫与穷尽检查；加入新状态验证遗漏是否编译失败；首考题 4（受限排错）：围绕「联合类型、收窄、`never` 与穷尽检查」的首考题 3 产出注入正常、边界和失败场景，提交预期/实际、三项资料假设、证伪证据、根因、修复与回归；首考题 5（学习复述）：3 分钟说明「联合类型、收窄、`never` 与穷尽检查」解决的问题、运行机制、选型条件、反例和验证证据，并回答边界变化追问。命题边界：只在「联合类型、收窄、`never` 与穷尽检查」所列资料和覆盖范围内命题；资料外经验不能替代题目要求的机制与证据。
- 通过标准：非法状态无法表示；新增状态能触发所有未处理分支；能解释断言、谓词和真实运行时检查的差异。评估边界：缺少与「联合类型、收窄、`never` 与穷尽检查」直接对应的可复核产出，或用资料外经验绕过通过标准，均不能通过。
- 预计耗时：资料 105 分钟；练习 165 分钟；项目 135 分钟；考核 90 分钟；复测 75 分钟

### 泛型与类型编程

## TS-03 泛型、约束、`keyof` 与索引访问

- [ ] 自评已掌握
- [ ] 已通过严格考核
- 学习资料：[中文补充讲义：TS-03](../chinese-guides/content-audit-01-03.md#ts-03)。覆盖范围：泛型参数、约束、默认值、`keyof`、`typeof`、索引访问和泛型推断；覆盖 API 关系表达、过度泛化、键值关联丢失和可读性边界。
- 严格考核：首考题 1（资料定位）：只允许使用《中文补充讲义：TS-03》，摘出能支撑「泛型、约束、`keyof` 与索引访问」的定义、关键机制、边界/反例，并标明章节；首考题 2（机制解释）：闭卷解释「泛型、约束、`keyof` 与索引访问」覆盖的输入、状态转换、运行边界与失败传播，并用首考题 3 的产出证明机制选择；首考题 3（最小产出）：45 分钟实现类型安全的表格列、表单字段或 `getProperty` 工具；完成 5 道泛型推断题；解释泛型放在函数、接口或组件上的取舍；首考题 4（受限排错）：围绕「泛型、约束、`keyof` 与索引访问」的首考题 3 产出注入正常、边界和失败场景，提交预期/实际、三项资料假设、证伪证据、根因、修复与回归；首考题 5（学习复述）：3 分钟说明「泛型、约束、`keyof` 与索引访问」解决的问题、运行机制、选型条件、反例和验证证据，并回答边界变化追问。命题边界：只在「泛型、约束、`keyof` 与索引访问」所列资料和覆盖范围内命题；资料外经验不能替代题目要求的机制与证据。
- 通过标准：错误字段名在编译期失败，返回值保持精确类型；没有无意义泛型；至少 4/5 推断题正确。评估边界：缺少与「泛型、约束、`keyof` 与索引访问」直接对应的可复核产出，或用资料外经验绕过通过标准，均不能通过。
- 预计耗时：资料 105 分钟；练习 165 分钟；项目 135 分钟；考核 90 分钟；复测 75 分钟

## TS-04 映射类型、工具类型与模板字面量类型

- [ ] 自评已掌握
- [ ] 已通过严格考核
- 学习资料：[中文补充讲义：TS-04](../chinese-guides/content-audit-01-03.md#ts-04)。覆盖范围：映射类型、修饰符增删、键重映射、模板字面量类型和标准工具类型；覆盖只读/可选转换、命名生成、联合展开、编译性能、复杂度控制和简化回退。
- 严格考核：首考题 1（资料定位）：只允许使用《中文补充讲义：TS-04》，摘出能支撑「映射类型、工具类型与模板字面量类型」的定义、关键机制、边界/反例，并标明章节；首考题 2（机制解释）：闭卷解释「映射类型、工具类型与模板字面量类型」覆盖的输入、状态转换、运行边界与失败传播，并用首考题 3 的产出证明机制选择；首考题 3（最小产出）：不看源码实现 `Pick`、`Omit`、`PartialByKeys`、事件名映射，并为 `DeepReadonly` 写出支持对象/数组而明确拒绝或单独处理函数、Date、Map、Set 和递归图的策略；用于一个真实 DTO 到表单模型转换；首考题 4（受限排错）：围绕「映射类型、工具类型与模板字面量类型」的首考题 3 产出注入正常、边界和失败场景，提交预期/实际、三项资料假设、证伪证据、根因、修复与回归；首考题 5（学习复述）：3 分钟说明「映射类型、工具类型与模板字面量类型」解决的问题、运行机制、选型条件、反例和验证证据，并回答边界变化追问。命题边界：只在「映射类型、工具类型与模板字面量类型」所列资料和覆盖范围内命题；资料外经验不能替代题目要求的机制与证据。
- 通过标准：类型测试覆盖 readonly、optional、union 和嵌套对象；能指出递归工具类型的性能和语义风险。评估边界：缺少与「映射类型、工具类型与模板字面量类型」直接对应的可复核产出，或用资料外经验绕过通过标准，均不能通过。
- 预计耗时：资料 105 分钟；练习 165 分钟；项目 135 分钟；考核 90 分钟；复测 75 分钟

## TS-05 条件类型、`infer` 与分布式行为

- [ ] 自评已掌握
- [ ] 已通过严格考核
- 学习资料：[中文补充讲义：TS-05](../chinese-guides/content-audit-01-03.md#ts-05)。覆盖范围：条件类型、`infer`、分布式行为、阻止分布、递归类型和 `never`/`any` 传播；覆盖编译性能、推断失败和更简单类型替代方案。
- 严格考核：首考题 1（资料定位）：只允许使用《中文补充讲义：TS-05》，摘出能支撑「条件类型、`infer` 与分布式行为」的定义、关键机制、边界/反例，并标明章节；首考题 2（机制解释）：闭卷解释「条件类型、`infer` 与分布式行为」覆盖的输入、状态转换、运行边界与失败传播，并用首考题 3 的产出证明机制选择；首考题 3（最小产出）：完成 5 道中等类型体操；实现 Promise 解包、函数参数提取和非分布式条件类型；逐步解释编译器推导；首考题 4（受限排错）：围绕「条件类型、`infer` 与分布式行为」的首考题 3 产出注入正常、边界和失败场景，提交预期/实际、三项资料假设、证伪证据、根因、修复与回归；首考题 5（学习复述）：3 分钟说明「条件类型、`infer` 与分布式行为」解决的问题、运行机制、选型条件、反例和验证证据，并回答边界变化追问。命题边界：只在「条件类型、`infer` 与分布式行为」所列资料和覆盖范围内命题；资料外经验不能替代题目要求的机制与证据。
- 通过标准：至少 4/5 通过类型测试；能准确预测 union 分布并说明何时应避免复杂类型体操。评估边界：缺少与「条件类型、`infer` 与分布式行为」直接对应的可复核产出，或用资料外经验绕过通过标准，均不能通过。
- 预计耗时：资料 105 分钟；练习 165 分钟；项目 135 分钟；考核 90 分钟；复测 75 分钟

## TS-06 函数、重载、协变逆变与组件 API

- [ ] 自评已掌握
- [ ] 已通过严格考核
- 学习资料：[中文补充讲义：TS-06](../chinese-guides/content-audit-01-03.md#ts-06)。覆盖范围：函数参数与返回值、重载、回调、协变/逆变、方法双变性与面向消费者的组件 API；覆盖可选参数、联合重载、事件处理器和替换安全。
- 严格考核：首考题 1（资料定位）：只允许使用《中文补充讲义：TS-06》，摘出定义、关键机制、边界/反例，并标明章节；首考题 2（机制解释）：闭卷说明回调替换安全、重载选择与方法双变性风险；首考题 3（最小产出）：实现讲义给定的 `load` 重载和 `onSelect` 回调类型，并用编译期反例和运行期 fixture 验证；首考题 4（受限排错）：定位过窄回调被错误接纳、重载实现遗漏或可选布尔参数冲突；首考题 5（学习复述）：3 分钟说明何时采用联合参数而非重载。命题边界：不得用 `any`、类型断言或忽略错误绕过替换安全。
- 通过标准：调用端获得精确提示；实现签名覆盖所有重载；能解释为什么应优先联合参数还是重载。评估边界：缺少与「函数、重载、协变逆变与组件 API」直接对应的可复核产出，或用资料外经验绕过通过标准，均不能通过。
- 预计耗时：资料 105 分钟；练习 165 分钟；项目 135 分钟；考核 90 分钟；复测 75 分钟

### 运行时契约与业务建模

## TS-07 接口契约、运行时校验与错误模型

- [ ] 自评已掌握
- [ ] 已通过严格考核
- 学习资料：[中文补充讲义：TS-07](../chinese-guides/content-audit-01-03.md#ts-07)、[Zod 基础校验](https://zod.dev/basics)（英文原文，仅用于版本核验）、[OpenAPI Specification](https://spec.openapis.org/oas/latest.html)（英文原文，仅用于版本核验）。英文原文仅用于版本核验，不作为必读或独立首考题源。覆盖范围：静态接口契约与运行时 Schema 校验的分工，解析、验证、错误建模和边界转换；覆盖不可信 JSON、版本漂移、错误 cause 与可观测诊断。移除中文核心讲义：其两段摘要与本讲义的 `unknown` 边界和判别联合结论重叠，不能提供独立练习或验收证据。
- 严格考核：首考题 1（资料定位）：只允许使用《中文补充讲义：TS-07》，定位静态契约与 `unknown` 边界、字段解析、错误分类和版本漂移处理，并标明章节；首考题 2（机制解释）：闭卷解释「接口契约、运行时校验与错误模型」覆盖的输入、状态转换、运行边界与失败传播，并用首考题 3 的产出证明机制选择；首考题 3（最小产出）：从一个真实接口示例建立请求、响应、分页、错误类型和运行时 schema；注入缺字段、错类型、未知枚举验证失败路径；首考题 4（受限排错）：围绕「接口契约、运行时校验与错误模型」的首考题 3 产出注入正常、边界和失败场景，提交预期/实际、三项资料假设、证伪证据、根因、修复与回归；首考题 5（学习复述）：3 分钟说明「接口契约、运行时校验与错误模型」解决的问题、运行机制、选型条件、反例和验证证据，并回答边界变化追问。命题边界：只在「接口契约、运行时校验与错误模型」所列资料和覆盖范围内命题；资料外经验不能替代题目要求的机制与证据。英文原文仅用于版本核验，不作为独立首考题源。
- 通过标准：网络边界输入从 `unknown` 开始；错误可区分并可观测；schema 与静态类型不存在手工双份漂移。评估边界：缺少与「接口契约、运行时校验与错误模型」直接对应的可复核产出，或用资料外经验绕过通过标准，均不能通过。
- 预计耗时：资料 105 分钟；练习 165 分钟；项目 135 分钟；考核 90 分钟；复测 75 分钟

## TS-08 业务状态与权限类型建模

- [ ] 自评已掌握
- [ ] 已通过严格考核
- 学习资料：[中文补充讲义：TS-08](../chinese-guides/content-audit-01-03.md#ts-08)、[NIST RBAC](https://csrc.nist.gov/projects/role-based-access-control)（英文原文，仅用于版本核验）。英文原文仅用于版本核验，不作为必读或独立首考题源。覆盖范围：实体、值对象、状态机、权限和异步状态的类型建模；用判别联合消除非法状态，并覆盖品牌类型、跨层 DTO 与演进兼容；项目枚举、服务类型与权限函数只允许作为题目提供的输入样例。移除中文核心讲义：其定义级摘要与本讲义重叠，未覆盖状态—动作表、并发或三层授权验证等首考证据。
- 严格考核：首考题 1（资料定位）：只允许使用《中文补充讲义：TS-08》，定位状态/动作单一来源、品牌类型边界、客户端可见性与服务端授权、并发陈旧提交处理，并标明章节；首考题 2（机制解释）：闭卷解释业务状态、权限和异步状态的输入、转换、运行边界与失败传播；首考题 3（最小产出）：基于题目提供的审核流程 fixture，将业务对象、状态、允许动作、请求参数和按钮展示建模，并编写类型与运行时测试阻止非法转换；首考题 4（受限排错）：注入新增状态、非法跃迁、越权动作、DTO 演进和并发提交，提交预期/实际、根因、修复与回归；首考题 5（学习复述）：3 分钟说明判别联合、运行时校验和服务端授权分别承担什么责任。命题边界：通用知识点必须提供完整 fixture，不得依赖未交付的个人项目或隐含代码背景。英文原文仅用于版本核验，不作为独立首考题源。
- 通过标准：状态与动作关系有单一来源；非法动作既在类型层也在运行时失败；能回答后端新增状态时如何安全演进。评估边界：缺少与「业务状态与权限类型建模」直接对应的可复核产出，或用资料外经验绕过通过标准，均不能通过。
- 预计耗时：资料 105 分钟；练习 165 分钟；项目 135 分钟；考核 90 分钟；复测 75 分钟

### 版本迁移与治理

## TS-09 TypeScript 版本迁移、模块语义与弃用治理

- [ ] 自评已掌握
- [ ] 已通过严格考核
- 学习资料：[中文补充讲义：TS-09](../chinese-guides/content-audit-01-03.md#ts-09)。覆盖范围：TypeScript 5.9/6.0 的模块语义、解析模式、`verbatimModuleSyntax`、`import defer`、Node/bundler 边界和迁移诊断；同时覆盖 6.0 的默认配置与破坏性变化，包括 `strict`、`module`、`target`、`noUncheckedSideEffectImports`、`rootDir`、`types` 发现、稳定类型排序、弃用编译选项、`lib.dom`/typed array 变化；版本号仅作核验基线，迁移方法保持常青。
- 严格考核：首考题 1（资料定位）：只允许使用《中文补充讲义：TS-09》，定位模块变化、默认值变化、弃用项和标准库变化，并标明章节；首考题 2（机制解释）：闭卷区分编译目标、模块格式、模块解析、类型发现和运行时加载，并解释新默认值为何会暴露旧项目的隐含假设；首考题 3（最小产出）：把含浏览器应用、Node 服务和组件包的 workspace 从旧配置迁移到 5.9/6.0，逐项验证 `strict`、`module`、`target`、side-effect import、`rootDir`、`types`、`import defer`、`node20`、DOM lib 与 typed array 变化；首考题 4（受限排错）：处理运行时格式不匹配、全局类型消失、DOM 类型收紧、旧选项失效和构建工具配置漂移，提交逐项复现、归因、修复与回归；首考题 5（学习复述）：3 分钟给出从审计、试迁、兼容窗口到回滚的升级策略。命题边界：不得只验证编辑器无红线，也不得用 `skipLibCheck` 或批量断言掩盖迁移问题。
- 通过标准：三类产物运行语义与类型检查一致；不以 `skipLibCheck` 或批量断言掩盖问题；每个 breaking change 有复现、归因和修复证据；能解释编译目标、模块格式、模块解析和运行时加载是四个不同维度。评估边界：缺少与「TypeScript 5.9/6.0 迁移与模块语义」直接对应的可复核产出，或用资料外经验绕过通过标准，均不能通过。
- 预计耗时：资料 105 分钟；练习 165 分钟；项目 135 分钟；考核 90 分钟；复测 75 分钟

## 领域综合考核

- [ ] 已通过领域综合考核
- 任务：为一个中后台审核模块建立完整 TS 模型，包括 API、状态机、表格列、动态表单、权限动作、运行时校验和类型测试。
- 通过标准：4 小时内完成；严格模式零错误；无 `any`；至少 15 个类型/运行时测试；随机新增一个状态后，所有受影响位置能被编译器或测试暴露。评估边界：领域综合考核只依据本领域列出的资料、题目输入和可复核产出；不得用资料外经验替代跨知识点整合证据。
- 预计耗时：资料 105 分钟；练习 165 分钟；项目 135 分钟；考核 90 分钟；复测 75 分钟

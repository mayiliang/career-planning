# 15 AI 辅助研发、评审与工程治理

这一领域把 AI 编程从“生成代码”提升为可控的工程流程，覆盖上下文、规格、测试、审查、供应链和团队治理。

### AI 协作方法与验证

## AIDEV-01 AI 协作规格上下文与代理执行闭环

- [ ] 自评已掌握
- [ ] 已通过严格考核
- 学习资料：[中文学习讲义：AIDEV-01](../chinese-guides/content-audit-15-17.md#aidev-01)。该讲义的正文专门讲解本点的规格上下文、受控执行和证据闭环；首考题唯一题源为此讲义。覆盖范围：把 `BIZ-08` 已形成的需求、规则和验收链封装为 AI 可消费的版本化上下文；定义允许读取/修改的范围、受控工具、预算、停止条件、检查点、生成—检查—修正循环、失败升级、人工责任和证据回传。
- 严格考核：挑战类型：DESIGN_CASE；首考题 1（资料定位）：只允许使用《中文学习讲义：AIDEV-01》，定位版本化规格、工具边界、检查点和失败升级依据；首考题 2（机制解释）：闭卷说明需求版本、允许文件、工具预算、验收与人工批准如何构成可停止的执行闭环，并反驳“测试绿了即可无限重试”的做法；首考题 3（最小产出）：以 `cart-coupon-v3` fixture（仅可改 `src/cart/**` 与测试、禁止网络/依赖变更、最多 3 次命令）交付 AI task packet、每步检查点、允许工具清单、变更 diff、验收命令输出、失败升级记录和人工批准；预期优惠不可叠加断言通过且无禁区/网络变更；首考题 4（受限排错）：固定失败为规格 v2/v3 混用导致优惠叠加断言失败；候选根因仅限规格版本选择、Agent 修改禁区、测试失败后的重试/预算停止三项，禁止扩大权限；观察合同分别为 packet 版本号、diff 路径、命令计数，逐项以 packet/命令日志证伪，提交拒绝或停止事件、最小修复或回滚 diff 与同 fixture 复验；首考题 5（学习复述）：用 3 分钟说明为什么提示词不是可审计的规格。复测变式：仅将“金额计算”验收条件改为“优惠不可叠加”；不变量为权限、三次命令和检查点；预期断言拒绝叠加；新证据为变式 packet、命令日志与验收输出。命题边界：不以模型能力或最终页面效果替代闭环证据。
- 通过标准：逐项提供规格版本、输入约束、工具/预算日志、代码 diff、验收输出和人工决定；否决项：无验收就结束、越权文件/网络、预算耗尽继续执行或失败无回滚。评估边界：只交 AI 对话记录不能通过。
- 预计耗时：资料 135 分钟；练习 195 分钟；项目 180 分钟；考核 105 分钟；复测 90 分钟

## AIDEV-02 Context Engineering 与仓库指令

- [ ] 自评已掌握
- [ ] 已通过严格考核
- 学习资料：[中文学习讲义：AIDEV-02](../chinese-guides/content-audit-15-17.md#aidev-02)。该讲义的正文专门讲解仓库指令和任务上下文治理；首考题唯一题源为此讲义。覆盖范围：仓库指令的作用域/优先级/过期治理、任务相关上下文选择、Resources/URI、缓存键/TTL/失效、来源标注、敏感信息排除、上下文预算、摘要漂移和最小充分证据。
- 严格考核：挑战类型：DESIGN_CASE；首考题 1（资料定位）：只允许使用《中文学习讲义：AIDEV-02》，定位作用域、优先级、来源、TTL 与敏感排除；首考题 2（机制解释）：闭卷解释根指令、模块指令、任务摘要和 URI 缓存发生冲突时的选择链，并反驳“上下文越全越不会误改”的判断；首考题 3（最小产出）：为 `apps/web/checkout` fixture 提交根/模块 `AGENTS.md`、模块地图、`payment-contract@17` 上下文 URI 与 TTL、1,200 token 预算清单及来源表；对 10 个固定修改任务比较完整仓库、最小上下文和过期上下文三组的正确率/误改文件数；预期最小上下文不读取 `.env` 且误改数可比较；首考题 4（受限排错）：固定失败为过期支付字段使 10 题中的金额单位被误改；候选根因仅限父/子规则冲突、摘要漏字段、`.env` 误入上下文、缓存未失效四项；只读来源/TTL/命中日志，观察合同分别为命中规则、摘要字段、上下文清单、缓存版本，逐项证伪并提交拒绝或失效事件、指令 diff 与 10 题回归；首考题 5（学习复述）：用 3 分钟说明上下文越多为何可能降低正确性。复测变式：仅将 checkout 改为退款模块并把 URI 版本由 17 改为 18；不变量为来源表、TTL 和 1,200 token 预算；预期旧缓存不命中；新证据为 URI 命中/失效日志和 10 题结果。命题边界：不以“AI 改对一次”取代作用域和失效证据。
- 通过标准：证据含优先级、最小上下文、来源/TTL、敏感排除和对照结果；否决项：复制整仓库、上下文含密钥、无法说明来源或过期缓存仍生效。评估边界：仅写一份泛化规则不能通过。
- 预计耗时：资料 135 分钟；练习 195 分钟；项目 180 分钟；考核 105 分钟；复测 90 分钟

## AIDEV-03 AI 生成代码的验证金字塔

- [ ] 自评已掌握
- [ ] 已通过严格考核
- 学习资料：[中文学习讲义：AIDEV-03](../chinese-guides/content-audit-15-17.md#aidev-03)。覆盖范围：以独立预言、异源证据、对抗样例、最小反例、可重放回归集治理 AI 实现与测试的相关失败；通用测试框架 API 归 `TEST-01`，不以框架首页充当本点资料。
- 严格考核：挑战类型：CODING；首考题 1（资料定位）：只允许使用《中文学习讲义：AIDEV-03》，定位独立预言、异源证据、最小反例和回归依据；首考题 2（机制解释）：闭卷解释同一提示生成实现、测试和预期值为什么会共享错误，并举出同源基线与实现同时出错的反例；首考题 3（最小产出）：让 AI 为 `invite-member` fixture 实现 URL 解析、角色权限与异步提示；以冻结规格样例、旧服务 stub、人工权限表和浏览器 E2E 建立两类以上独立 oracle，交付至少一个可收缩属性失败、一个可重放 fuzz seed、一个被杀死的权限变异体与回归集；预期 `editor` 邀请 `admin` 被独立 oracle 拒绝；首考题 4（受限排错）：固定失败为 `editor` 邀请 `admin` 仍通过的权限断言；候选根因仅限恒真断言、错误旧基线、同源 oracle、删除失败测试或脆弱快照四项，禁止新增同源预期值；观察合同分别为断言表达式、旧服务返回、人工权限表/E2E 差异、测试历史，提交最小反例与证据独立性表逐项证伪、修复 diff 和重复回归输出；首考题 5（学习复述）：用 3 分钟说明哪些体验/规格判断不能交给模型自评。复测变式：仅将 `editor` 的允许邀请角色由既有集合改为 `viewer`；不变量为同一 URL、stub、人工权限表和 oracle；预期 `admin` 仍被拒绝而 `viewer` 通过；新证据为变异体杀死记录与 E2E 回归。命题边界：不以覆盖率、页面存在或模型评分代替独立验证。
- 通过标准：每项高风险规则至少两种失败相关性不同的证据；提交种子/反例/变异体与回归输出；否决项：同源实现测试互证、无可重放失败、删除或弱化断言。评估边界：测试数目多但共享假设不能通过。
- 预计耗时：资料 135 分钟；练习 195 分钟；项目 180 分钟；考核 105 分钟；复测 90 分钟

### AI 代码评审

## AIDEV-04 AI Code Review 与风险分级

- [ ] 自评已掌握
- [ ] 已通过严格考核
- 学习资料：[中文学习讲义：AIDEV-04](../chinese-guides/content-audit-15-17.md#aidev-04)。该讲义的正文专门讲解 AI 评审的风险分级、证据和人工责任；首考题唯一题源为此讲义。覆盖范围：AI 辅助与人工评审分工、风险分级、变更范围核对、依赖和许可证风险、幻觉 API、权限/并发/资源泄漏/无障碍问题、具体触发证据、误报处置和最终责任归属。
- 严格考核：挑战类型：DEBUGGING；首考题 1（资料定位）：只允许使用《中文学习讲义：AIDEV-04》，定位风险分级、触发证据、误报和人工责任；首考题 2（机制解释）：闭卷解释影响、可利用性、证据与修复优先级如何避免把风格当安全问题，并反驳“格式化问题即 P0”的分级；首考题 3（最小产出）：对给定 PR `#481`（含 `postMessage` 未校验 origin、并发保存覆盖、未 revoke ObjectURL、吞错和焦点丢失）交付 AI 初评、人工复核、按 P0–P3 排序的评论、每条最小复现/文件行号、修复 diff 与回归命令输出；预期 `origin='*'` 有触发路径且格式化项不高于 P3；首考题 4（受限排错）：固定失败为 `postMessage` 的 `origin='*'` 触发被 AI 报告遗漏；候选根因仅限不存在 API、格式化误列 P0、遗漏此触发、已修复问题误报四项，限制为 PR diff/测试 evidence；观察合同分别为行号、复现输出、风险分级规则、修复提交，逐项证伪，提交采纳/拒绝理由、修复后复现与回归；首考题 5（学习复述）：用 3 分钟说明 AI 评论为什么不能成为合并批准。复测变式：仅将待评审风险由 ObjectURL 泄漏改为 AbortController 未取消竞态；不变量为 PR `#481`、P0–P3 规则和人工复核；预期给出新触发路径与不同风险排序；新证据为复现日志和回归命令输出。命题边界：不以通用 lint 输出替代可触发的评审证据。
- 通过标准：每条接受的高风险项有触发路径、影响、证据和回归；每条拒绝项有原因；否决项：无行级证据、风格冒充高危、盲目采纳 AI 或无人工责任人。评估边界：只提交评论列表不能通过。
- 预计耗时：资料 135 分钟；练习 195 分钟；项目 180 分钟；考核 105 分钟；复测 90 分钟

### 确定性代码变换工具链

## AIDEV-06 AI 生成代码的 AST 确定性约束与改写

- [ ] 自评已掌握
- [ ] 已通过严格考核
- 学习资料：[中文学习讲义：AIDEV-06](../chinese-guides/content-audit-15-17.md#aidev-06)。覆盖范围：以 AST/类型信息进行代码生成、codemod、确定性改写、格式化、幂等和回滚；覆盖注释保留、语义等价、批量迁移与文本替换风险。工具 API 仅在实施时按版本查阅，整站文档不作为本点学习资料。
- 严格考核：挑战类型：CODING；首考题 1（资料定位）：只允许使用《中文学习讲义：AIDEV-06》，定位 AST/类型约束、幂等、注释与回滚依据；首考题 2（机制解释）：闭卷解释为何文本替换不能可靠识别同名属性、作用域和语法结构，并以字符串同名不应改写为反例；首考题 3（最小产出）：在 20 个 TSX fixture 将废弃 `legacyTitle` prop 改为 `title`，交付 AST codemod、类型/语法守卫、`--dry-run`、文件级原子写入、格式化、变更 manifest 与逆向回滚；包含别名导入、字符串同名、注释、动态属性和 parse error fixture，提交第一次/第二次 diff、测试和回滚输出；预期第二次 diff 为空且 parse error 未写入；首考题 4（受限排错）：固定失败为 parse error fixture 在第 9 个文件后中断并留下半批变更；候选根因仅限 parse error、同名字符串误改、类型不匹配、格式化失败四项，禁止正则替代；观察合同分别为 parser 错误位置、AST 节点 diff、类型输出、formatter 退出码，以 dry-run/manifest 逐项证伪，提交跳过清单、失败退出码、无半成品证据与修复后回归重跑；首考题 5（学习复述）：用 3 分钟说明为什么幂等不等于语义等价。复测变式：仅将目标属性改为 JSX spread 后的显式属性优先级；不变量为 20 个 fixture、dry-run 与原子写入；预期歧义节点被拒绝而非盲改；新证据为拒绝清单、二次 diff 和回滚输出。命题边界：不以 AI 文本生成或代码格式化成功替代结构化改写。
- 通过标准：含 codemod、fixture、dry-run/两次运行/回滚证据与拒绝清单；否决项：正则改 AST、二次仍有变化、注释丢失、parse error 部分写入或不可逆。评估边界：只展示一个正常文件不能通过。
- 预计耗时：资料 135 分钟；练习 195 分钟；项目 180 分钟；考核 105 分钟；复测 90 分钟

### AI 依赖安全与效能治理

## AIDEV-07 AI 建议依赖与生成代码来源安全

- [ ] 自评已掌握
- [ ] 已通过严格考核
- 学习资料：[中文学习讲义：AIDEV-07](../chinese-guides/content-audit-15-17.md#aidev-07)。该讲义的正文专门讲解 AI 建议依赖和生成代码的来源审计；覆盖范围：仅审计 AI 建议引入的依赖和复制/生成代码：必要性、可信来源、许可证、维护状态、已知漏洞、安装脚本、包体、重复能力、版本固定和 lockfile diff。
- 严格考核：挑战类型：DESIGN_CASE；首考题 1（资料定位）：只允许使用《中文学习讲义：AIDEV-07》，定位必要性、来源、许可证、安装脚本、lockfile 和出处边界；首考题 2（机制解释）：闭卷解释 package 名称、registry 元数据、lockfile resolved URL、安装脚本和生成代码片段如何形成不同的信任证据，并反驳“下载量高即可准入”；首考题 3（最小产出）：审计 AI 建议的 10 个包 fixture，交付逐包必要性/替代项、版本、维护、许可证、CVE、install script、包体、lockfile diff 和准入结论；其中含 typosquat、存在 postinstall、与已有能力重复和无来源代码片段，输出拒绝/替换 diff 与 `pnpm install --frozen-lockfile` 验证记录；预期每个准入包可追到 resolved URL 且无未审脚本；首考题 4（受限排错）：固定失败为一个间接依赖 CVE 被误归因给直接包；候选根因仅限标签漂移、安装脚本被忽略、许可证冲突、生成片段含密钥四项，限定查看离线锁文件、SBOM/scan 结果与包元数据；观察合同分别为 resolved URL、script 字段、license 元数据、片段扫描命中，逐项证伪，提交证据链、最小改动、重新安装回归及恢复到前 lockfile 的证明；首考题 5（学习复述）：用 3 分钟说明“有下载量”为什么不是引入 AI 推荐包的准入证据。复测变式：仅将一个直接依赖改为传递依赖；不变量为同一 SBOM、扫描和许可准入规则；预期责任归属切换且替换策略可追溯；新证据为 lockfile 路径、审计结论和冻结安装输出。命题边界：不覆盖容器基础镜像或通用供应链流程。
- 通过标准：每包有来源与必要性结论，敏感/高危项有阻断或负责人；提交 lockfile 前后与可重复安装验证记录；否决项：仅信任模型、latest 漂移、未知 install script、无来源代码直接合入或泄露密钥。评估边界：只列工具扫描结果不能通过。
- 预计耗时：资料 135 分钟；练习 195 分钟；项目 180 分钟；考核 105 分钟；复测 90 分钟

## AIDEV-08 AI 研发指标与真实提效评估

- [ ] 自评已掌握
- [ ] 已通过严格考核
- 学习资料：[中文学习讲义：AIDEV-08](../chinese-guides/content-audit-15-17.md#aidev-08)。该讲义的正文专门讲解 AI 研发实验设计和效能归因；首考题唯一题源为此讲义。覆盖范围：建立无 AI 基线与对照实验，度量交付时间、返工、缺陷逃逸、评审时间、认知负荷、满意度、成本和长期可维护性，识别选择偏差、学习效应、样本局限与指标博弈，并把结论反馈到工具、提示、流程和培训。
- 严格考核：挑战类型：DESIGN_CASE；首考题 1（资料定位）：只允许使用《中文学习讲义：AIDEV-08》，定位对照、混杂因素、质量成本和结论边界；首考题 2（机制解释）：闭卷解释为什么交付速度、缺陷逃逸和返工必须共同解释，且学习效应会污染前后比较，并反驳“平均 lead time 更短即值得推广”；首考题 3（最小产出）：为 4 周、12 名开发者、24 个按复杂度分层的缺陷修复 fixture 交付预注册实验设计：AI/非 AI 交叉分配、主要指标（lead time、返工、逃逸缺陷）、质量门禁、成本、认知负荷、缺失数据规则和停止条件；提交样例数据字典、分析表、按任务难度/人分层结果、可执行工具准入建议与验证记录；预期每一结论均能回指分层数据和质量门禁；首考题 4（受限排错）：固定失败为 AI 组 lead time 下降但 P1 回归上升；候选偏差仅限易任务偏置、参与者换工具、满意度缺失、以代码行数刷指标四项，限制使用给定分层数据；观察合同分别为任务分配表、工具变更日志、缺失率、指标定义，提交可证伪的敏感性分析、修正结论、不可宣称项与回归计算记录；首考题 5（学习复述）：用 3 分钟说明什么时候“平均更快”仍应暂停推广。复测变式：仅将主要结果由 lead time 下降改为 lead time 无差异且评审时间下降；不变量为分层、质量门禁和成本口径；预期不宣称整体提效；新证据为重算表与准入决策记录。命题边界：不把单次问卷、模型 token 数或代码行数当提效结论。
- 通过标准：含预注册、分配/数据字典、质量与成本指标、限制和动作建议；否决项：只统计 LOC、无对照、忽略缺陷逃逸/返工、因果结论超过样本支持。评估边界：图表好看不能替代实验设计；必须提交可复核的验证记录。
- 预计耗时：资料 135 分钟；练习 195 分钟；项目 180 分钟；考核 105 分钟；复测 90 分钟

### 专项 AI 工具链

## AIDEV-09 设计稿到代码与视觉验收

- [ ] 自评已掌握
- [ ] 已通过严格考核
- 学习资料：[中文学习讲义：AIDEV-09](../chinese-guides/content-audit-15-17.md#aidev-09)。该讲义的正文专门讲解设计稿到代码的 AI 辅助转换与视觉验收；首考题唯一题源为此讲义。覆盖范围：设计稿/截图到组件结构、Token 映射、响应式推断、视觉 diff 和人工验收；覆盖字体/素材缺失、不可访问设计与生成结果漂移。
- 严格考核：挑战类型：CODING；首考题 1（资料定位）：只允许使用《中文学习讲义：AIDEV-09》，定位 token 映射、响应式推断、视觉 diff 和人工验收依据；首考题 2（机制解释）：闭卷解释截图像素、设计 token、组件状态和可访问交互为什么须分别验证；首考题 3（最小产出）：以给定 checkout 设计图、token JSON、1440/768/375 三个 viewport、键盘路径和 `prefers-reduced-motion` 为固定输入，让 AI 实现页面；交付组件/token diff、三尺寸截图 diff、焦点顺序/对比度输出、字体缺失与长文案降级证据以及两轮评审修改；首考题 4（受限排错）：固定失败为 375 viewport 的 loading 焦点丢失；候选根因仅限硬编码颜色绕过 token、375 溢出、动画未降级、缺字体布局漂移四项，限制修改组件/token；用 DOM/a11y 与前后截图逐项证伪，提交最小修复和三尺寸回归；首考题 5（学习复述）：用 3 分钟说明视觉相似为何不能证明可用与可访问。复测变式：仅将按钮状态改为禁用/错误；不变量为三 viewport、token 与键盘路径；预期状态语义、焦点和视觉基线均通过；新证据为状态截图、a11y 输出和 token diff。命题边界：不以设计工具截图或 AI 生成代码本身作为验收。
- 通过标准：每个 viewport/状态有视觉与可访问证据，token 映射可追溯；否决项：只看单一截图、像素近似替代键盘/对比度、硬编码绕过 token 或无异常状态。评估边界：只交页面 URL 不能通过。
- 预计耗时：资料 135 分钟；练习 195 分钟；项目 180 分钟；考核 105 分钟；复测 90 分钟

## AIDEV-10 AI 研发工具准入、数据与团队治理

- [ ] 自评已掌握
- [ ] 已通过严格考核
- 学习资料：[中文学习讲义：AIDEV-10](../chinese-guides/content-audit-15-17.md#aidev-10)。覆盖范围：团队 AI 工具准入、数据分类、上下文发送、日志留存、审批、例外、指标和责任；覆盖个人账号、敏感仓库与影子 AI 使用。框架整站只在政策更新时作版本核验，不作为本点资料。
- 严格考核：挑战类型：DESIGN_CASE；首考题 1（资料定位）：只允许使用《中文学习讲义：AIDEV-10》，定位数据分类、准入、例外、日志与事故响应；首考题 2（机制解释）：闭卷解释数据分级、供应商处理边界、审批和检测为何必须连成可执行控制；首考题 3（最小产出）：为 50 人前端团队交付工具准入矩阵，固定输入为公开代码、内部源码、客户 PII、密钥和生产日志五类数据及三种 AI 工具；包含允许/禁止、脱敏规则、模型/地区条件、人工复核、保留期、例外时限、责任人、审计字段和 shadow-AI 检测规则，并给出一份 policy/config diff、事故 runbook 与演练验证记录；首考题 4（受限排错）：固定失败为允许工具日志意外保留客户 PII；候选根因仅限个人账号粘贴 token、例外过期仍可用、工具代理绕过 DLP、检测误伤公开仓库四项，限制使用受限日志/事件；以封禁或更正配置 diff 逐项证伪，提交通知、恢复与演练回归证据；首考题 5（学习复述）：用 3 分钟说明“允许使用 AI”为什么不等于允许上传任何上下文。复测变式：仅将客户类别改为受监管客户并把工具改为本地模型；不变量为五类数据、例外期限和审计字段；预期矩阵给出不同准入结论；新证据为更新后的矩阵、策略 diff 和演练日志。命题边界：不以通用隐私口号代替团队可执行控制。
- 通过标准：提交数据到工具到控制的逐项矩阵、例外/审计/演练证据；否决项：密钥/客户数据无条件外发、无责任人/过期机制、只写政策无检测或事故无法收敛。评估边界：一页治理原则不能通过。
- 预计耗时：资料 135 分钟；练习 195 分钟；项目 180 分钟；考核 105 分钟；复测 90 分钟

## AIDEV-11 AI 驱动的浏览器运行时调试与证据闭环

- [ ] 自评已掌握
- [ ] 已通过严格考核
- 学习资料：[中文学习讲义：AIDEV-11](../chinese-guides/content-audit-15-17.md#aidev-11)。该讲义的正文专门讲解 AI 编码 Agent 的浏览器调试、证据收集和回归闭环；首考题唯一题源为此讲义。覆盖范围：为编码 Agent 提供受控浏览器与运行时反馈；复现脚本、检查点和环境固定；DOM/可访问性树、Console、Network、Storage、截图、性能 trace、内存和框架运行时证据；修改前后差异、失败归因、证据包与回归。
- 严格考核：挑战类型：DEBUGGING；首考题 1（资料定位）：只允许使用《中文学习讲义：AIDEV-11》，定位复现、浏览器证据、工具权限、脱敏与回归边界；首考题 2（机制解释）：闭卷解释源码静态阅读与 DOM、网络、trace、a11y 运行时证据各自能证伪什么；首考题 3（最小产出）：在隔离 `orders-demo` fixture，以固定账户/数据/网络节流复现“点击保存后重复 POST 且 INP > 500ms”，给 Agent 只读 `*.demo.test` 与白名单 `localhost` 工具；交付复现脚本、DOM/a11y、Console、HAR 脱敏片段、performance trace、前后截图、最小 abort/debounce 修复 diff、同输入回归和副作用未重放证据；首考题 4（受限排错）：固定失败为点击保存后 403 且 HAR 含 token；候选根因仅限陈旧页面、测试数据漂移、Agent 点击真实提交、删除断言使测试变绿四项，限制在白名单工具和脱敏证据包；逐项证伪，提交拒绝/停止事件、证据校验、修复或回滚及人工接管回归记录；首考题 5（学习复述）：用 3 分钟说明哪些调试动作可自动化、哪些必须审批以及如何证明未制造回归。复测变式：仅将保存交互改为键盘提交；不变量为固定账户/数据/节流、白名单工具和脱敏证据包；预期只触发一次请求且焦点不丢失；新证据为 DOM/a11y、HAR、trace 与回归输出。命题边界：不要求生产账号，不授权绕过认证/支付/写操作。
- 通过标准：证据包逐项含固定环境、原始运行时证据、前后同输入、最小 diff、回归与权限日志；否决项：只看源码/最终截图、删除断言、无限制浏览器权限、泄露敏感字段或无副作用保护。评估边界：测试变绿本身不能通过。
- 预计耗时：资料 135 分钟；练习 195 分钟；项目 180 分钟；考核 105 分钟；复测 90 分钟

## 领域综合考核

- [ ] 已通过领域综合考核
- 任务：选择一个可复核需求，完成规格、AI 辅助实现、分层测试、代码评审、视觉验收、依赖审计和提效复盘；提交完整变更证据，并答辩哪些决策不能交给模型。
- 通过标准：规格可验收；变更与来源可追溯；门禁能发现故意植入的功能、安全和供应链缺陷；提效结论同时包含速度、质量和返工数据；人工责任边界明确。评估边界：领域综合考核只依据本领域列出的资料、题目输入和可复核产出；不得用资料外经验替代跨知识点整合证据。
- 预计耗时：准备 90 分钟；实现 360 分钟；验证 150 分钟；答辩 90 分钟

# 17 现代渲染、数据层、实时协作与离线

所有“通过”均需满足[统一考核规则](00-assessment-rules.md)。本领域补齐框架之上的应用架构能力：渲染边界、Server State、实时协议、协作一致性与离线恢复。

### 现代渲染与边缘运行时

## RENDER-01 SPA、SSR、SSG、ISR 与混合渲染决策

- [ ] 自评已掌握
- [ ] 已通过严格考核
- 学习资料：[中文学习讲义：RENDER-01](../chinese-guides/render-01-spa-ssr-ssg-isr-hybrid-decisions.md#render-01)。覆盖范围：独立讲义按路由的数据时效、个性化、缓存、SEO/状态码、首屏与交互成本、基础设施和失败路径比较 CSR/SSR/SSG/ISR/混合方案，覆盖安全隔离、测量、迁移和决策记录。
- 严格考核：挑战类型：DESIGN_CASE；题源包含《中文学习讲义：RENDER-01》；首考题 1（资料定位）：只允许使用《中文学习讲义：RENDER-01》，定位能支撑「SPA、SSR、SSG、ISR 与混合渲染决策」的定义、关键机制、边界与反例并标明章节；首考题 2（机制解释）：以“公开文章、登录后订单控制台、按城市个性化首页”三条路由画出请求命中、缓存键/失效、HTML 生成、数据注入与交互接管链路；说明公开缓存不能承载租户数据，且 SSR 不等于所有交互都应服务端完成；首考题 3（最小产出）：fixture 固定为文章 10 分钟可接受陈旧、控制台含租户 `t-a/t-b` 且不得共享数据、首页按城市且上游 500 ms；提交三路由决策矩阵、两种实际渲染实现、缓存/失效配置、TTFB/LCP/交互就绪测量与一份回退开关说明；预期公开页可缓存、私有页不串租户、慢首页有可用骨架，并以首访/刷新/切换租户/上游失败四组录屏和网络瀑布验证；首考题 4（受限排错）：给定文章失效后旧标题、城市首页服务端/接管不一致；仅允许候选：缓存键/失效、渲染确定性、回退开关；分别以响应头、服务端/客户端 DOM 和切换 trace 证伪，提交最小修复及同三路由回归记录；首考题 5（学习复述）：面对内容负责人和控制台负责人分别说明为何前者可选择静态再生、后者不能照搬，且说出一项不应 SSR 的交互场景。复测变式：仅把首页加入 A/B 分组并使 CDN 命中率下降，保持三路由、租户和上游延迟不变；预期私有键仍隔离且回退可用；提交更新决策矩阵、命中指标和故障录屏。命题边界：答案必须区分框架实现与通用 Web 约束。
- 通过标准：①每条路由的时效、个性化、成本、故障与回滚取舍可追溯（证据：决策矩阵与 ADR）；②两种实现的首访及失效行为符合矩阵（证据：网络瀑布、指标和自动化断言）；③私有数据不进入公共缓存且失败可切回（证据：租户隔离/故障录屏）。评估边界：仅依据本点学习资料、题目固定的三路由/租户/上游输入和可复核的产出证据评估，不以其他框架经验替代。否决项：只报框架名、未提供测量，或以清空全站缓存掩盖错误均不通过。
- 预计耗时：资料 90 分钟；练习 150 分钟；项目 135 分钟；考核 90 分钟；复测 75 分钟

## RENDER-02 流式 SSR、Hydration 与 Islands 架构

- [ ] 自评已掌握
- [ ] 已通过严格考核
- 学习资料：[中文学习讲义：RENDER-02](../chinese-guides/render-02-streaming-ssr-hydration-islands.md#render-02)。覆盖范围：独立讲义解释流式响应边界、Hydration 一致性、选择性接管、Islands、序列化安全、无 JS 基线、客户端导航、部分失败、取消、缓存和可观测；React 专有 RSC/Server Functions 归 `REACT-09`。
- 严格考核：挑战类型：CODING；题源包含《中文学习讲义：RENDER-02》；首考题 1（资料定位）：只允许使用《中文学习讲义：RENDER-02》，定位流式边界、首包、Hydration、Islands 和恢复策略；首考题 2（机制解释）：以商品详情画出 80 ms 摘要、1.5 s 推荐区、数量岛和无 JS 提交的字节到达、序列化、加载与激活时序；说明流式边界不能把私有数据写入公开状态，且 Islands 不等于取消服务端回退；首考题 3（最小产出）：fixture 固定为摘要成功、推荐延迟 1.5 s、评论区 500 错误及弱网 400 kbps；提交流式页面、边界图、可序列化状态清单、无 JS 表单回退与瀑布截图；预期首包先含摘要、评论独立报错、数量岛可用且无 JS 可提交，以中止响应、禁 JS、延迟接管和客户端导航四组自动化验证；首考题 4（受限排错）：给定推荐阻塞首包、库存接管后不一致、中止流后无响应；仅允许候选：边界位置、序列化状态、取消清理；分别以响应分块、状态快照和中止 trace 证伪，提交最小修复及同页面回归记录；首考题 5（学习复述）：用同一商品页说明流式 SSR、传统 SSR、Islands、CSR 各自把等待留给谁，及为何不是四选一的性能口号。复测变式：仅把推荐改为需授权的个性化区，保持摘要、数量岛和弱网不变；预期其数据不进入公开流或序列化状态；提交响应分块、DOM 和授权回归记录。命题边界：不得以 React RSC 私有协议作为通用 Web 机制；实验特性必须标明版本与稳定性。
- 通过标准：①骨架、慢区和交互岛职责一致（证据：响应分块与组件边界图）；②中止、部分错误、禁 JS 和接管差异可恢复（证据：四组测试记录）；③状态不泄露、重复副作用受控（证据：日志与 DOM 一致性断言）。评估边界：仅依据本点学习资料、题目固定输入和可复核产出评估。否决项：只展示脚手架、把私有协议当通用机制或未测无 JS/中断路径均不通过。
- 预计耗时：资料 90 分钟；练习 150 分钟；项目 135 分钟；考核 90 分钟；复测 75 分钟

## EDGE-01 Edge Runtime、Serverless 执行约束与部署边界

- [ ] 自评已掌握
- [ ] 已通过严格考核
- 学习资料：[中文学习讲义：EDGE-01](../chinese-guides/content-audit-15-17.md#edge-01)。该讲义的正文专门讲解 Edge/Serverless BFF 的运行时约束、租户边界和回退；覆盖范围：BFF 聚合、认证/租户边界、Web 标准与 Node API 差异、Edge/Serverless CPU/内存/连接/时长限制、冷启动、多区域状态一致性、缓存、密钥、日志追踪、过载和常规服务回退。
- 严格考核：挑战类型：CODING；题源包含《中文学习讲义：EDGE-01》；首考题 1（资料定位）：只允许使用《中文学习讲义：EDGE-01》，定位能支撑「Edge Runtime、Serverless 执行约束与部署边界」的定义、关键机制、边界与反例并标明章节；首考题 2（机制解释）：以 `t-a`/`t-b` 订单摘要画出浏览器认证、边缘鉴权、公共/私有缓存、上游预算和源站回退链；说明边缘运行时限制不能以降级租户隔离或下放密钥解决；首考题 3（最小产出）：fixture 固定为 `t-a`、`t-b` 各一名登录用户、价格上游 300 ms 超时、画像上游 200、运行时禁用 `fs`；提交 Edge BFF 的认证/授权伪码或实现、缓存键表、超时预算、trace 样本、两租户响应和常规服务回退开关；预期画像可降级、价格不跨租户、禁用 API 不在边缘执行，以三种异常和一次切回演练验证；首考题 4（受限排错）：给定 `fs` 导入失败、东京到源站 900 ms、`t-b` 命中 `t-a` 缓存；仅允许候选：运行时兼容、区域回退、缓存键隔离；分别以构建堆栈、trace 和响应头证伪，提交最小修复及同请求回归记录；首考题 5（学习复述）：用“只转发一个后端接口、需聚合两个有不同延迟的接口”对比何时不应/应增加 BFF。复测变式：仅把画像上游改为地区驻留受限，保持租户、预算和价格上游不变；预期边缘改走合规源站回退；提交区域 trace、缓存键与切换记录。命题边界：不得把 BFF 当作绕过后端治理的代理层。
- 通过标准：①凭证、密钥和授权边界正确（证据：代码审查与请求记录）；②超时、隔离和降级遵守预算（证据：trace/故障注入）；③回退可实际执行（证据：切换演练）。评估边界：仅依据本点学习资料、题目固定输入和可复核产出评估。否决项：以 CDN 命中代替租户隔离、将密钥下放或只给供应商专属结论均不通过。
- 预计耗时：资料 90 分钟；练习 150 分钟；项目 135 分钟；考核 90 分钟；复测 75 分钟

### Server State 与一致性

## DATA-01 Server State、缓存键、失效与请求去重

- [ ] 自评已掌握
- [ ] 已通过严格考核
- 学习资料：[中文学习讲义：DATA-01](../chinese-guides/data-01-server-state-cache-keys-invalidation-deduplication.md#data-01)。覆盖范围：独立讲义从远端事实边界推进到完整 query key、fresh/stale/gc、去重、取消竞态、失效影响图、分页/预取、SSR hydration、权限/身份隔离和可观测，避免把缓存当本地业务事实。
- 严格考核：挑战类型：CODING；题源包含《中文学习讲义：DATA-01》；首考题 1（资料定位）：只允许使用《中文学习讲义：DATA-01》，定位能支撑「Server State、缓存键、失效与请求去重」的定义、关键机制、边界与反例并标明章节；首考题 2（机制解释）：以 `t-a` 状态、游标 `c1/c2` 与 `o-7` 画出命中、陈旧、取消、重取、写后失效和登出清理状态机；说明客户端 UI 状态不能替代含租户/筛选/游标的 Server State 键；首考题 3（最小产出）：fixture 固定为两租户、两筛选、两页游标、详情 `o-7` 和 800 ms 慢查询；提交完整缓存键表、fresh/stale/gc 值、写入影响图、取消/去重实现和 HAR；预期同键并发只发一次、切租户取消旧请求、写后仅相关列表/详情更新、登出清私有项，以四组输入自动化验证；首考题 4（受限排错）：给定切租户仍显示旧订单、旧响应覆盖新筛选、重复请求；仅允许候选：键维度、取消竞态、请求去重；分别以缓存快照、Abort trace 和 HAR 证伪，提交最小修复及同筛选回归记录；首考题 5（学习复述）：为筛选条件、模态框开关和远端订单分别选择 URL、客户端状态或 Server State，并解释错放的后果。复测变式：仅把 `o-7` 权限从可看改为不可看，保持租户、筛选和游标不变；预期缓存/预取立即失权；提交缓存快照、HAR 与 UI 断言。命题边界：必须说明使用的库版本及通用原理。
- 通过标准：①键和失效范围完整（证据：缓存键表/影响图）；②竞态、取消、去重及登出可复核（证据：HAR 与自动化断言）；③列表与详情一致（证据：写后快照）。评估边界：仅依据本点学习资料、题目固定输入和可复核产出评估。否决项：全量清缓存、未含租户/筛选/游标或只展示成功请求均不通过。
- 预计耗时：资料 90 分钟；练习 150 分钟；项目 135 分钟；考核 90 分钟；复测 75 分钟

## DATA-02 乐观更新、并发冲突与离线突变

- [ ] 自评已掌握
- [ ] 已通过严格考核
- 学习资料：[中文学习讲义：DATA-02](../chinese-guides/data-02-optimistic-updates-conflicts-offline-mutations.md#data-02)。覆盖范围：独立讲义从适用性、操作身份、可逆补丁推进到并发版本、冲突合并、unknown 结果、持久 outbox、跨身份隔离、乱序/重复重放、撤销与悲观策略，明确服务器权威与用户恢复路径。
- 严格考核：挑战类型：CODING；题源包含《中文学习讲义：DATA-02》；首考题 1（资料定位）：只允许使用《中文学习讲义：DATA-02》，定位能支撑「乐观更新、并发冲突与离线突变」的定义、关键机制、边界与反例并标明章节；首考题 2（机制解释）：以 `T-9@v3` 离线编辑画出快照、操作 ID、基版本、outbox、409、合并和登出隔离链；说明乐观 UI 不等于可以忽略未知结果、乱序或跨身份队列；首考题 3（最小产出）：fixture 固定为 `T-9@v3`、同一操作 ID 双击、另一设备先提交 `v4`、断网后重启、用户 `u-a` 登出再以 `u-b` 登录；提交队列记录、乐观补丁/回滚、冲突 UI、重放日志和身份清理策略；预期双击只生效一次、409 保留两方内容且不静默覆盖、`u-b` 不见 `u-a` 队列，以四组录像和服务端幂等记录验证；首考题 4（受限排错）：给定失败日志：旧成功覆盖新编辑、重试重复创建、409 后输入消失；仅允许候选：版本比较、幂等键、回滚快照；分别以事件序列、服务端记录和 UI 快照证伪，提交最小修复及同操作回归记录；首考题 5（学习复述）：用转账审批、草稿标题和库存扣减三例说明哪些必须悲观、哪些可乐观及谁承担失败成本。复测变式：仅把服务端响应改为“已处理但响应丢失”，保持操作 ID 和版本不变；预期 UI 显示未知并可恢复查询；提交幂等记录、状态机和回归录屏。命题边界：不能假设网络请求严格有序或只执行一次。
- 通过标准：①用户输入和队列跨断网/重启可恢复（证据：持久化快照与重放日志）；②重复、乱序、409 和登出隔离正确（证据：服务端记录/四组测试）；③冲突有可行动的呈现与选择（证据：UI 录屏）。评估边界：仅依据本点学习资料、题目固定输入和可复核产出评估。否决项：默认最后写入覆盖、队列跨身份复用或将未知结果直接标失败均不通过。
- 预计耗时：资料 90 分钟；练习 150 分钟；项目 135 分钟；考核 90 分钟；复测 75 分钟

### 实时消息与多人协作

## REALTIME-01 SSE、WebSocket、WebTransport 与实时消息可靠性

- [ ] 自评已掌握
- [ ] 已通过严格考核
- 学习资料：[中文学习讲义：REALTIME-01](../chinese-guides/realtime-01-sse-websocket-webtransport-reliability.md#realtime-01)。覆盖范围：独立讲义先定义延迟/丢失/重复/顺序合同，再比较轮询、SSE、WebSocket 与 WebTransport，覆盖应用消息、游标恢复、至少一次去重、背压、鉴权、扩缩容、版本、可观测和降级切换。
- 严格考核：挑战类型：CODING；首考题 1（资料定位）：只允许使用《中文学习讲义：REALTIME-01》，比较 SSE、WebSocket 与 WebTransport 的能力、兼容和安全边界；首考题 2（机制解释）：以订单、光标和聊天确认画出连接、编号、ACK、重连游标、背压与降级链；说明传输可靠不等于业务恰好一次，且可丢光标不能套用到订单；首考题 3（最小产出）：fixture 固定为订单事件 `101..105`、光标 20 Hz、代理 60 秒空闲断连、WebTransport 不支持；提交 SSE 游标恢复、WebSocket 双向确认、WebTransport 可靠流/数据报小实验、能力检测和协议决策表；预期订单无静默丢失、光标允许跳帧、断连后从游标续接，以断网、切网、重复、乱序、丢包和背压六组日志验证；首考题 4（受限排错）：给定 103 重复/104 缺失、代理断连后快速重连、慢消费者内存增长；仅允许候选：游标幂等、退避策略、背压释放；分别以事件序列、连接日志和内存曲线证伪，提交最小修复及同网络回归记录；首考题 5（学习复述）：为三类消息分别选择传输与业务确认，并解释为什么“可靠 WebSocket”不等于业务恰好一次。复测变式：仅把恢复窗口缩短到 100 条，保持订单序列和断连时间不变；预期缺口走快照/轮询补齐；提交游标、补洞日志和降级测试。命题边界：必须区分传输可靠、应用确认、幂等和业务恰好一次语义。
- 通过标准：①消息类别与可靠性边界一致（证据：协议决策表）；②恢复、去重、背压和资源释放可测（证据：六组事件日志）；③不支持能力可保留核心任务（证据：降级自动化）。评估边界：仅依据本点学习资料、题目固定输入和可复核产出评估。否决项：宣称任一传输自带持久化/恰好一次或无重连上限均不通过。
- 预计耗时：资料 90 分钟；练习 150 分钟；项目 135 分钟；考核 90 分钟；复测 75 分钟

## COLLAB-01 CRDT、OT、Presence 与多人协作一致性

- [ ] 自评已掌握
- [ ] 已通过严格考核
- 学习资料：[中文学习讲义：COLLAB-01](../chinese-guides/content-audit-15-17.md#collab-01)。覆盖范围：CRDT、OT 与服务端串行的状态/操作模型和取舍；因果关系、收敛、Presence/意识状态、离线并发、撤销/重做、Schema 演进、权限、恶意更新、历史压缩、快照和恢复。Yjs 与 Automerge 整站仅在实施选型时按具体 API 核验，不作本点资料。
- 严格考核：挑战类型：CODING；首考题 1（资料定位）：只允许使用《中文学习讲义：COLLAB-01》，定位 CRDT、OT、Presence、离线并发与收敛边界；首考题 2（机制解释）：以 `D-1` 两端离线编辑、并发删除和 Presence 光标画出操作/状态传播、因果合并、撤销与持久化链；说明收敛不自动解决权限或恶意更新，Presence 也不应等同文档历史；首考题 3（最小产出）：fixture 固定为客户端 A/B、Schema v1/v2、乱序重复更新、256 KB 恶意更新与断线 5 分钟；提交限定操作集合的协作文档、收敛断言、Presence 分离存储、迁移策略、快照/压缩与服务端限额；预期 A/B 最终文档一致、Presence 不落库、旧 Schema 明确阻断或迁移，以合并前后状态哈希、重连记录与拒绝日志验证；首考题 4（受限排错）：给定并发删除后两端不同、旧客户端写 v1、历史增长 10 倍；仅允许候选：因果合并、版本闸门、快照压缩；分别以状态哈希、拒绝日志和历史大小证伪，提交最小修复及同文档回归记录；首考题 5（学习复述）：对共享正文、强顺序审批和临时光标分别说明 CRDT、OT、服务端串行或非持久 Presence 的选择理由。复测变式：仅增加第三端从旧快照离线恢复，保持 A/B 操作和 Schema 不变；预期已确认删除不回滚；提交三端哈希、迁移记录和回归日志。命题边界：安全与访问控制不能交给 CRDT 自动解决，也不考具体 P2P 连接协议。
- 通过标准：①定义操作内可证明收敛（证据：状态哈希与并发测试）；②版本、撤销、快照和历史边界明确（证据：迁移/恢复演练）；③Presence、权限与资源限制分层（证据：库记录和拒绝日志）。评估边界：仅依据本点学习资料、题目固定输入和可复核产出评估。否决项：只演示两端 happy path、让 Presence 持久化或依赖客户端限制恶意更新均不通过。
- 预计耗时：资料 90 分钟；练习 150 分钟；项目 135 分钟；考核 90 分钟；复测 75 分钟

### 离线、本地优先与应用能力

## PWA-01 Service Worker、离线缓存与版本迁移

- [ ] 自评已掌握
- [ ] 已通过严格考核
- 学习资料：[中文学习讲义：PWA-01](../chinese-guides/content-audit-15-17.md#pwa-01)。该讲义的正文专门讲解 Service Worker 生命周期、离线缓存与版本迁移；覆盖范围：Service Worker 安装/等待/激活、作用域、离线应用壳、导航回退、运行时缓存、更新提示、旧资源兼容、Cache API/IndexedDB/OPFS 选型、配额/驱逐、版本迁移、清理和多标签协调。模型制品专属下载与激活归 `WEBAI-05`。
- 严格考核：挑战类型：CODING；首考题 1（资料定位）：只允许使用《中文学习讲义：PWA-01》，定位生命周期、应用壳/运行时缓存、配额与存储迁移边界；首考题 2（机制解释）：以 v7 应用壳、仍开着 v6 的标签和 200 MB 附件画出安装、等待、激活、存储、驱逐与升级协调链；说明立即接管不等于安全更新，浏览器缓存也不是永久存储；首考题 3（最小产出）：fixture 固定为 v6→v7（旧 chunk 已下线）、两标签、50 MB 存储预算、离线导航和一次升级中断；提交缓存清单/预算、升级提示与确认流程、迁移日志、多标签消息和离线回退；预期旧标签不白屏、确认后原子切换、空间不足可释放，以断网刷新、升级中断、配额耗尽、驱逐后恢复四组验证；首考题 4（受限排错）：给定新 HTML 指向已删 chunk、`t-b` 看到 `t-a` 响应、迁移中断；仅允许候选：版本原子性、缓存范围、迁移事务；分别以缓存清单、响应头和恢复日志证伪，提交最小修复及同升级回归记录；首考题 5（学习复述）：为应用壳、可再生图片、草稿和私密订单分别选择存储/不缓存策略，并说明原因。复测变式：仅让用户拒绝更新并连续工作 24 小时，保持版本、预算和两标签不变；预期兼容窗口后有明确升级提示；提交缓存清单、提示录屏和恢复记录。命题边界：模型制品分片、摘要校验和原子激活归 WEBAI-05；不得把浏览器存储视为永久可靠或适合保存明文敏感数据。
- 通过标准：①离线核心流与更新边界正确（证据：四组录屏/缓存清单）；②迁移、配额、驱逐可恢复可观测（证据：日志与恢复演练）；③私有与敏感内容不入共享缓存（证据：范围测试）。评估边界：仅依据本点学习资料、题目固定输入和可复核产出评估。否决项：默认立即接管、无限配额假设或只测新装环境均不通过。
- 预计耗时：资料 90 分钟；练习 150 分钟；项目 135 分钟；考核 90 分钟；复测 75 分钟

## LOCALFIRST-01 本地优先数据、浏览器数据库与同步引擎

- [ ] 自评已掌握
- [ ] 已通过严格考核
- 学习资料：[中文学习讲义：LOCALFIRST-01](../chinese-guides/content-audit-15-17.md#localfirst-01)。该讲义的正文专门讲解本地数据库、同步引擎与恢复边界；覆盖范围：local-first 与 offline-capable 的差别；IndexedDB/OPFS/SQLite Wasm 选择；事务、索引、Schema 迁移、配额/持久化/逐出、加密边界、Worker 并发、跨标签协调、变更日志、outbox/inbox、游标同步、幂等、删除墓碑、冲突检测/合并、快照压缩、备份导出、恢复、可观测性与多设备一致性；CRDT 协同细节归入 `COLLAB-01`。
- 严格考核：挑战类型：CODING；首考题 1（资料定位）：只允许使用《中文学习讲义：LOCALFIRST-01》，定位事务、版本升级、OPFS、持久化请求及配额逐出依据；首考题 2（机制解释）：以 `K-1` 编辑、墓碑与两标签并发画出事务、outbox/inbox、拉取、冲突、压缩和恢复链；说明 local-first 不等于永不逐出或用墙钟最后写入覆盖冲突；首考题 3（最小产出）：fixture 固定为 DB v3→v4 升级、`K-1` 两端并发、50 MB 配额、OPFS 不支持、服务端快照落后及同步消息 `m1/m1/m2` 乱序；提交 Schema/迁移、事务与 outbox、墓碑/冲突 UI、跨标签协调、导出文件和恢复脚本；预期重启后已确认编辑在、重复消息无副作用、删除不复活、空环境可恢复，以升级中断、断网重启、逐出、乱序、导入五组证据验证；首考题 4（受限排错）：给定升级中断无法打开、两标签覆盖、旧快照复活墓碑、满配额半写入；仅允许候选：迁移闸门、领导者协调、墓碑比较、事务边界；分别以迁移日志、跨标签消息、状态哈希和事务记录证伪，提交最小修复及同库回归记录；首考题 5（学习复述）：对个人草稿、团队协作文档和只读仪表盘判断是否值得承担 local-first 同步成本。复测变式：仅把设备时钟后拨 24 小时并重放旧消息，保持 DB、身份和队列不变；预期墓碑不复活；提交冲突日志、状态哈希和导入回归。命题边界：不得用时间戳最后写入覆盖所有冲突，也不得承诺浏览器存储永不被逐出。
- 通过标准：①本地事务、迁移、协调和同步顺序可审计（证据：Schema/日志）；②刷新、断网、逐出、重复乱序及删除均可恢复（证据：五组故障演练）；③导入导出保持关联完整性（证据：空环境恢复核对）。评估边界：仅依据本点学习资料、题目固定输入和可复核产出评估。否决项：用 localStorage 代替数据库、只演示在线单设备或承诺不被逐出均不通过。
- 预计耗时：资料 120 分钟；练习 180 分钟；项目 180 分钟；考核 105 分钟；复测 90 分钟

## PWA-02 Web App Manifest、安装、推送与后台能力

- [ ] 自评已掌握
- [ ] 已通过严格考核
- 学习资料：[中文学习讲义：PWA-02](../chinese-guides/content-audit-15-17.md#pwa-02)。该讲义的正文专门讲解安装、Share Target、推送和后台能力的渐进增强；覆盖范围：Manifest 成员、图标/主题/显示模式/作用域、安装资格与自定义安装、`launch_handler`/快捷方式/文件/协议/Share Target 的渐进增强；分享文本/URL/文件的 method、enctype、大小/类型/数量校验、临时文件生命周期、CSRF/导航与不可信输入；推送订阅、通知权限与操作、后台事件、静默推送限制、退订、隐私、配额和浏览器/平台差异。
- 严格考核：挑战类型：CODING；首考题 1（资料定位）：只允许使用《中文学习讲义：PWA-02》，定位安装、分享目标、推送和通知规则；首考题 2（机制解释）：以分享 URL+PDF、确认入库、订单推送与深链画出 Manifest、导航、SW、推送服务和通知链；说明系统安装/后台能力不可用时核心任务仍须可完成，分享输入不能被信任；首考题 3（最小产出）：fixture 固定为 1 个 URL、1 个 12 MB 伪造 MIME 的 PDF、重复 share ID、被拒绝通知权限、过期订阅及不支持 Share Target 的移动端；提交 manifest、接收/预览/确认流程、输入校验、订阅生命周期、最小推送载荷、深链白名单和降级矩阵；预期不信任类型/扩展名、重复不重复入库、拒绝授权仍可完成核心任务，以六组真机/模拟证据验证；首考题 4（受限排错）：给定分享字段不匹配、伪造 PDF、订阅 410 持续发送、通知跳外站；仅允许候选：接收契约、内容验证、退订、深链白名单；分别以请求载荷、扫描结果、订阅日志和导航记录证伪，提交最小修复及同真机回归记录；首考题 5（学习复述）：为文件导入、营销提醒和待办提醒分别说明何时不该注册分享目标或请求通知权限。复测变式：仅让系统不暴露安装入口，保持分享、订阅和权限状态不变；预期应用内提示不阻断浏览器核心路径；提交真机录屏、能力矩阵和任务结果。命题边界：分享目标收到的所有文本、URL 和文件均为不可信输入；不得在无用户意图时诱导授权，也不得承诺后台任务永久运行。
- 通过标准：①安装/非安装和支持/不支持路径均完成核心任务（证据：矩阵）；②输入、深链、订阅和临时存储安全可测（证据：六组日志）；③授权与通知可撤回可恢复（证据：订阅状态记录）。评估边界：仅依据本点学习资料、题目固定输入和可复核产出评估。否决项：信任 MIME/扩展名、收到分享即发布、强迫授权或只测单桌面浏览器均不通过。
- 预计耗时：资料 120 分钟；练习 180 分钟；项目 165 分钟；考核 105 分钟；复测 90 分钟

### 数据接口专项

## GQL-01 GraphQL Schema、缓存与前端契约

- [ ] 自评已掌握
- [ ] 已通过严格考核
- 学习资料：[中文学习讲义：GQL-01](../chinese-guides/content-audit-15-17.md#gql-01)。该讲义的正文专门讲解 Schema、前端缓存与错误/权限契约；覆盖范围：Schema、query/mutation/subscription、片段、nullability、错误、分页、规范化缓存、复杂度和增量交付。GraphQL Learn 整站不作为本点资料。
- 严格考核：挑战类型：CODING；题源包含《中文学习讲义：GQL-01》；首考题 1（资料定位）：只允许使用《中文学习讲义：GQL-01》，定位能支撑「GraphQL Schema、缓存与前端契约」的定义、关键机制、边界与反例并标明章节；首考题 2（机制解释）：以 `OrderConnection`、`o-7` 与拒绝字段画出验证、解析、null/错误、实体键、游标和部分成功到 UI 的链；说明 GraphQL 不能自动消除 N+1、授权或跨租户串写；首考题 3（最小产出）：fixture 固定为两租户、第一页后新增一条订单、`o-7` 部分字段无权限、列表 20 条/页；提交 Schema、生成类型、稳定游标契约、缓存实体键、错误 UI 与复杂度/字段授权规则；预期列表无重复/跳项、无权字段不泄露且 UI 可解释，以 N+1 计数、权限、部分错误和写后更新四组测试验证；首考题 4（受限排错）：给定 20 单产生 21 次调用、同 ID 串租户、data+errors 白屏；仅允许候选：批处理、实体键、部分错误边界；分别以调用计数、缓存快照和 UI 断言证伪，提交最小修复及同查询回归记录；首考题 5（学习复述）：为实时订阅、资源缓存查询和单一命令接口分别说明 GraphQL、REST、RPC 的契约优势与额外成本。复测变式：仅增加一个增量交付字段，保持 Schema、租户和分页不变；预期未到达字段不被误作 null；提交 payload、UI 断言和缓存快照。命题边界：GraphQL 不能被描述成自动解决性能或授权。
- 通过标准：①Schema、null/错误、分页和实体键相互一致（证据：契约测试）；②授权和复杂度在服务端有效（证据：拒绝/计数日志）；③部分成功与写后缓存行为可复核（证据：UI/网络测试）。评估边界：仅依据本点学习资料、题目固定输入和可复核产出评估。否决项：只凭客户端成功、以 ID 单独作跨租户键或声称 GraphQL 自动解决性能/授权均不通过。
- 预计耗时：资料 90 分钟；练习 150 分钟；项目 135 分钟；考核 90 分钟；复测 75 分钟

## 领域综合考核

- [ ] 已通过领域综合考核
- 任务：设计并实现一个支持 SSR、Server State、实时通知和离线写入的垂直切片；提交渲染决策、协议契约、缓存/冲突模型、故障注入记录和恢复演示。
- 通过标准：首屏与水合一致；缓存键、写入幂等和冲突呈现明确；断线、重复消息、离线提交和服务端失败均可恢复；渲染与数据边界有测量依据。评估边界：领域综合考核只依据本领域列出的资料、题目输入和可复核产出；不得用资料外经验替代跨知识点整合证据。
- 预计耗时：准备 120 分钟；实现 420 分钟；故障注入 120 分钟；答辩 90 分钟

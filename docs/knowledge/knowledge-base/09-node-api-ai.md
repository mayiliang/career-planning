# 09 Node.js 服务与接口工具

所有“通过”均需满足[统一考核规则](00-assessment-rules.md)。本领域负责服务运行时、接口规范和确定性代码生成；MCP 协议本体统一由领域 13 负责。所有现行中文资料均列于“学习资料”，并由首考题 1 逐项精确引用。

### Node.js 服务运行时

## NODE-01 Node 运行时、事件循环与非阻塞 I/O

- [ ] 自评已掌握
- [ ] 已通过严格考核
- 学习资料：[中文主讲义：NODE-01](../chinese-guides/node-01-runtime-event-loop-nonblocking-io.md#node-01)、[中文｜Node.js 事件循环](https://nodejs.cn/learn/asynchronous-work/event-loop-timers-and-nexttick)、[中文｜不要阻塞事件循环与工作线程池](https://nodejs.cn/learn/asynchronous-work/dont-block-the-event-loop)。覆盖范围：独立主讲义从 JavaScript 主线程、内核 I/O 与 libuv 线程池逐层讲到事件循环阶段、`nextTick`/Promise 饥饿、公平性、延迟与利用率测量、分片/worker 选择、异步资源追踪、容量和版本实验；两篇 Node 中文页用于核对官方阶段、Node 20 计时变化、工作池与阻塞边界。正文不依赖固定测试输入，也不要求背诵偶然回调顺序。
- 严格考核：挑战类型：DEBUGGING；首考题 1（资料定位）：只允许使用《中文主讲义：NODE-01》《中文｜Node.js 事件循环》《中文｜不要阻塞事件循环与工作线程池》，分别摘出能支撑「Node 运行时、事件循环与非阻塞 I/O」的定义、关键机制、边界/反例，并标明来源；首考题 2（机制解释）：闭卷给出「读取完成回调中排入 `process.nextTick`、Promise、`setImmediate` 与 `setTimeout(0)`」的脚本，解释每个入队动作、事件循环阶段、微任务清空点及为何不能用浏览器顺序外推 Node；首考题 3（最小产出）：固定夹具为 64 MiB JSON、20 个并发 `/health` 请求和 150 ms 延迟预算；提交顺序预测表、`monitorEventLoopDelay` 前后直方图、把 CPU 解析改为分片或 worker 的实现与压测记录，产物须含命令、原始指标和结论；首考题 4（受限排错）：仅可改调度/worker 边界；夹具依次注入同步 `readFileSync`、无限 `nextTick` 递归、worker 抛错和晚到结果。提交请求时间线、延迟/吞吐证据、三项可证伪假设、根因、修复及回归；失败现象：事件循环延迟超过 150 ms、健康检查超时；首考题 5（掌握挑战）：面向值班同事用该图表解释“非阻塞不等于没有 CPU 饥饿”，比较分片与 worker 的消息复制、错误传播和状态所有权，并回答“若 95 分位延迟仍超预算应先验证什么”。复测变式：仅把 64 MiB 解析从 worker 线程改回主线程；不变量=20 个 `/health` 请求与 150 ms 预算不变，预期变化=p95 延迟越线，证据=`monitorEventLoopDelay` 直方图和请求时间线。证据要求：提交固定夹具输入、预期/实际、关键事件序列、根因和自动化回归。命题边界：只在所列资料和覆盖范围内命题。
- 通过标准：顺序预测至少 9/10（证据=可运行日志）；150 ms 预算内健康检查不被 CPU 工作饿死（证据=前后延迟直方图）；worker/分片错误可观测且结果不竞态（证据=四个失败夹具回归）。否决项：只给截图、只“异步化”同步阻塞、或以浏览器事件循环解释 Node，均不能通过。 证据映射：能力结论=指定日志/快照/散列；失败处理=固定夹具回归；边界选择=题 2 的机制说明。否决项：无固定输入、无失败证据或只交成功截图。 评估边界：仅以本点资料、题目输入与上述可复核产出评分，资料外经验不能替代证据。
- 预计耗时：资料 120 分钟；练习 180 分钟；项目 165 分钟；考核 105 分钟；复测 90 分钟

## NODE-02 文件、Stream、Buffer 与错误处理

- [ ] 自评已掌握
- [ ] 已通过严格考核
- 学习资料：[中文主讲义：NODE-02](../chinese-guides/node-02-files-streams-buffers-errors.md#node-02)、[中文｜流中的背压](https://nodejs.cn/learn/modules/backpressuring-in-streams)、[中文｜使用 Node.js 写入文件](https://nodejs.cn/learn/manipulating-files/writing-files-with-nodejs)。覆盖范围：独立主讲义从句柄与 Buffer 推进到记录分块、Stream 背压、`pipeline`、结束/关闭、错误原因链、临时文件、`fsync`、原子替换、并发、路径安全、Web Stream 互操作和可恢复批量导入；两篇 Node 中文教程核对背压内存对照和基础异步写入。完整 API 根页继续作为遇到具体方法时的按需参考，不作为连续必读。
- 严格考核：挑战类型：DEBUGGING；首考题 1（资料定位）：只允许使用《中文主讲义：NODE-02》《中文｜流中的背压》《中文｜使用 Node.js 写入文件》，分别摘出定义、背压/关闭机制和反例并标明来源；首考题 2（机制解释）：闭卷画出 2 GiB 输入从 Readable、Transform、Writable 到临时文件替换的状态图，说明 `write()` 返回 false 后为何必须等待 drain，以及错误、取消和 close 如何传递；首考题 3（最小产出）：固定夹具为 2 GiB NDJSON、Writable `highWaterMark=16 KiB` 且每 10 ms 消费一次；实现 `pipeline` 转换、AbortSignal 取消、`.tmp` 写入/`fsync`/原子替换。提交内存曲线、临时文件清单、取消后的文件散列和四类错误结果；验证：执行固定夹具并记录断言/日志；首考题 4（受限排错）：仅可改 Stream/文件资源管理；注入 ENOSPC、EACCES、半行 JSON、消费者提前关闭四个候选。提交每一夹具的错误链（含 cause）、句柄计数、预期/实际、三项假设、排除证据、根因、修复与同夹具回归；首考题 5（掌握挑战）：向评审解释为什么“读入 Buffer 再写出”在此夹具不可接受，说明背压与原子替换各守住什么边界，并现场判断哪个错误应保留原目标文件。复测变式：仅把本地 Writable 改为每 10 ms 消费一次的网络 Writable；不变量=取消后原目标文件散列不变，预期变化=出现 drain 等待，证据=背压时间线与临时文件清单。证据要求：提交固定夹具输入、预期/实际、关键事件序列、根因和自动化回归。命题边界：只在所列资料和覆盖范围内命题。
- 通过标准：峰值内存不随 2 GiB 输入线性增长（证据=内存曲线）；失败/取消后目标文件散列保持旧值且 `.tmp` 清理（证据=夹具清单）；每个异常保留 cause 与文件上下文、所有句柄归零（证据=错误/句柄报告）。否决项：手工串接未处理背压、catch 后吞错、或以“本机成功”代替失败回归，均不能通过。 证据映射：能力结论=指定日志/快照/散列；失败处理=固定夹具回归；边界选择=题 2 的机制说明。否决项：无固定输入、无失败证据或只交成功截图。 评估边界：仅以本点资料、题目输入与上述可复核产出评分，资料外经验不能替代证据。
- 预计耗时：资料 120 分钟；练习 180 分钟；项目 165 分钟；考核 105 分钟；复测 90 分钟

## NODE-04 Node.js HTTP 服务与 BFF 生产工程

- [ ] 自评已掌握
- [ ] 已通过严格考核
- 学习资料：[中文主讲义：NODE-04](../chinese-guides/node-04-http-bff-production-engineering.md#node-04)、[中文｜AbortController](https://developer.mozilla.org/zh-CN/docs/Web/API/AbortController)。覆盖范围：独立主讲义从 BFF 边界和请求状态机推进到运行时校验、身份、端到端 deadline、取消、聚合容量、重试/幂等、缓存、流式背压、过载、可观测性、健康检查、优雅停机、代理信任与滚动发布；MDN 页面核对通用取消接口。Node HTTP 与 `perf_hooks` 根参考按实际运行版本查具体方法，不作为初学者从头到尾的必读材料。
- 严格考核：挑战类型：DEBUGGING；首考题 1（资料定位）：只允许使用《中文主讲义：NODE-04》《中文｜AbortController》，定位请求生命周期、超时、取消、背压、连接复用和服务关闭依据；首考题 2（机制解释）：闭卷画出浏览器请求经网关、BFF、下游服务再流式返回的完整链路，区分客户端断开、应用超时、下游超时和进程终止；首考题 3（最小产出）：固定夹具：租户 t-7、请求 r-41、幂等键 k-41、两个下游 80/250 ms；客户端在 120 ms 断开、SIGTERM 在 180 ms 到达；实现一个类型化 BFF，聚合两个下游接口，具备 Schema 校验、认证上下文、总截止时间、取消传播、并发上限、幂等写入、流式响应、结构化错误、健康检查和优雅停机；可观察验证：按固定夹具断言关键状态，并提交日志或快照；首考题 4（受限排错）：受限范围：仅可改题 3 的请求生命周期、连接池、取消和停机边界，不得改夹具、授权或目标；重放慢下游、客户端中止、重复写入、SIGTERM 四个候选。提交请求/关闭时间线、连接池与幂等日志、预期/实际、三项假设、排除证据、根因、修复和同夹具回归；首考题 5（掌握挑战）：向网关负责人说明客户端中止与下游超时为何不能共用重试策略，并为 r-41 在停机中选择“完成、取消或查询”的恢复动作。复测变式：仅把第二下游从 250 ms 响应改为连接池耗尽；不变量=k-41 不重复写入，预期变化=停止接流量并快速失败，证据=关闭时间线与幂等日志。命题边界：不得把框架默认超时、进程管理器重启或网关重试当作端到端可靠性证明。
- 通过标准：所有外部 I/O 共享可计算的截止时间并传播取消；客户端断开后不继续无意义工作；重试仅用于允许重试且具备退避/抖动的操作；写入以幂等键去重；响应流尊重背压；日志可用 request/trace id 串联；就绪与存活检查分离；SIGTERM 后停止接新流量、等待在途请求并在上限内退出；压力测试中延迟、错误率、内存和句柄数有预算且无持续泄漏。评估边界：只在本机 happy path 返回 JSON、依赖无限重试或用扩容掩盖阻塞不能通过。 证据映射：能力结论=指定日志/快照/散列；失败处理=固定夹具回归；边界选择=题 2 的机制说明。否决项：无固定输入、无失败证据或只交成功截图。
- 预计耗时：资料 120 分钟；练习 180 分钟；项目 165 分钟；考核 105 分钟；复测 90 分钟

### 命令行与接口工具

## NODE-03 CLI 参数、退出码与用户体验

- [ ] 自评已掌握
- [ ] 已通过严格考核
- 学习资料：[07–09 内容审计中文讲义：CLI 契约](../chinese-guides/content-audit-07-09.md#node-03)、[中文｜从命令行运行 Node.js 脚本](https://nodejs.cn/learn/command-line/run-nodejs-scripts-from-the-command-line)。覆盖分工：本地讲义完整承担参数/子命令、配置优先级、stdout/stderr、退出码、确认、信号、dry-run、机器输出和零副作用验收；Node 页只查证文件/shebang/`--eval`/`--watch`/`node --run` 的运行方式，不能独立覆盖 CLI 契约。覆盖范围：CLI 参数/子命令、配置优先级、stdout/stderr、退出码、信号、dry-run 和机器可读输出；覆盖交互/CI 两种使用模式；Commander 英文 README 已从必读资源移除。
- 严格考核：挑战类型：TOOL_OPERATION；首考题 1（资料定位）：只允许使用《07–09 内容审计中文讲义：CLI 契约》《中文｜从命令行运行 Node.js 脚本》，定位契约、信号和反例；首考题 2（机制解释）：闭卷说明 `sync --config file --format json --dry-run` 从参数、环境、配置到执行计划的优先级，并区分人类诊断和机器消费的通道/退出语义；首考题 3（最小产出）：固定输入为冲突的 `--endpoint`、`APP_ENDPOINT`、配置文件，及 3 个待删除对象；实现 `sync`/`validate` 子命令、JSON stdout、诊断 stderr、dry-run、`--yes` 确认和退出码表。提交四组命令逐字输出、CI 非交互结果和 SIGINT 零副作用证明；可观察验证：按固定夹具断言关键状态，并提交日志或快照；首考题 4（受限排错）：仅可改参数/输出/信号层；注入未知 flag、无效配置、管道关闭(EPIPE)、SIGINT 于写入前后四个候选。提交退出码、stdout/stderr 捕获、计划/实际差异、三项假设、排除证据、根因、修复与同夹具回归；失败现象：管道消费者提前关闭触发 EPIPE，SIGINT 后仍尝试写入；首考题 5（掌握挑战）：把同一命令的失败解释给脚本作者和人工操作者，说明为何 JSON stdout 不能混入进度文字、何时应返回可重试错误。复测变式：仅在 CI 夹具中撤销 `--yes`；不变量=待删除对象与 JSON 输出契约不变，预期变化=退出码表示拒绝且零删除，证据=stdout/stderr 捕获与执行计划。证据要求：提交固定夹具输入、预期/实际、关键事件序列、根因和自动化回归。命题边界：只在所列资料和覆盖范围内命题。
- 通过标准：四组退出码/通道与契约表一致（证据=捕获文件）；dry-run 和 SIGINT 均无持久化副作用（证据=前后清单/散列）；CI 可消费 JSON 且人类诊断只在 stderr（证据=管道测试）。否决项：破坏操作默认执行、混写 stdout/stderr、或没有给出信号时点证据，均不能通过。 证据映射：能力结论=指定日志/快照/散列；失败处理=固定夹具回归；边界选择=题 2 的机制说明。否决项：无固定输入、无失败证据或只交成功截图。 评估边界：仅以本点资料、题目输入与上述可复核产出评分，资料外经验不能替代证据。
- 预计耗时：资料 120 分钟；练习 180 分钟；项目 165 分钟；考核 105 分钟；复测 90 分钟

## API-01 OpenAPI/YAML 解析与规范化

- [ ] 自评已掌握
- [ ] 已通过严格考核
- 学习资料：[07–09 内容审计中文讲义：规范读取与规范化](../chinese-guides/content-audit-07-09.md#api-01)。覆盖范围：OpenAPI 3.2/YAML/JSON Schema 方言、路径/Webhook、参数、媒体类型、安全方案、`$ref`/动态引用、Overlay/多文档组合、版本识别、规范化、结构 diff 和定位诊断。07–09 讲义已覆盖完整 fixture、失败边界和验收，重复的进阶讲义已移出；本点负责规范读取与规范化，不负责业务模型设计或消费者发布门禁；考核必须提供完整、多文件且含错误的规范 fixture；三份英文规范页已从必读资源移除。
- 严格考核：挑战类型：DEBUGGING；首考题 1（资料定位）：只允许使用《07–09 内容审计中文讲义：规范读取与规范化》，定位方言、引用和诊断边界；首考题 2（机制解释）：闭卷说明多文件 YAML 如何先解析、解析 `$ref` 图、识别 OpenAPI/JSON Schema 方言，再生成稳定的 operation 中间表示；指出 example 为什么不是类型；首考题 3（最小产出）：固定夹具为 20 份规范：3.0/3.1/3.2、两个文件、Webhook、两个 content type、循环/远程 `$ref`、缺 operationId 和错误缩进。输出规范化 JSON、每项源位置和结构 diff；提交 fixture 清单、IR 快照与诊断 JSON；验证：执行固定夹具并记录断言/日志；首考题 4（受限排错）：仅可改读取/解析/规范化层；注入 `$ref` A→B→A、远程域名不在 allowlist、同名 operation、`oneOf` 分支歧义四个候选。提交精确路径、预期/实际、三项假设、排除证据、拒绝/降级策略和同夹具回归；首考题 5（掌握挑战）：向生成器维护者解释“可解析”与“可安全规范化”的差别，现场把一个 example 与 schema 冲突案例归为诊断而非类型推断。复测变式：仅把一份规范替换为 Overlay 后版本；不变量=原 `$ref` 图不变，预期变化=结构 diff 给出破坏/潜在破坏/安全扩展分类，证据=IR 快照与诊断 JSON。证据要求：提交固定夹具输入、预期/实际、关键事件序列、根因和自动化回归。命题边界：只在所列资料和覆盖范围内命题。
- 通过标准：20 个固定夹具全部产生期望 IR/诊断（证据=快照）；版本/方言、源路径、引用策略均显式（证据=规范化报告）；循环和未允许远程引用有上限且无挂起（证据=失败回归）；兼容性分类可回指变更节点（证据=diff）。否决项：静默吞掉解析错误、把 example 当类型、或不报告引用源路径，均不能通过。 证据映射：能力结论=指定日志/快照/散列；失败处理=固定夹具回归；边界选择=题 2 的机制说明。否决项：无固定输入、无失败证据或只交成功截图。 评估边界：仅以本点资料、题目输入与上述可复核产出评分，资料外经验不能替代证据。
- 预计耗时：资料 120 分钟；练习 180 分钟；项目 165 分钟；考核 105 分钟；复测 90 分钟

## API-02 类型推断、代码生成与可维护输出

- [ ] 自评已掌握
- [ ] 已通过严格考核
- 学习资料：[07–09 内容审计中文讲义：确定性代码生成](../chinese-guides/content-audit-07-09.md#api-02)。覆盖范围：规范化 Schema 到类型/服务代码的 AST 映射、命名冲突、确定性格式化、增量更新和 typecheck；07–09 讲义已覆盖代码路径、失败回滚和 typecheck 验收，重复的核心讲义已移出；不承担 API 业务语义设计；现有 service 约定只允许作为题目给出的输入样例，不作为默认评分依据。TypeScript Compiler API、OpenAPI、Prettier 与 Node.js Packages 页已从必读资源移除。
- 严格考核：挑战类型：CODING；首考题 1（资料定位）：只允许使用《07–09 内容审计中文讲义：确定性代码生成》，定位 AST 映射、命名与回滚边界；首考题 2（机制解释）：闭卷说明规范化 operation/schema 如何变为命名稳定的类型和 service AST，何处保留手写扩展、为何格式化不能掩盖非确定顺序；首考题 3（最小产出）：固定输入为含同名 `User`、`a-b`/`a_b` 路径、nullable union 和手写 `extensions.ts` 的 IR；运行两次生成 `types.ts`/`client.ts`，提交字节 diff、命名映射、typecheck、以及只改一个 schema 字段后的审查 diff；可观察验证：按固定夹具断言关键状态，并提交日志或快照；首考题 4（受限排错）：仅可改映射/写入层；注入命名碰撞、无效 TS 标识符、循环类型、写入中断四个候选。提交生成目录前后散列、错误节点、预期/实际、三项假设、排除证据、原子回滚和同夹具回归；首考题 5（掌握挑战）：评审“生成器输出可读”与“用户可安全维护”的区别，说明为何不能覆盖 `extensions.ts`，并对一处 schema 变更给出可审查的影响说明。复测变式：仅打乱 IR 中 operation 顺序；不变量=既有 schema 与 `extensions.ts` 不变，预期变化=字节 diff 为空，证据=两轮产物散列与 typecheck。证据要求：提交固定夹具输入、预期/实际、关键事件序列、根因和自动化回归。命题边界：只在所列资料和覆盖范围内命题。
- 通过标准：同一输入两次输出字节级相同（证据=散列/diff）；全部 fixture typecheck 且命名映射无歧义（证据=编译日志）；写入失败不破坏旧生成物或手写扩展（证据=前后散列）；单字段变更仅产生预期 diff（证据=审查报告）。否决项：靠排序偶然通过、直接字符串拼接而无诊断、或覆盖手写文件，均不能通过。 证据映射：能力结论=指定日志/快照/散列；失败处理=固定夹具回归；边界选择=题 2 的机制说明。否决项：无固定输入、无失败证据或只交成功截图。 评估边界：仅以本点资料、题目输入与上述可复核产出评分，资料外经验不能替代证据。
- 预计耗时：资料 120 分钟；练习 180 分钟；项目 165 分钟；考核 105 分钟；复测 90 分钟

## 领域综合考核

- [ ] 已通过领域综合考核
- 任务：扩展 `get_apidoc`，完成一个 OpenAPI/YAML 查询与 TS 类型输出能力，同时提供 CLI 和 MCP Tool、测试、缓存、错误、安全说明及评估报告。
- 通过标准：8 小时内完成；至少 25 个测试；CLI/MCP 输出一致；恶意路径和无效 schema 被拒绝；生成结果通过 typecheck 且可重复。评估边界：领域综合考核只依据本领域列出的资料、题目输入和可复核产出；不得用资料外经验替代跨知识点整合证据。 证据映射：能力结论=指定日志/快照/散列；失败处理=固定夹具回归；边界选择=题 2 的机制说明。否决项：无固定输入、无失败证据或只交成功截图。
- 预计耗时：资料 120 分钟；练习 180 分钟；项目 165 分钟；考核 105 分钟；复测 90 分钟
